yazılım sürecimin tarihsel olarak en başından en sonuna kadar projelerim veya readme'leri:
1) snake: Klasik bir yılan oyunu sunan Python projesi.
"import turtle
import time
import random

hiz = 0.15

#pencere ayarları
pencere = turtle.Screen() #pencere oluşturma
pencere.title("Snake")    #başlık
pencere.bgcolor('black')  #arka plan rengi
pencere.setup(width=600, height=600) #pencere boyutları
pencere.tracer(0)         #pencerenin güncellenmesi engellenir


#yılanın kafası
kafa = turtle.Turtle()   #bir nesne oluşturur
kafa.speed(0)            #hız 0
kafa.shape('triangle')   #kafa şekli
kafa.color('lightblue')  #kafa rengi
kafa.penup()             #hareket anında çizgi çekmeyecek
kafa.goto(0, 100)        #kafa başlangıç noktası
kafa.direction = 'stop'  #kafanın yönü sabit


#hareket fonksiyonu
def move():
    if(kafa.direction == 'up'):        #kafanın yönü yukarıysa
        kafa.setheading(90)            #yön yukarı tarafa doğru
        kafa.sety(kafa.ycor() + 20)    #değişken sayısal olarak arttırılıp yılan hareket ettirilir
    if(kafa.direction == 'down'):
        kafa.setheading(270)
        kafa.sety(kafa.ycor() - 20)
    if(kafa.direction == 'right'):
        kafa.setheading(0)
        kafa.setx(kafa.xcor() + 20)
    if(kafa.direction == 'left'):
        kafa.setheading(180)
        kafa.setx(kafa.xcor() - 20)

#hareket yön fonksiyonları
def goUp():
    if (kafa.direction != 'down'): #kafa aşağı değilse
        kafa.direction = 'up'      #kafanın yönü yukarıdır

def goDown():
    if (kafa.direction != 'up'):
        kafa.direction = 'down'

def goRight():
    if (kafa.direction != 'left'):
        kafa.direction = 'right'

def goLeft():
    if (kafa.direction != 'right'):
        kafa.direction = 'left'

#yemek
yemek = turtle.Turtle()   #bir nesne oluşturur
yemek.speed(0)            #yemek hızı 0
yemek.shape('circle')     #yemek şekli
yemek.color('red')        #yemek rengi
yemek.penup()             #hareket anında çizgi çizmeyecek
yemek.goto(0, 0)          #yemek başlangıç noktası
yemek.shapesize(0.5,0.5)  #kafanın yönü sabit


kuyruklar = []

puan = 0

#puan kısmı
yaz = turtle.Turtle()   #bir nesne oluşturur
yaz.speed(0)            #skor hızı 0
yaz.shape('square')     #skor şekli
yaz.color('white')      #skor rengi
yaz.penup()             #hareket anında çizgi çizmeyecek
yaz.goto(0, 250)        #skor başlangıç noktası
yaz.hideturtle()        #şekil kapandı
yaz.write('Puan : {}'.format(puan), align='center', font=('Impact',28,'italic'))


pencere.listen()
pencere.onkey(goUp,'Up')
pencere.onkey(goDown,'Down')
pencere.onkey(goRight,'Right')
pencere.onkey(goLeft,'Left')



while True:
    pencere.update()       #ekran güncellenir
    
    #kafa çarpma kontrolü
    if (kafa.xcor() > 290 or kafa.xcor() < -290 or kafa.ycor() > 290 or kafa.ycor() < -290):
        time.sleep(0.4)          #0.4 saniye bekletti
        kafa.goto(0,0)           #kafa (0,0) noktasına taşınır
        kafa.direction = 'stop'  #kafanın yönü sabit

        for kuyruk in kuyruklar:  #kuyruk pencerenin görünmeyecek bir yerine taşınır
            kuyruk.goto(1000,1000)

        kuyruklar = []             #kuyruklar sıfırlandı
        puan = 0                   #puan sıfırlandı
        yaz.clear()                #önceki puan silinecek
        yaz.write('Puan : {}'.format(puan), align='center', font=('Impact',28,'italic')) #puan tekrardan oluşturuldu s->0

        hiz=0.15


    for kuyruk in kuyruklar:
        if kafa.distance(kuyruk) < 20:  #kafa ile kuyruk çarpışma kontrolü
            time.sleep(0.4)             #bekleme
            kafa.goto(0, 0)             #kafa başlangıç noktasına gider
            kafa.direction = 'stop'     #hareket durur
        
        
            for k in kuyruklar:         #kuyruklar görünmeyecek bir noktaya taşınır
                k.goto(1000, 1000)
        
            kuyruklar.clear()
            puan = 0                   #puan sıfırlanır
            yaz.clear()                #skor yazısı temizlenir
            yaz.write('Puan : {}'.format(puan), align='center', font=('Impact', 28, 'italic'))  #skor yeniden yazılır
            hiz = 0.15                 #hız başlangıç değerine döner
            break                      #kır


    if(kafa.distance(yemek) < 20):
        x=random.randint(-280,280)
        y=random.randint(-280,280)
        yemek.goto(x,y)

        puan +=10     #yemek yendiğinde puan ekleyecek
        yaz.clear()   #önceki puan silinecek
        yaz.write('Puan : {}'.format(puan), align='center', font=('Impact',28,'italic')) #puan tekrardan oluşturuldu


        yeniKuyruk = turtle.Turtle()  #bir nesne oluşturur
        yeniKuyruk.speed(0)           #kuyruk hızı 0
        yeniKuyruk.shape('square')    #kuyruk şekli
        yeniKuyruk.color('green')     #kuyruk rengi
        yeniKuyruk.penup()            #hareket anında çizgi çizmeyecek
        kuyruklar.append(yeniKuyruk)

        hiz -= 0.005                  #yemek yendiğinde hız arttı

    for i in range(len(kuyruklar)-1 ,0 ,-1):  #kuyruklar dizisinin 1 eksiğinden başlayıp 0'a birer azalarak gidiyor   
        x = kuyruklar[i-1].xcor()             #her nesne bir öncekinin yerine geçecek(x koordinatı)
        y = kuyruklar[i-1].ycor()             #her nesne bir öncekinin yerine geçecek(y koordinatı)
        kuyruklar[i].goto(x,y)                #kuyruğun aktif konumu verilir

    if(len(kuyruklar)>0):                     #en baştaki kuyruk kafa yerine geçecek
        x = kafa.xcor()                       #kafa x koordinatı
        y = kafa.ycor()                       #kafa y koordinatı
        kuyruklar[0].goto(x,y)                #en baştaki kuyruk kafa yerine geçer
        
                       
    move()              #fonksiyon çalıştırılır
    time.sleep(hiz)     #belirtilen hızda hız fonksiyonu çalıştırılır"


2) PDF Dönüştürücü
Bu proje Word, Excel, PowerPoint ve Görselleri PDF formatına dönüştüren bir web uygulamasıdır. Flask kullanılarak geliştirilmiştir.

Kullanılan Teknolojiler
Python (Flask)
HTML, CSS, JavaScript
Bootstrap
Proje Kurulumu
1- Depoyu Klonlayın
git clone https://github.com/kullanici/pdf-donusturucu.git
cd pdf-donusturucu
2- Gerekli Bağımlılıkları Yükleyin
pip install -r requirements.txt
3- Projeyi Çalıştırın
python app.py
4- Tarayıcınızda Uygulamayı Açın
Tarayıcınızda aşağıdaki URL'yi açarak uygulamayı kullanabilirsiniz:

http://127.0.0.1:5000
Kullanım
"Dönüştür & İndir" butonuna tıklayarak bir dosya seçin.
Sistem otomatik olarak PDF dosyanızı oluşturacaktır.
Daha fazla detay için 'Bilgi' sayfasını ziyaret edebilirsiniz.

"import os
import comtypes.client
import pythoncom
import unicodedata
import re
from PIL import Image
from flask import Flask, request, render_template, send_file, redirect, url_for, jsonify
from werkzeug.utils import secure_filename

#flask uygulaması başlatılır
app = Flask(__name__)

#yükleme ve dönüştürme klasörleri belirlenir
UPLOAD_FOLDER = "uploads"
CONVERTED_FOLDER = "converted"

#yüklenen ve dönüştürülen PDF dosyaları için klasörler belirlenir
app.config["UPLOAD_FOLDER"] = UPLOAD_FOLDER
app.config["CONVERTED_FOLDER"] = CONVERTED_FOLDER

#klasörler oluşturulur
os.makedirs(UPLOAD_FOLDER, exist_ok=True)
os.makedirs(CONVERTED_FOLDER, exist_ok=True)

#desteklenen dosya türleri bir küme içinde tanımlanır
ALLOWED_EXTENSIONS = {"doc", "docx", "xls", "xlsx", "png", "jpg", "jpeg", "ppt", "pptx"}

#dosya adı standart hale getirilir(Türkçe karakterler kaldırılıp boşluklar "_" ile değiştirilir)
def sanitize_filename(filename):
    nfkd_form = unicodedata.normalize('NFKD', filename) #karakterler ASCII'ye çevrilir
    only_ascii = nfkd_form.encode('ASCII', 'ignore').decode('ASCII') #ASCII olmayan karakterler kaldırılır
    sanitized_name = re.sub(r'[^a-zA-Z0-9_.-]', '_', only_ascii) #geçersiz karakterler "_" ile değiştirilir
    return sanitized_name.lower()

#dosyanın uzantısını kontrol edilir
def allowed_file(filename):
    return "." in filename and filename.rsplit(".", 1)[1].lower() in ALLOWED_EXTENSIONS
    #dosya adı parçalandıktan sonra nokta koyulup uzantı küçük harfe çevrildikten sonra desteklenen dosya türü mü diye kontrol edilir

#dosya PDF'e çevrilir
def convert_to_pdf(file_path):
    ext = os.path.splitext(file_path)[1].lower() #dosya uzantısı belirlenir
    base_name = os.path.basename(file_path).rsplit(".", 1)[0] #dosya ikiye parçalandıktan sonra 0.indexten adı uzantısız alınır
    sanitized_base_name = sanitize_filename(base_name) #dosya adını temizlenir
    pdf_file = os.path.join(app.config["CONVERTED_FOLDER"], sanitized_base_name + ".pdf") #hedef PDF dosya yolu belirlenir

    print(f"\nDönüştürme Başladı: {file_path}")
    print(f"Hedef PDF: {pdf_file}")

    try:
        if ext in [".doc", ".docx"]:  #word
            print("Word dosyası algılandı.")
            pythoncom.CoInitialize() #COM nesnesi başlatılır
            app_word = comtypes.client.CreateObject("Word.Application") #word uygulaması başlatılır
            app_word.Visible = False #word uygulaması arka planda çalışır
            doc = app_word.Documents.Open(os.path.abspath(file_path)) #word belgesi açılır
            doc.SaveAs(os.path.abspath(pdf_file), FileFormat=17) #dosya PDF formatında kaydedilir
            doc.Close()
            app_word.Quit()
            pythoncom.CoUninitialize()

        elif ext in [".xls", ".xlsx"]:  #excel
            print("Excel dosyası algılandı.")
            pythoncom.CoInitialize()
            app_excel = comtypes.client.CreateObject("Excel.Application")
            app_excel.Visible = False
            wb = app_excel.Workbooks.Open(os.path.abspath(file_path))
            wb.ExportAsFixedFormat(0, os.path.abspath(pdf_file))
            wb.Close(False)
            app_excel.Quit()
            pythoncom.CoUninitialize()

        elif ext in [".png", ".jpg", ".jpeg"]:  #image
            print("Görsel dosyası algılandı.")
            img = Image.open(file_path)  #pillow kütüphanesi ile resim açılır
            rgb_img = img.convert("RGB") #RGB formatına çevrilir
            rgb_img.save(pdf_file)       #resim PDF olarak kaydedilir

        elif ext in [".ppt", ".pptx"]:  #powerpoint
            print("PowerPoint dosyası algılandı.")
            pythoncom.CoInitialize()
            app_ppt = comtypes.client.CreateObject("PowerPoint.Application")
            app_ppt.Visible = 1         #arayüz açılmadan çalıştırılır
            presentation = app_ppt.Presentations.Open(os.path.abspath(file_path), WithWindow=False)
            presentation.SaveAs(os.path.abspath(pdf_file), FileFormat=32)
            presentation.Close()
            app_ppt.Quit()
            pythoncom.CoUninitialize()

        else:
            return None

        print(f"✅ PDF OLUŞTURULDU: {pdf_file}")
        return pdf_file

    except Exception as e:
        print(f"Dönüştürme hatası: {e}")
        return None #None döndürülerek hata olduğunu belirten bir sonuç geri gönderilir

@app.route("/", methods=["GET", "POST"]) #flask uygulamasına / (ana sayfa) rotasını tanımlayıp hem GET hem de POST isteklerini kabul eder
def upload_file():
    if request.method == "POST":
        if "file" not in request.files:
            #kullanıcı tarafından yüklenen dosyala 'file' anahtarına sahip değilse bulunamadı hatası döndürür
            return jsonify({"success": False, "error": "Dosya bulunamadı"})

        file = request.files["file"]
        if file.filename == "":
            #kullanıcı dosya seçmeden butona tıklıyorsa seçilmedi hatası döndürür
            return jsonify({"success": False, "error": "Dosya seçilmedi"})

        if file and allowed_file(file.filename):
            #dosya geçerliyse adı dönüştürülür ve uploads/ klasörüne kaydedilir
            sanitized_filename = sanitize_filename(file.filename)
            file_path = os.path.join(app.config["UPLOAD_FOLDER"], sanitized_filename)
            file.save(file_path)

            print(f"✅ Dosya kaydedildi: {file_path}")
            pdf_path = convert_to_pdf(file_path)
            #dosya PDF'E dönüştürülüp kullanıcıya yanıt gönderilir
            if pdf_path:
                return jsonify({"success": True, "filename": os.path.basename(pdf_path)})
            else:
                return jsonify({"success": False, "error": "Dönüştürme başarısız"})

    return render_template("index.html")

#bilgi sayfası
@app.route("/bilgi")
def bilgi():
    return render_template("bilgi.html") #Flask HTML sayfasını tarayıcıya gönderir

@app.route("/download/<filename>") #dosyayı indirmek isteyen kullanıcılar için bir route tanımlanır
def download_file(filename):       #PDF dosyasının yolu belirlenir
    pdf_path = os.path.join(app.config["CONVERTED_FOLDER"], filename) #dosya converted klasöründen alınır
    #dosyanın var olup olmadığının kontrolü yapılır
    if os.path.exists(pdf_path):
        return send_file(pdf_path, as_attachment=True)
    else:
        return "❌ Dosya bulunamadı!", 404

if __name__ == "__main__":
    app.run(debug=True)
#flask uygulaması başlatılır"

3) # Muhammed Esat Demir - Kişisel Portföy Web Sitesi

Bu proje, Yazılım Mühendisliği öğrencisi Muhammed Esat Demir'in kişisel portföyünü, deneyimlerini, yeteneklerini ve iletişim bilgilerini sergilemek amacıyla oluşturulmuş tek sayfalık (one-page) bir web sitesidir. Proje, Bootstrap 3 tabanlı açık kaynaklı **Browny** şablonu kullanılarak geliştirilmiştir.

## 🔍 Proje Hakkında

Bu web sitesi aşağıdaki bölümlerden oluşur:

- **Hakkımda:** Kısa biyografi ve iletişim bilgileri
- **Eğitim:** Eğitim geçmişi ve alınan dersler
- **Yetenekler:** Teknik ve kişisel beceriler
- **Deneyim:** Kulüp faaliyetleri ve staj geçmişi
- **Profil:** LinkedIn ve GitHub bağlantıları
- **Portföy:** Tamamlanan projeler
- **İletişim:** Ziyaretçilerin mesaj gönderebileceği bir iletişim formu

## 🛠️ Kullanılan Teknolojiler

- HTML5 & CSS3
- JavaScript & jQuery
- Bootstrap 3
- Font Awesome
- Animate.css
- Owl Carousel
- Bootsnav

## 📁 Dosya Yapısı

proje-dizini ├── assets/ │ ├── css/ │ ├── js/ │ ├── images/ │ └── download/ ├── index.html └── README.md


## 🎯 Amaç

Bu proje, web geliştirme becerilerini uygulamalı olarak göstermek, CV'yi dijital olarak sunmak ve potansiyel iş/staj başvurularında referans olarak kullanılmak üzere geliştirilmiştir.

## ✨ Şablon Bilgisi

- **Şablon Adı:** Browny - Free Bootstrap One Page Portfolio Resume Template  
- **Versiyon:** 1.0  
- **Tasarımcı:** ThemeSINE  
- **Website:** [https://www.themesine.com](https://www.themesine.com)

## 📜 Lisans

Bu proje, ThemeSINE tarafından sağlanan ücretsiz lisans kapsamında dağıtılmaktadır. Detaylı bilgi için:  
[https://www.themesine.com/license/](https://www.themesine.com/license/)

## 🙌 Teşekkürler

Aşağıdaki açık kaynaklı projeler için teşekkürler:

- [Bootstrap](https://getbootstrap.com/)
- [jQuery](http://jquery.com/)
- [Modernizr](https://modernizr.com/)
- [Sticky.js](http://stickyjs.com/)
- [jQuery Easing](https://github.com/gdsmith/jquery.easing)
- [Bootsnav](http://bootsnav.danurstrap.com/)
- [Pexels](https://www.pexels.com/)
- [Unsplash](https://unsplash.com/)

---

**Not:** Bu proje eğitim amaçlı geliştirilmiştir.

4) İnşaat Proje Yönetim Sistemi
Bu proje, örnek bir inşaat firması senaryosu için geliştirilen web tabanlı bir proje yönetim sistemidir. Sistem, projelerin takibi, kaynak yönetimi, kullanıcı yönetimi ve raporlamaları içermektedir. Hem veritabanı yapısı hem de görsel arayüz belgeleriyle birlikte sunulmuştur.

🚀 Projenin Amacı
İnşaat firmalarının projelerini daha etkin yönetebilmesi için:

Projelerin genel ve detay bilgilerinin izlenmesi,
Kaynak kullanımının takip edilmesi,
Kullanıcı rollerinin tanımlanması,
Raporlama fonksiyonlarının sağlanması amaçlanmıştır.
🛠 Kullanılan Teknolojiler
Backend: Python (Flask mikro web çatısı)
Veritabanı: MySQL (MySQL Connector ile bağlantı)
Frontend:
HTML5, CSS3
Bootstrap 5
Jinja2 Template Engine
JavaScript (form etkileşimleri için)
PDF Oluşturma: pdfkit (rapor çıktısı)
E-posta Servisi: smtplib ile Gmail SMTP
Kimlik Doğrulama: itsdangerous ile token tabanlı işlem
Dokümantasyon: Microsoft Word, PDF
Görseller: PNG formatında arayüz ekran görüntüleri
⚙️ Kurulum ve Çalıştırma
1. Reponun Klonlanması
git clone https://github.com/kullaniciadi/insaat-proje-yonetimi.git && cd insaat-proje-yonetimi
2. Sanal Ortam (Opsiyonel)
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
3. Gerekli Paketlerin Kurulması
pip install -r requirements.txt
4. Veritabanı Kurulumu
insaat.sql dosyasını MySQL üzerinde çalıştırarak veritabanını oluşturun.
Gerekirse app.py içinde veritabanı bağlantı ayarlarını güncelleyin.
5. Sunucuyu Başlatma
python app.py
Tarayıcıdan erişim: http://localhost:5000
🔐 Giriş Bilgisi
Uygulama ilk açıldığında yönetici hesabı veritabanına manuel eklenmelidir.

📄 Lisans
Bu proje eğitim amaçlıdır. Ticari kullanım için izin gereklidir.
"import datetime, re
from flask import Flask, request, jsonify, render_template, redirect, session, make_response
import mysql.connector
import json
import pdfkit
from itsdangerous import URLSafeTimedSerializer, SignatureExpired, BadSignature
import smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart



smtp_server = "smtp.gmail.com"
smtp_port = 587
sender_email = ""
app_password = ""

app = Flask(__name__)

# Veritabanı Bağlantısı
db = mysql.connector.connect(
    host="localhost",
    user="root",   
    password="",  #MySQL şifresi
    database="insaat"
)

cursor = db.cursor()
cursor.execute("SET SESSION sql_mode = ''")

app.secret_key = "gizli-key"
sifre_token_uretecisi = URLSafeTimedSerializer(app.secret_key)


@app.route("/")
def home():
    return "İnşaat Yönetim Sistemi API Çalışıyor!"










@app.route("/kullanici_sil/<int:kid>", methods=["DELETE"])
def sil_kullanici(kid):
    cursor.execute("DELETE FROM kullanici WHERE KID = %s", (kid,))
    db.commit()
    
    return jsonify({"message": f"{kid} ID'li kullanıcı silindi."})



@app.route("/giris", methods=["GET", "POST"])
def kullanici_giris():
    if request.method == "POST":
        email = request.form.get("email")
        sifre = request.form.get("sifre")

        sql = "SELECT KID, KAdi, email, rol FROM kullanici WHERE email = %s AND sifre = %s"
        cursor.execute(sql, (email, sifre))
        kullanici = cursor.fetchone()

        if kullanici:
            session["KID"] = kullanici[0]
            session["KAdi"] = kullanici[1]
            session["email"] = kullanici[2]
            session["rol"] = kullanici[3]
            return redirect("/panel")
        else:
            return render_template("login.html", hata="E-posta veya şifre hatalı!")

    return render_template("login.html")





@app.route("/panel")
def panel():
    if "KAdi" not in session:
        return redirect("/login")
    
    return render_template("panel.html", kullanici=session["KAdi"])



@app.route("/logout")
def logout():
    session.clear()
    return redirect("/login")



@app.route("/api/projeler", methods=["GET"])
def get_projeler():
    cursor.execute("SELECT * FROM proje")
    projeler = cursor.fetchall()

    proje_listesi = []
    for p in projeler:
        proje_listesi.append({
            "PID": p[0],
            "PAdi": p[1],
            "baslangic_tarihi": p[2].strftime('%Y-%m-%d'),
            "bitis_tarihi": p[3].strftime('%Y-%m-%d'),
            "PDurumu": p[4]
        })

    return app.response_class(
        response=json.dumps(proje_listesi, ensure_ascii=False),
        mimetype='application/json'
    )




@app.route("/projeler", methods=["GET", "POST"])
def projeler():
    if "KAdi" not in session:
        return redirect("/login")

    if request.method == "POST":
        data = request.form
        sql = "INSERT INTO proje (PAdi, baslangic_tarihi, bitis_tarihi, PDurumu) VALUES (%s, %s, %s, %s)"
        values = (data["PAdi"], data["baslangic_tarihi"], data["bitis_tarihi"], data["PDurumu"])
        cursor.execute(sql, values)
        db.commit()
        return redirect("/projeler")

    cursor.execute("SELECT * FROM proje")
    projeler = cursor.fetchall()
    proje_listesi = []

    for p in projeler:
        pid = p[0]
        # En son aşamayı al
        cursor.execute("""
            SELECT aciklama FROM proje_asamalari 
            WHERE PID = %s ORDER BY tarih DESC LIMIT 1
        """, (pid,))
        son_asama = cursor.fetchone()
        son_aciklama = son_asama[0] if son_asama else "Bu proje, planlanan sürede ilerlemektedir. İlerleme raporları zamanla eklenecektir."

        proje_listesi.append({
            "PID": pid,
            "PAdi": p[1],
            "baslangic_tarihi": p[2].strftime('%Y-%m-%d'),
            "bitis_tarihi": p[3].strftime('%Y-%m-%d'),
            "PDurumu": p[4],
            "aciklama": son_aciklama
        })

    return render_template("proje.html", projeler=proje_listesi)





@app.route("/proje/<int:pid>", methods=["GET", "POST"])
def proje_detay(pid):
    if "KAdi" not in session:
        return redirect("/login")

    
    # Yeni aşama ekleniyorsa
    if request.method == "POST":
        tarih = request.form["tarih"]
        aciklama = request.form["aciklama"]
        ekleyen_id = session["KID"]
        ekleyen_rol = session["rol"]

        if ekleyen_rol == "yönetici":
            sql = """INSERT INTO proje_asamalari 
             (PID, tarih, aciklama, ekleyen_id, onayli, onaylayan_id)
             VALUES (%s, %s, %s, %s, 1, %s)"""
            cursor.execute(sql, (pid, tarih, aciklama, ekleyen_id, ekleyen_id))
        else:
            sql = """INSERT INTO proje_asamalari 
             (PID, tarih, aciklama, ekleyen_id, onayli, onaylayan_id)
             VALUES (%s, %s, %s, %s, 0, NULL)"""
            cursor.execute(sql, (pid, tarih, aciklama, ekleyen_id))

        db.commit()
        return redirect(f"/proje/{pid}")


    # Proje bilgisi
    cursor.execute("SELECT * FROM proje WHERE PID = %s", (pid,))
    p = cursor.fetchone()
    if not p:
        return "Proje bulunamadı", 404

    proje = {
        "PID": p[0],
        "PAdi": p[1],
        "baslangic_tarihi": p[2].strftime('%Y-%m-%d'),
        "bitis_tarihi": p[3].strftime('%Y-%m-%d'),
        "PDurumu": p[4]
    }

    # Aşamaları çek (ekleyen ve onaylayan bilgileri dahil)
    # Aşamaları çek (ekleyen, onaylayan, düzenleyen dahil)
    cursor.execute("""
    SELECT pa.AID, pa.tarih, pa.aciklama, pa.onayli,
       ekleyen.KAdi AS ekleyen_ad, ekleyen.rol AS ekleyen_rol, ekleyen.KID AS ekleyen_id,
       onaylayan.KAdi AS onaylayan_ad, onaylayan.rol AS onaylayan_rol,
       duzenleyen.KAdi AS duzenleyen_ad, duzenleyen.rol AS duzenleyen_rol,
       pa.duzenleme_tarihi, pa.eski_aciklama
    FROM proje_asamalari pa
    JOIN kullanici ekleyen ON pa.ekleyen_id = ekleyen.KID
    LEFT JOIN kullanici onaylayan ON pa.onaylayan_id = onaylayan.KID
    LEFT JOIN kullanici duzenleyen ON pa.duzenleyen_id = duzenleyen.KID
    WHERE pa.PID = %s
    ORDER BY pa.tarih ASC""", (pid,))

    asamalar = []
    for a in cursor.fetchall():
        asamalar.append({
        "aid": a[0],
        "tarih": a[1].strftime('%Y-%m-%d'),
        "aciklama": a[2],
        "onayli": bool(a[3]),
        "ekleyen_ad": a[4],
        "ekleyen_rol": a[5],
        "ekleyen_id": a[6],
        "onaylayan_ad": a[7] if a[7] else None,
        "onaylayan_rol": a[8] if a[8] else None,
        "duzenleyen_ad": a[9] if a[9] else None,
        "duzenleyen_rol": a[10] if a[10] else None,
        "duzenleme_tarihi": a[11].strftime('%Y-%m-%d %H:%M') if a[11] else None,
        "eski_aciklama": a[12]
    })



    # En güncel açıklamayı çek
    cursor.execute("""
    SELECT aciklama 
    FROM proje_asamalari 
    WHERE PID = %s 
    ORDER BY tarih DESC, AID DESC 
    LIMIT 1""", (pid,))
    aciklama_son = cursor.fetchone()
    guncel_aciklama = aciklama_son[0] if aciklama_son else "Bu proje, planlanan sürede ilerlemektedir."

    return render_template("proje_detay.html", proje=proje, asamalar=asamalar, guncel_aciklama=guncel_aciklama)




@app.route("/proje/<int:pid>/asama-onayla/<int:aid>", methods=["POST"])
def asama_onayla(pid, aid):
    if session.get("rol") != "yönetici":
        return "Yetkisiz işlem", 403

    onaylayan_id = session["KID"]
    sql = """
        UPDATE proje_asamalari 
        SET onayli = 1, onaylayan_id = %s 
        WHERE AID = %s
    """
    cursor.execute(sql, (onaylayan_id, aid))
    db.commit()
    return redirect(f"/proje/{pid}")







@app.route("/proje/<int:pid>/asama-duzenle/<int:aid>", methods=["POST"])
def asama_duzenle(pid, aid):
    if "KID" not in session or session.get("rol") != "yönetici":
        return "Yetkisiz işlem", 403

    yeni_aciklama = request.form["aciklama"]
    duzenleyen_id = session["KID"]
    now = datetime.datetime.now()

    # Mevcut açıklamayı çek
    cursor.execute("SELECT aciklama FROM proje_asamalari WHERE AID = %s", (aid,))
    mevcut = cursor.fetchone()
    eski_aciklama = mevcut[0] if mevcut else None

    # Güncelleme sorgusu
    sql = """
        UPDATE proje_asamalari 
        SET aciklama = %s, 
            eski_aciklama = %s,
            duzenleyen_id = %s,
            duzenleme_tarihi = %s
        WHERE AID = %s
    """
    cursor.execute(sql, (yeni_aciklama, eski_aciklama, duzenleyen_id, now, aid))
    db.commit()

    return redirect(f"/proje/{pid}")






@app.route("/proje/<int:pid>/asama-ekle", methods=["POST"])
def asama_ekle(pid):
    if "KID" not in session:
        return redirect("/login")

    tarih = request.form.get("tarih")
    aciklama = request.form.get("aciklama")
    ekleyen_id = session["KID"]
    ekleyen_rol = session["rol"]

    if ekleyen_rol == "yönetici":
        # otomatik onayla
        sql = """INSERT INTO proje_asamalari 
                 (PID, tarih, aciklama, ekleyen_id, onayli, onaylayan_id)
                 VALUES (%s, %s, %s, %s, 1, %s)"""
        cursor.execute(sql, (pid, tarih, aciklama, ekleyen_id, ekleyen_id))
    else:
        sql = """INSERT INTO proje_asamalari 
                 (PID, tarih, aciklama, ekleyen_id, onayli, onaylayan_id)
                 VALUES (%s, %s, %s, %s, 0, NULL)"""
        cursor.execute(sql, (pid, tarih, aciklama, ekleyen_id))

    db.commit()
    return redirect(f"/proje/{pid}")





@app.route("/proje-duzenle/<int:pid>", methods=["GET", "POST"])
def proje_duzenle(pid):
    if request.method == "POST":
        data = request.form
        sql = """
            UPDATE proje 
            SET PAdi = %s, baslangic_tarihi = %s, bitis_tarihi = %s, PDurumu = %s
            WHERE PID = %s
        """
        values = (
            data["PAdi"],
            data["baslangic_tarihi"],
            data["bitis_tarihi"],
            data["PDurumu"],
            pid
        )
        cursor.execute(sql, values)
        db.commit()
        return redirect("/projeler")

    cursor.execute("SELECT * FROM proje WHERE PID = %s", (pid,))
    p = cursor.fetchone()
    proje = {
        "PID": p[0],
        "PAdi": p[1],
        "baslangic_tarihi": p[2].strftime('%Y-%m-%d'),
        "bitis_tarihi": p[3].strftime('%Y-%m-%d'),
        "PDurumu": p[4]
    }
    return render_template("proje_duzenle.html", proje=proje)





@app.route("/proje_ekle", methods=["POST"])
def proje_ekle():
    data = request.get_json()

    sql = "INSERT INTO proje (PAdi, baslangic_tarihi, bitis_tarihi, PDurumu) VALUES (%s, %s, %s, %s)"
    values = (data["PAdi"], data["baslangic_tarihi"], data["bitis_tarihi"], data["PDurumu"])

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": "Proje başarıyla eklendi!"})




@app.route("/proje_guncelle/<int:pid>", methods=["PUT"])
def proje_guncelle(pid):
    data = request.get_json()

    sql = """
        UPDATE proje 
        SET PAdi = %s, baslangic_tarihi = %s, bitis_tarihi = %s, PDurumu = %s
        WHERE PID = %s
    """
    values = (data["PAdi"], data["baslangic_tarihi"], data["bitis_tarihi"], data["PDurumu"], pid)

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": f"{pid} ID'li proje güncellendi."})




@app.route("/proje_sil/<int:pid>", methods=["DELETE"])
def proje_sil(pid):
    cursor.execute("DELETE FROM proje WHERE PID = %s", (pid,))
    db.commit()

    return jsonify({"message": f"{pid} ID'li proje silindi."})







@app.route("/kaynak_ekle", methods=["POST"])
def kaynak_ekle():
    data = request.get_json()

    sql = """
        INSERT INTO kaynak (PID, tip, KAdi, miktar, KDurumu)
        VALUES (%s, %s, %s, %s, %s)
    """
    values = (
        data["PID"],
        data["tip"],
        data["KAdi"],
        data["miktar"],
        data["KDurumu"]
    )

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": "Kaynak başarıyla eklendi!"})



@app.route("/kaynak_guncelle/<int:kaid>", methods=["PUT"])
def kaynak_guncelle(kaid):
    data = request.get_json()

    sql = """
        UPDATE kaynak 
        SET PID = %s, tip = %s, KAdi = %s, miktar = %s, KDurumu = %s
        WHERE KAID = %s
    """
    values = (
        data["PID"],
        data["tip"],
        data["KAdi"],
        data["miktar"],
        data["KDurumu"],
        kaid
    )

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": f"{kaid} ID'li kaynak güncellendi."})



@app.route("/kaynak_sil/<int:kaid>", methods=["DELETE"])
def kaynak_sil(kaid):
    cursor.execute("DELETE FROM kaynak WHERE KAID = %s", (kaid,))
    db.commit()

    return jsonify({"message": f"{kaid} ID'li kaynak silindi."})




@app.route("/proje/<int:pid>/kaynaklar", methods=["GET", "POST"])
def proje_kaynaklar(pid):
    if "KID" not in session:
        return redirect("/login")

    if request.method == "POST":
        kaid = request.form.get("kaid")
        tarih = request.form.get("tarih")
        kullanilan_miktar = request.form.get("kullanilan_miktar")
        aciklama = request.form.get("aciklama")

        sql = """
            INSERT INTO kaynak_kullanim (KAID, tarih, kullanilan_miktar, aciklama)
            VALUES (%s, %s, %s, %s)
        """
        cursor.execute(sql, (kaid, tarih, kullanilan_miktar, aciklama))
        db.commit()
        return redirect(f"/proje/{pid}/kaynaklar")

    # Proje bilgisi
    cursor.execute("SELECT * FROM proje WHERE PID = %s", (pid,))
    p = cursor.fetchone()
    proje = {
        "PID": p[0],
        "PAdi": p[1]
    }

    # Kaynaklar
    cursor.execute("SELECT * FROM kaynak WHERE PID = %s", (pid,))
    kaynaklar_raw = cursor.fetchall()
    kaynaklar = [{
        "KAID": k[0],
        "PID": k[1],
        "tip": k[2],
        "KAdi": k[3],
        "miktar": k[4],
        "KDurumu": k[5]
    } for k in kaynaklar_raw]

    # Kullanım geçmişi
    cursor.execute("""
        SELECT k.tarih, k.kullanilan_miktar, k.aciklama 
        FROM kaynak_kullanim k
        JOIN kaynak ka ON k.KAID = ka.KAID
        WHERE ka.PID = %s
        ORDER BY k.tarih DESC
    """, (pid,))
    kullanimlar = [{
        "tarih": row[0].strftime('%Y-%m-%d'),
        "kullanilan_miktar": row[1],
        "aciklama": row[2]
    } for row in cursor.fetchall()]

    return render_template("kaynak.html", proje=proje, kaynaklar=kaynaklar, kullanimlar=kullanimlar)





@app.route("/proje/<int:pid>/kaynak-planla", methods=["GET", "POST"])
def kaynak_planla(pid):
    if "KID" not in session or session["rol"] != "yönetici":
        return "Bu işlem sadece yöneticiler içindir.", 403

    if request.method == "POST":
        kaid = request.form["kaid"]
        miktar = request.form["planlanan_miktar"]

        # Planlama tablosuna ekle
        cursor.execute("""
            INSERT INTO proje_kaynak (PID, KAID, planlanan_miktar)
            VALUES (%s, %s, %s)
        """, (pid, kaid, miktar))
        db.commit()
        return redirect(f"/proje/{pid}/kaynak-detay")

    # Proje adını al
    cursor.execute("SELECT PAdi FROM proje WHERE PID = %s", (pid,))
    proje_adi = cursor.fetchone()[0]

    # Kaynak tanımlarını çek
    cursor.execute("SELECT KTipID, KAdi, tip FROM kaynak_tanim ORDER BY tip, KAdi")
    kaynaklar = cursor.fetchall()

    # Türlerine göre ayır
    malzeme = [ (k[0], k[1]) for k in kaynaklar if k[2] == "Malzeme" ]
    ekipman = [ (k[0], k[1]) for k in kaynaklar if k[2] == "Ekipman" ]
    personel = [ (k[0], k[1]) for k in kaynaklar if k[2] == "Personel" ]

    return render_template(
        "kaynak_planla.html",
        pid=pid,
        proje_adi=proje_adi,
        malzeme=malzeme,
        ekipman=ekipman,
        personel=personel
    )




@app.route("/kaynaklar")
def kaynaklar_sayfasi():
    if "KID" not in session:
        return redirect("/login")

    cursor.execute("SELECT * FROM proje")
    projeler = cursor.fetchall()
    proje_listesi = [{
        "PID": p[0],
        "PAdi": p[1],
        "baslangic_tarihi": p[2].strftime('%Y-%m-%d'),
        "bitis_tarihi": p[3].strftime('%Y-%m-%d'),
        "PDurumu": p[4]
    } for p in projeler]

    return render_template("kaynak_projeler.html", projeler=proje_listesi)





@app.route("/proje/<int:pid>/kaynak-detay", methods=["GET"])
def kaynak_detay(pid):
    if "KID" not in session:
        return redirect("/login")

    # Proje bilgisi
    cursor.execute("SELECT PAdi FROM proje WHERE PID = %s", (pid,))
    proje = cursor.fetchone()
    if not proje:
        return "Proje bulunamadı", 404

    proje_info = {"PID": pid, "PAdi": proje[0]}

    # Kullanım zaman çizelgesi (soldaki liste)
    cursor.execute("""
        SELECT kk.tarih, kt.KAdi, kk.kullanilan_miktar, kk.aciklama
        FROM kaynak_kullanim kk
        JOIN kaynak k ON kk.KAID = k.KAID
        JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
        WHERE k.PID = %s
        ORDER BY kk.tarih DESC
    """, (pid,))
    kullanimlar = [{
        "tarih": row[0].strftime('%Y-%m-%d'),
        "kaynagin_adi": row[1],
        "kullanilan_miktar": row[2],
        "aciklama": row[3]
    } for row in cursor.fetchall()]

    # Kaynak tanımları (sol kullanım için)
    cursor.execute("""
        SELECT KTipID, KAdi, tip
        FROM kaynak_tanim
        ORDER BY tip, KAdi
    """)
    kullanim_kaynaklar = [{
        "KTipID": row[0],
        "KAdi": row[1],
        "tip": row[2]
    } for row in cursor.fetchall()]

    # Sağdaki Planlama Karşılaştırması (planlanan, mevcut, fark)
    cursor.execute("""
        SELECT 
             kt.KAdi,
            SUM(pk.planlanan_miktar),
            COALESCE(SUM(kk.kullanilan_miktar), 0),
            kt.tip,
            (SUM(pk.planlanan_miktar) - COALESCE(SUM(kk.kullanilan_miktar), 0)) AS fark
        FROM proje_kaynak pk
        JOIN kaynak k ON pk.KAID = k.KAID
        JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
        LEFT JOIN kaynak_kullanim kk ON kk.PKID = pk.PKID
        WHERE pk.PID = %s
        GROUP BY kt.KAdi, kt.tip
        HAVING SUM(pk.planlanan_miktar) > 0
        ORDER BY FIELD(kt.tip, 'Malzeme', 'Ekipman', 'Personel'), kt.KAdi
    """, (pid,))



    planlamalar = [{
        "KAdi": row[0],
        "planlanan_miktar": row[1],
        "mevcut_miktar": row[2],
        "tip": row[3],
        "fark": row[4]
    } for row in cursor.fetchall()]

    return render_template(
        "kaynak_detay.html",
        proje=proje_info,
        kullanimlar=kullanimlar,
        kullanim_kaynaklar=kullanim_kaynaklar,
        planlama_kaynaklar=kullanim_kaynaklar,
        planlamalar=planlamalar
    )





@app.route("/proje/<int:pid>/kaynak-kullanim-ekle", methods=["POST"])
def kaynak_kullanim_ekle(pid):
    if "KID" not in session:
        return redirect("/login")

    kaid_tanim = request.form.get("kaid")
    tarih = request.form.get("tarih")
    kullanilan_miktar = request.form.get("kullanilan_miktar")
    aciklama = request.form.get("aciklama")

    if not kaid_tanim:
        return "Bir kaynak seçilmedi!", 400

    # Kaynak adı ve tipi alınıyor
    cursor.execute("SELECT KAdi, tip FROM kaynak_tanim WHERE KTipID = %s", (kaid_tanim,))
    kaynak_tanim = cursor.fetchone()
    if not kaynak_tanim:
        return "Kaynak bulunamadı!", 400

    kaynak_adi, kaynak_tipi = kaynak_tanim

    # kaynak tablosunda bu kaynak var mı kontrolü
    cursor.execute("""
        SELECT KAID FROM kaynak
        WHERE PID = %s AND KAdi = %s
    """, (pid, kaynak_adi))
    mevcut_kaynak = cursor.fetchone()

    if mevcut_kaynak:
        proje_kaynak_kaid = mevcut_kaynak[0]
    else:
        cursor.execute("""
            INSERT INTO kaynak (PID, KTipID, KAdi, miktar, KDurumu)
            VALUES (%s, %s, %s, 0, 'aktif')
        """, (pid, kaid_tanim, kaynak_adi))
        db.commit()
        proje_kaynak_kaid = cursor.lastrowid

    # proje_kaynak'ta var mı kontrolü
    cursor.execute("""
        SELECT PKID FROM proje_kaynak
        WHERE PID = %s AND KAID = %s
    """, (pid, proje_kaynak_kaid))
    proje_kaynak_kontrol = cursor.fetchone()

    if not proje_kaynak_kontrol:
        cursor.execute("""
            INSERT INTO proje_kaynak (PID, KAID, planlanan_miktar)
            VALUES (%s, %s, %s)
        """, (pid, proje_kaynak_kaid, "0"))
        db.commit()
        # yeni oluşturulan PKID'yi alalım
        proje_kaynak_pkid = cursor.lastrowid
    else:
        proje_kaynak_pkid = proje_kaynak_kontrol[0]

    # 🔥 HATA BURADA OLUYORDU: şimdi doğru PKID'yi ekliyoruz
    cursor.execute("""
        INSERT INTO kaynak_kullanim (KAID, tarih, kullanilan_miktar, aciklama, PKID)
        VALUES (%s, %s, %s, %s, %s)
    """, (proje_kaynak_kaid, tarih, kullanilan_miktar, aciklama, proje_kaynak_pkid))

    db.commit()

    return redirect(f"/proje/{pid}/kaynak-detay")







@app.route("/rapor_ekle", methods=["POST"])
def rapor_ekle():
    data = request.get_json()

    sql = """
        INSERT INTO rapor (PID, KID, RTarihi, aciklama)
        VALUES (%s, %s, %s, %s)
    """
    values = (
        data["PID"],
        data["KID"],
        data["RTarihi"],
        data["aciklama"]
    )

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": "Rapor başarıyla eklendi!"})




@app.route("/rapor_sil/<int:rid>", methods=["DELETE"])
def rapor_sil(rid):
    cursor.execute("DELETE FROM rapor WHERE RID = %s", (rid,))
    db.commit()

    return jsonify({"message": f"{rid} ID'li rapor silindi."})





@app.route("/raporlar")
def raporlar():
    if "KID" not in session:
        return redirect("/login")

    # Tüm projeleri çekelim
    cursor.execute("SELECT PID, PAdi, baslangic_tarihi, bitis_tarihi, PDurumu FROM proje")
    projeler = [{
        "PID": row[0],
        "PAdi": row[1],
        "baslangic": row[2].strftime("%Y-%m-%d"),
        "bitis": row[3].strftime("%Y-%m-%d"),
        "durum": row[4]
    } for row in cursor.fetchall()]

    return render_template("raporlar.html", projeler=projeler)





@app.route("/rapor/<int:pid>")
def rapor_goruntule(pid):
    if "KID" not in session:
        return redirect("/login")

    # 1. Proje Bilgileri
    cursor.execute("""
        SELECT PAdi, baslangic_tarihi, bitis_tarihi, PDurumu, Aciklama
        FROM proje
        WHERE PID = %s
    """, (pid,))
    proje_row = cursor.fetchone()
    if not proje_row:
        return "Proje bulunamadı", 404

    proje = {
    "PID": pid,  # <-- Bunu EKLE!
    "PAdi": proje_row[0],
    "baslangic": proje_row[1].strftime("%Y-%m-%d"),
    "bitis": proje_row[2].strftime("%Y-%m-%d"),
    "durum": proje_row[3],
    "aciklama": proje_row[4] if proje_row[4] else ""
}


    # 2. Aşamalar
    cursor.execute("""
        SELECT tarih, aciklama, ekleyen_ad, ekleyen_rol, onaylayan_ad, onaylayan_rol
        FROM proje_asama
        WHERE PID = %s
        ORDER BY tarih
    """, (pid,))
    asamalar = []
    for row in cursor.fetchall():
        asamalar.append({
            "tarih": row[0].strftime("%Y-%m-%d"),
            "aciklama": row[1],
            "ekleyen_ad": row[2],
            "ekleyen_rol": row[3],
            "onaylayan_ad": row[4],
            "onaylayan_rol": row[5]
        })

    # 3. Kullanım Zaman Çizelgesi
    cursor.execute("""
        SELECT kk.tarih, kt.KAdi, kk.kullanilan_miktar, kk.aciklama
        FROM kaynak_kullanim kk
        JOIN kaynak k ON kk.KAID = k.KAID
        JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
        WHERE k.PID = %s
        ORDER BY kk.tarih
    """, (pid,))
    kullanimlar = []
    for row in cursor.fetchall():
        kullanimlar.append({
            "tarih": row[0].strftime("%Y-%m-%d"),
            "kaynagin_adi": row[1],
            "kullanilan_miktar": row[2],
            "aciklama": row[3]
        })

    # 4. Planlanan vs Mevcut Kaynaklar
    cursor.execute("""
    SELECT 
        kt.KAdi,
        SUM(pk.planlanan_miktar),
        COALESCE(SUM(kk.kullanilan_miktar), 0),
        kt.tip,
        (SUM(pk.planlanan_miktar) - COALESCE(SUM(kk.kullanilan_miktar), 0)) AS fark
    FROM proje_kaynak pk
    JOIN kaynak k ON pk.KAID = k.KAID
    JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
    LEFT JOIN kaynak_kullanim kk ON kk.PKID = pk.PKID
    WHERE pk.PID = %s
    GROUP BY kt.KAdi, kt.tip
    HAVING SUM(pk.planlanan_miktar) > 0
    ORDER BY FIELD(kt.tip, 'Malzeme', 'Ekipman', 'Personel'), kt.KAdi
""", (pid,))

    planlamalar = [{
    "KAdi": row[0],
    "planlanan_miktar": row[1],
    "mevcut_miktar": row[2],
    "tip": row[3],
    "fark": row[4]
} for row in cursor.fetchall()]


    return render_template(
        "rapor.html",
        proje=proje,
        asamalar=asamalar,
        kullanimlar=kullanimlar,
        planlamalar=planlamalar
    )







# pdfkit ayarı (wkhtmltopdf yolu)
path_wkhtmltopdf = r"C:\Program Files\wkhtmltopdf\bin\wkhtmltopdf.exe"
config = pdfkit.configuration(wkhtmltopdf=path_wkhtmltopdf)








@app.route("/rapor/<int:pid>/pdf")
def rapor_pdf_indir(pid):
    if "KID" not in session:
        return redirect("/login")

    # --- Proje Bilgileri
    cursor.execute("""
        SELECT PAdi, baslangic_tarihi, bitis_tarihi, PDurumu, Aciklama
        FROM proje
        WHERE PID = %s
    """, (pid,))
    proje_row = cursor.fetchone()
    if not proje_row:
        return "Proje bulunamadı", 404

    proje = {
        "PID": pid,
        "PAdi": proje_row[0],
        "baslangic": proje_row[1].strftime("%Y-%m-%d"),
        "bitis": proje_row[2].strftime("%Y-%m-%d"),
        "durum": proje_row[3],
        "aciklama": proje_row[4] if proje_row[4] else ""
    }

    # --- Aşamalar
    cursor.execute("""
        SELECT tarih, aciklama, ekleyen_ad, ekleyen_rol, onaylayan_ad, onaylayan_rol
        FROM proje_asama
        WHERE PID = %s
        ORDER BY tarih
    """, (pid,))
    asamalar = [{
        "tarih": row[0].strftime("%Y-%m-%d"),
        "aciklama": row[1],
        "ekleyen_ad": row[2],
        "ekleyen_rol": row[3],
        "onaylayan_ad": row[4],
        "onaylayan_rol": row[5]
    } for row in cursor.fetchall()]

    # --- Kullanım Zaman Çizelgesi
    cursor.execute("""
        SELECT kk.tarih, kt.KAdi, kk.kullanilan_miktar, kk.aciklama
        FROM kaynak_kullanim kk
        JOIN kaynak k ON kk.KAID = k.KAID
        JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
        WHERE k.PID = %s
        ORDER BY kk.tarih
    """, (pid,))
    kullanimlar = [{
        "tarih": row[0].strftime("%Y-%m-%d"),
        "kaynagin_adi": row[1],
        "kullanilan_miktar": row[2],
        "aciklama": row[3]
    } for row in cursor.fetchall()]

    # --- Planlanan vs Mevcut
    cursor.execute("""
    SELECT 
        kt.KAdi,
        SUM(pk.planlanan_miktar) AS planlanan_miktar,
        COALESCE(SUM(kk.kullanilan_miktar), 0) AS mevcut_miktar,
        kt.tip,
        (SUM(pk.planlanan_miktar) - COALESCE(SUM(kk.kullanilan_miktar), 0)) AS fark
    FROM proje_kaynak pk
    JOIN kaynak k ON pk.KAID = k.KAID
    JOIN kaynak_tanim kt ON k.KTipID = kt.KTipID
    LEFT JOIN kaynak_kullanim kk ON kk.PKID = pk.PKID
    WHERE pk.PID = %s
    GROUP BY kt.KAdi, kt.tip
    HAVING SUM(pk.planlanan_miktar) > 0
    ORDER BY FIELD(kt.tip, 'Malzeme', 'Ekipman', 'Personel'), kt.KAdi
""", (pid,))






    planlamalar = [{
        "KAdi": row[0],
        "planlanan_miktar": row[1],
        "mevcut_miktar": row[2],
        "fark": float(row[1]) - float(row[2]),
        "tip": row[3]
    } for row in cursor.fetchall()]




    # --- HTML render
    rendered = render_template(
        "rapor_pdf.html",
        proje=proje,
        asamalar=asamalar,
        kullanimlar=kullanimlar,
        planlamalar=planlamalar
    )

    # --- PDF oluştur
    pdf = pdfkit.from_string(rendered, False, configuration=config)

    response = make_response(pdf)
    response.headers['Content-Type'] = 'application/pdf'
    response.headers['Content-Disposition'] = f'inline; filename=proje_{pid}_raporu.pdf'
    return response







def extract_number(text):
    match = re.search(r"(\d+(\.\d+)?)", str(text))
    return float(match.group(1)) if match else 0.0





@app.route("/asama_ekle_api", methods=["POST"])
def asama_ekle_api():
    if "KID" not in session:
        return redirect("/login")

    data = request.get_json()

    sql = """
        INSERT INTO proje_asama (PID, tarih, aciklama, ekleyen_ad, ekleyen_rol, onaylayan_ad, onaylayan_rol)
        VALUES (%s, %s, %s, %s, %s, %s, %s)
    """
    values = (
        data["PID"],
        data["tarih"],
        data["aciklama"],
        data["ekleyen_ad"],
        data["ekleyen_rol"],
        data.get("onaylayan_ad"),
        data.get("onaylayan_rol")
    )

    cursor.execute(sql, values)
    db.commit()

    return jsonify({"message": "Aşama başarıyla eklendi!"})




@app.route("/kullanicilar")
def kullanicilar():
    if "KID" not in session:
        return redirect("/login")

    cursor.execute("SELECT KID, KAdi, email, rol FROM kullanici")
    kullanicilar = [{
        "KID": row[0],
        "KAdi": row[1],
        "email": row[2],
        "rol": row[3]
    } for row in cursor.fetchall()]

    return render_template("kullanicilar.html", kullanicilar=kullanicilar)






@app.route("/kullanici_ekle", methods=["GET", "POST"])
def kullanici_ekle():
    if "KID" not in session:
        return redirect("/login")

    if request.method == "POST":
        ad = request.form.get("ad")
        email = request.form.get("email")
        sifre = request.form.get("sifre")
        rol = request.form.get("rol")

        sql = "INSERT INTO kullanici (KAdi, email, sifre, rol) VALUES (%s, %s, %s, %s)"
        cursor.execute(sql, (ad, email, sifre, rol))
        db.commit()
        return redirect("/kullanicilar")

    return render_template("kullanici_ekle.html")








@app.route("/kullanici_sil/<int:kid>", methods=["POST"])
def kullanici_sil(kid):
    if "KID" not in session:
        return redirect("/login")

    cursor.execute("DELETE FROM kullanici WHERE KID = %s", (kid,))
    db.commit()
    return redirect("/kullanicilar")






@app.route("/kullanici_duzenle/<int:kid>", methods=["GET", "POST"])
def kullanici_duzenle(kid):
    if "KID" not in session:
        return redirect("/login")

    if request.method == "POST":
        ad = request.form.get("ad")
        email = request.form.get("email")
        sifre = request.form.get("sifre")
        rol = request.form.get("rol")

        sql = "UPDATE kullanici SET KAdi = %s, email = %s, sifre = %s, rol = %s WHERE KID = %s"
        cursor.execute(sql, (ad, email, sifre, rol, kid))
        db.commit()
        return redirect("/kullanicilar")

    cursor.execute("SELECT KAdi, email, sifre, rol FROM kullanici WHERE KID = %s", (kid,))  # << BURADA KAdi doğru
    row = cursor.fetchone()
    if not row:
        return "Kullanıcı bulunamadı", 404

    kullanici = {
        "KAdi": row[0],   
        "email": row[1],
        "sifre": row[2],
        "rol": row[3]
    }


    return render_template("kullanici_duzenle.html", kullanici=kullanici, KID=kid)















@app.route("/kayit", methods=["GET", "POST"])
def kayit():
    if request.method == "POST":
        data = request.form

        if data["sifre"] != data["sifre_tekrar"]:
            return render_template("kayit.html", hata="Şifreler eşleşmiyor!")

        # Kullanıcının gönderdiği "rol" yerine sabit olarak 'çalışan' atanıyor
        sql = "INSERT INTO kullanici (KAdi, email, sifre, rol) VALUES (%s, %s, %s, %s)"
        values = (data["adsoyad"], data["email"], data["sifre"], "çalışan")
        try:
            cursor.execute(sql, values)
            db.commit()
            return redirect("/login")
        except Exception as e:
            return render_template("kayit.html", hata="Veritabanı hatası: " + str(e))

    return render_template("kayit.html")




@app.route("/login", methods=["GET"])
def login():
    return render_template("login.html")



@app.route("/sifremi-unuttum", methods=["GET", "POST"])
def sifremi_unuttum():
    if request.method == "POST":
        email = request.form["email"]

        # Veritabanında e-posta var mı kontrol et
        cursor.execute("SELECT * FROM kullanici WHERE email = %s", (email,))
        kullanici = cursor.fetchone()

        if kullanici:
            token = sifre_token_uretecisi.dumps(email, salt="sifre-salt")
            sifre_sifirlama_maili_gonder(email, token)
            mesaj = "Şifre sıfırlama bağlantısı e-posta adresinize gönderildi."
        else:
            mesaj = "Bu e-posta adresiyle kayıtlı kullanıcı bulunamadı."

        return render_template("sifremi_unuttum.html", mesaj=mesaj)

    return render_template("sifremi_unuttum.html")




@app.route("/sifre-sifirla/<token>", methods=["GET", "POST"])
def sifre_sifirla(token):
    try:
        email = sifre_token_uretecisi.loads(token, salt="sifre-salt", max_age=1800)  # 30 dakika geçerli
    except SignatureExpired:
        return "🔐 Bağlantı süresi dolmuş. Lütfen yeniden talep edin."
    except BadSignature:
        return "❌ Geçersiz bağlantı. Güvenlik nedeniyle işlem yapılamıyor."

    if request.method == "POST":
        yeni_sifre = request.form["sifre"]
        sifre_tekrar = request.form["sifre_tekrar"]

        if yeni_sifre != sifre_tekrar:
            return render_template("sifre_sifirla.html", hata="Şifreler eşleşmiyor!", email=email)

        # Şifreyi güncelle
        sql = "UPDATE kullanici SET sifre = %s WHERE email = %s"
        cursor.execute(sql, (yeni_sifre, email))
        db.commit()

        return redirect("/login")

    return render_template("sifre_sifirla.html", email=email)





def sifre_sifirlama_maili_gonder(email, token):
    reset_link = f"http://127.0.0.1:5000/sifre-sifirla/{token}"
    
    subject = "Şifre Sıfırlama Talebi"
    body = f"""
Merhaba,

Şifrenizi sıfırlamak için aşağıdaki bağlantıya tıklayın:
{reset_link}

Eğer bu talebi siz oluşturmadıysanız, bu e-postayı yok sayabilirsiniz.

Demir İnşaat
    """

    message = MIMEMultipart()
    message["From"] = sender_email
    message["To"] = email
    message["Subject"] = subject
    message.attach(MIMEText(body, "plain"))

    try:
        with smtplib.SMTP(smtp_server, smtp_port) as server:
            server.starttls()
            server.login(sender_email, app_password)
            server.sendmail(sender_email, email, message.as_string())
            print("E-posta gönderildi.")
    except Exception as e:
        print("E-posta gönderilemedi:", e)


if __name__ == "__main__":
    app.run(debug=True)"

5) OCR Projesi (Python ile Derin Öğrenme)
Bu proje, basılı karakterleri tanımak için bir OCR (Optik Karakter Tanıma) sistemidir. Projede Keras ve EasyOCR kullanılarak eğitilmiş modeller ile karakter tanıma gerçekleştirilmiştir.

🎯 Projenin Amacı
Görseller üzerinden karakter tanıma gerçekleştirmek
Eğitilmiş bir sinir ağı modeli ile OCR uygulamak
Kullanıcı dostu bir arayüz ile kolay test ve kullanım sağlamak
🧰 Kullanılan Teknolojiler
Backend: Python, Flask
ML Framework: TensorFlow + Keras
Frontend: HTML5, CSS, Jinja2(Flask Templates)
Görüntü İşleme: OpenCV, Pillow
Veri: 3 farklı veri seti + easyocr veri seti
📁 Proje Yapısı
arayuz/: Uygulama arayüzü
egitim/: Model eğitimi için kodlar
model/: Eğitilmiş modeller (.keras, .pth)
on_isleme/: Görüntü işleme fonksiyonları
ocr_karisik.py, app.py : OCR demo çalışmaları
veriler/: Örnek veri ve görseller
templates/ : Web arayüzü
uploads/ : Yüklenen Görseller
⚙️ Kurulum ve Çalıştırma
1. Reponun Klonlanması
git clone https://github.com/kullaniciadi/ocr_proje.git
cd ocr_proje
2. Sanal Ortam (Opsiyonel)
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
3. Gerekli Paketlerin Kurulması
pip install -r requirements.txt
4. Web Arayüzü ile OCR Test Etme
python arayuz.py
Tarayıcıdan erişim: http://localhost:5000
📄 Lisans
Bu proje eğitim ve kişisel kullanım içindir. Ticari kullanım için lütfen iletişime geçin.

bu proje için çok fazla sayıda veri kullandım, farklı kaynaklardan hatta kendi veri setimi dahi oluşturup sisteme dahil ettim vs.

6) 🏥 Sosyal Yardım Takip Sistemi
Modern web teknolojileri ile geliştirilmiş, sosyal yardım başvurularını yönetmek için tasarlanmış kapsamlı bir web uygulaması.

📚 Proje Hakkında
Bu proje, Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı Bilgi İşlem Dairesi'nde yapılan staj sürecinde geliştirilmiştir. Proje, sosyal yardım kurumlarının başvuru süreçlerini dijitalleştirmek ve yönetim süreçlerini optimize etmek amacıyla tasarlanmıştır.

🎯 Staj Hedefleri
Modern web teknolojileri ile full-stack uygulama geliştirme
Django REST API ve React frontend entegrasyonu
Rol tabanlı kullanıcı yönetimi ve güvenlik sistemleri
Veritabanı tasarımı ve API geliştirme
Deployment ve CI/CD süreçleri
🌟 Özellikler
🔐 Kullanıcı Yönetimi
Çoklu Rol Sistemi: Admin, Sosyal Çalışan, Yardım Alan, Gönüllü
Güvenli Kimlik Doğrulama: JWT token tabanlı sistem
Profil Yönetimi: Kullanıcı bilgileri ve tercihleri
📋 Başvuru Sistemi
Yeni Başvuru Oluşturma: Detaylı form ile başvuru
Başvuru Takibi: Gerçek zamanlı durum güncellemeleri
Durum Yönetimi: Beklemede, İncelemede, Onaylandı, Reddedildi
Personel Atama: Uzman personel atama sistemi
🎨 Modern Arayüz
Responsive Tasarım: Tüm cihazlarda uyumlu
Material-UI: Google Material Design prensipleri
Kullanıcı Dostu: Sezgisel navigasyon ve işlemler
🔒 Güvenlik
Rol Tabanlı Erişim: Yetki seviyesine göre içerik
API Güvenliği: CORS ve JWT koruması
Veri Validasyonu: Giriş verilerinin doğrulanması
🏗️ Teknoloji Stack
Backend
Framework: Django 5.2.4
API: Django REST Framework
Veritabanı: PostgreSQL (Production) / SQLite (Development)
Kimlik Doğrulama: JWT (JSON Web Tokens)
Deployment: Railway
Frontend
Framework: React 18.2.0
UI Kütüphanesi: Material-UI (MUI) 5.14.1
Routing: React Router DOM 6.14.1
HTTP Client: Axios 1.4.0
Deployment: Vercel
🛠️ Staj Sürecinde Öğrenilen Teknolojiler
Backend: Django ORM, REST API tasarımı, JWT authentication
Frontend: React hooks, Context API, Material-UI component sistemi
DevOps: Git workflow, deployment süreçleri, environment management
Database: Model tasarımı, migration yönetimi, PostgreSQL
🚀 Kurulum
Gereksinimler
Python 3.8+
Node.js 16+
npm veya yarn
Git
1. Projeyi Klonlayın
git clone https://github.com/muhammedesatdemir/sosyal-yardim.git
cd sosyal-yardim
2. Backend Kurulumu
cd backend

# Virtual environment oluşturun
python -m venv venv

# Virtual environment'ı aktifleştirin
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Bağımlılıkları yükleyin
pip install -r requirements.txt

# Veritabanı migration'larını çalıştırın
python manage.py migrate

# Superuser oluşturun
python manage.py createsuperuser

# Sunucuyu başlatın
python manage.py runserver
3. Frontend Kurulumu
cd frontend

# Bağımlılıkları yükleyin
npm install

# Geliştirme sunucusunu başlatın
npm start
🌐 Kullanım
Giriş Yapma
http://localhost:3000 adresine gidin
Kayıt olun veya mevcut hesabınızla giriş yapın
Dashboard'a yönlendirileceksiniz
Başvuru Oluşturma
"Yeni Başvuru" menüsüne tıklayın
Gerekli bilgileri doldurun
Başvurunuzu gönderin
Başvuru Takibi
"Başvurularım" menüsüne gidin
Mevcut başvurularınızı görüntüleyin
Durum güncellemelerini takip edin
👥 Kullanıcı Rolleri
🎯 Admin
Tüm başvuruları yönetme
Kullanıcı hesaplarını yönetme
Sistem ayarlarını yapma
Yardım türlerini ekleme/düzenleme
👨‍💼 Sosyal Çalışan
Başvuruları inceleme
Durum güncellemeleri yapma
Personel atama
Raporlama
🆘 Yardım Alan
Yeni başvuru oluşturma
Kendi başvurularını takip etme
Profil bilgilerini güncelleme
🤝 Gönüllü
Başvuruları görüntüleme
Yardım süreçlerine destek olma
📁 Proje Yapısı
sosyal-yardim/
├── backend/                 # Django API
│   ├── users/              # Kullanıcı yönetimi
│   ├── yardim_basvurulari/ # Başvuru sistemi
│   └── sosyal_yardim_api/  # Ana proje
├── frontend/               # React uygulaması
│   ├── src/components/     # React bileşenleri
│   ├── src/contexts/       # Context API
│   └── src/services/       # API servisleri
├── docs/                   # Dokümantasyon
└── deployment/             # Deployment dosyaları
📋 Staj Sürecinde Geliştirilen Modüller
Kullanıcı Yönetimi: Rol tabanlı yetkilendirme sistemi
Başvuru Sistemi: Sosyal yardım başvuru takip modülü
Admin Paneli: Django admin entegrasyonu
API Güvenliği: CORS ve JWT koruma sistemleri
🔧 Konfigürasyon
Environment Variables
Backend (.env)
SECRET_KEY=your-secret-key
DEBUG=True
DATABASE_URL=your-database-url
JWT_SECRET_KEY=your-jwt-secret
ALLOWED_HOSTS=localhost,127.0.0.1
Frontend (.env)
REACT_APP_API_URL=http://localhost:8000/api
🧪 Test
Backend Testleri
cd backend
python manage.py test
Frontend Testleri
cd frontend
npm test
🚀 Deployment
Detaylı deployment rehberi için DEPLOYMENT_GUIDE.md dosyasını inceleyin.

Hızlı Deployment
Backend'i Railway'e deploy edin
Frontend'i Vercel'e deploy edin
Environment variables'ları ayarlayın
🎓 Staj Sürecinde Deployment Öğrenimleri
Cloud Platformları: Railway (backend), Vercel (frontend)
Environment Management: Production ve development ortamları
Database Deployment: PostgreSQL cloud deployment
CI/CD: Otomatik deployment süreçleri
🤝 Katkıda Bulunma
Fork yapın
Feature branch oluşturun (git checkout -b feature/amazing-feature)
Commit yapın (git commit -m 'Add amazing feature')
Push yapın (git push origin feature/amazing-feature)
Pull Request oluşturun
📝 Lisans
Bu proje MIT lisansı altında lisanslanmıştır.

📞 İletişim
Proje Linki: https://github.com/muhammedesatdemir/sosyal-yardim
Issues: GitHub Issues
E-posta: demirmuhammedesat@gmail.com
🎓 Staj Bilgileri
Kurum: Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı
Birim: Bilgi İşlem Dairesi
Proje Türü: Staj Projesi
Teknoloji Alanı: Full-Stack Web Geliştirme
Süre: Staj süresi boyunca geliştirilmiştir
🙏 Teşekkürler
🏛️ Kurum ve Mentorlar
Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı - Staj imkanı sağladığı için
Bilgi İşlem Dairesi - Teknik destek ve rehberlik için
Staj Mentorları - Proje geliştirme sürecinde yardımları için
🛠️ Teknoloji Kütüphaneleri
Django - Backend framework
React - Frontend library
Material-UI - UI component library
Railway - Backend hosting
Vercel - Frontend hosting
📝 Not
Bu proje, Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı Bilgi İşlem Dairesi'nde yapılan staj sürecinde eğitim amaçlı geliştirilmiştir. Proje, modern web teknolojileri öğrenimi ve pratik uygulama geliştirme deneyimi kazanmak amacıyla tasarlanmıştır.

⭐ Bu projeyi beğendiyseniz yıldız vermeyi unutmayın!

7) Kurum İçi Belge İmza Süreci Yönetim Sistemi
📋 Proje Hakkında
Bu proje, Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı Bilgi İşlem Dairesi'nde gerçekleştirilen staj sürecinde geliştirilmiştir. Sistem, kamu kurumlarında belgelerin kim tarafından ne zaman imzalanacağını belirleyen, imza sırasını yöneten ve belge sürecini dijitalleştiren bir uygulamadır.

🎯 Staj Süreci Detayları
Kurum: Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı
Birim: Bilgi İşlem Dairesi
Proje Türü: Staj Projesi
Geliştirme Süresi: Staj süresi boyunca
Hedef: Kamu kurumlarında dijital dönüşüm süreçlerine katkı sağlamak
🏛️ Proje Amacı
Bu sistem, geleneksel kağıt tabanlı belge imza süreçlerini dijital ortama taşıyarak:

İmza süreçlerini hızlandırır
Belge takibini kolaylaştırır
Şeffaflık ve hesap verebilirliği artırır
Kamu kaynaklarının verimli kullanımını sağlar
Çevre dostu çözümler sunar
🏗️ Teknolojiler
Backend
Node.js + Express.js
MySQL veritabanı
Sequelize ORM
JWT kimlik doğrulama
Nodemailer e-posta bildirimleri
Multer dosya yükleme
Frontend
React + Tailwind CSS
React PDF PDF önizleme
Axios API çağrıları
📁 Proje Yapısı
belge_imza_sirasi_yonetimi/
├── backend/                 # Node.js API
│   ├── config/             # Veritabanı ve uygulama yapılandırması
│   ├── controllers/        # İş mantığı kontrolcüleri
│   ├── middleware/         # Middleware fonksiyonları
│   ├── models/            # Sequelize veritabanı modelleri
│   ├── routes/            # API rotaları
│   ├── services/          # İş mantığı servisleri
│   ├── uploads/           # Yüklenen dosyalar
│   └── app.js            # Ana uygulama dosyası
├── frontend/              # React uygulaması
│   ├── public/           # Statik dosyalar
│   ├── src/             # Kaynak kodlar
│   │   ├── components/  # React bileşenleri
│   │   ├── pages/      # Sayfa bileşenleri
│   │   ├── services/   # API servisleri
│   │   ├── utils/      # Yardımcı fonksiyonlar
│   │   └── App.js     # Ana uygulama bileşeni
└── README.md           # Bu dosya
🚀 Kurulum
Backend Kurulumu
cd backend
npm install
npm run dev
Frontend Kurulumu
cd frontend
npm install
npm start
📊 Veritabanı Şeması
Tablolar
users: Kullanıcı bilgileri ve rolleri
documents: Belge bilgileri ve dosya yolları
sign_flows: İmza sırası ve durumları
logs: Sistem aktivite kayıtları
👥 Kullanıcı Rolleri
Hazırlayan: Belge oluşturur ve imza sırasını belirler
İnceleyen: Belgeleri gözden geçirir
İmzalayan: Belgeleri imzalar
Yönetici: Tüm işlemleri yönetir
🔔 Özellikler
✅ Belge yükleme ve yönetimi (PDF)
✅ Dinamik imza sırası yönetimi
✅ E-posta bildirimleri
✅ İmza geçmişi ve loglama
✅ Kullanıcı rol tabanlı yetkilendirme
✅ Modern ve responsive arayüz
✅ PDF ön izleme
✅ Güvenli dosya yükleme
🎓 Staj Sürecinde Öğrenilen Teknolojiler ve Deneyimler
💻 Teknik Beceriler
Full-Stack Web Geliştirme: Node.js, Express.js, React
Veritabanı Yönetimi: MySQL, Sequelize ORM
API Geliştirme: RESTful API tasarımı ve implementasyonu
Frontend Geliştirme: Modern React hooks, Tailwind CSS
Güvenlik: JWT authentication, middleware yapıları
Dosya İşlemleri: Multer ile güvenli dosya yükleme
🏛️ Kamu Sektörü Deneyimi
Kurum İçi Süreç Analizi: Mevcut iş akışlarının analizi
Dijital Dönüşüm: Geleneksel süreçlerin dijitalleştirilmesi
Kullanıcı İhtiyaç Analizi: Farklı birimlerin ihtiyaçlarının belirlenmesi
Proje Yönetimi: Staj sürecinde proje planlama ve takibi
🔧 Geliştirme Süreci
Agile Metodoloji: Hızlı prototipleme ve iteratif geliştirme
Test ve Doğrulama: Kullanıcı geri bildirimleri ile sürekli iyileştirme
Dokümantasyon: Teknik ve kullanıcı dokümantasyonu hazırlama
Deployment: Uygulama kurulumu ve yapılandırması
📞 İletişim
Bu proje hakkında detaylı bilgi için:

Geliştirici: [Stajyer Öğrenci]
Kurum: Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı
Birim: Bilgi İşlem Dairesi
Bu proje, kamu sektöründe dijital dönüşüm süreçlerine katkı sağlamak amacıyla geliştirilmiştir.

8) 📄 Evrak Takip Sistemi
Modern, kullanıcı dostu ve güvenli bir evrak takip ve yönetim sistemi. Bu proje, kurumsal evrak süreçlerini dijitalleştirmek ve yönetimini kolaylaştırmak için geliştirilmiştir.

Bu proje, Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı Bilgi İşlem Dairesi'nde yapılan staj sürecinde geliştirilmiştir.

🌟 Mevcut Özellikler
✅ Çalışan Özellikler
👤 Kimlik Doğrulama: E-posta ve şifre ile güvenli giriş sistemi
📊 Dashboard: Evrak istatistikleri, grafikleri ve son evrakları görüntüleme
📄 Evrak Yönetimi:
Yeni evrak oluşturma (Evrak Kayıt)
Evrak listesini görüntüleme (Evrak Listesi)
Evrak detaylarını görüntüleme/düzenleme
Evrak durumu değiştirme (onay/red/iade)
👥 Kullanıcı Yönetimi: Admin kullanıcıları için yeni kullanıcı ekleme (sadece Admin)
🔧 Profil Yönetimi: Kullanıcı profil bilgilerini görüntüleme ve düzenleme
🛡️ Güvenlik Özellikleri
Güvenli Kimlik Doğrulama: E-posta tabanlı giriş sistemi
Şifre Güvenliği: BCrypt ile hash'lenmiş şifreler
Hesap Durumu Kontrolü: Aktif/pasif hesap yönetimi
CORS Koruması: Frontend-backend güvenli iletişimi
💫 Kullanıcı Deneyimi
Responsive Tasarım: Mobil ve masaüstü uyumlu
Material-UI: Modern ve tutarlı arayüz
Türkçe Dil Desteği: Tam Türkçe arayüz ve mesajlar
Anlık Bildirimler: Kullanıcı dostu hata ve başarı mesajları
🏗️ Proje Yapısı
evrak_takip_sistemi/
├── frontend/                 # React.js Frontend Uygulaması
│   ├── src/
│   │   ├── components/       # React bileşenleri
│   │   │   ├── auth/        # Kimlik doğrulama bileşenleri (LoginForm)
│   │   │   ├── dashboard/   # Dashboard bileşenleri (Dashboard, StatsCard, RecentDocuments)
│   │   │   ├── documents/   # Evrak yönetimi bileşenleri (CRUD işlemleri, tablo, form)
│   │   │   ├── layout/      # Sayfa düzeni bileşenleri (Header, Sidebar, MainLayout)
│   │   │   ├── profile/     # Profil yönetimi (Profile)
│   │   │   ├── users/       # Kullanıcı yönetimi (UserManagement - sadece Admin)
│   │   │   └── reports/     # Raporlama bileşenleri (henüz entegre edilmemiş)
│   │   ├── data/            # Mock veri dosyaları
│   │   ├── services/        # API servis katmanı
│   │   ├── types/           # TypeScript tip tanımları
│   │   ├── theme/           # Material-UI tema ayarları
│   │   └── utils/           # Yardımcı fonksiyonlar
│   ├── package.json
│   └── vite.config.ts
├── backend/                  # Node.js Backend API
│   ├── src/
│   │   ├── config/          # Yapılandırma dosyaları
│   │   ├── models/          # Sequelize veri modelleri
│   │   │   ├── User.js      # Kullanıcı modeli
│   │   │   └── Document.js  # Evrak modeli
│   │   ├── routes/          # API rotaları
│   │   │   ├── auth.js      # Kimlik doğrulama API'si
│   │   │   ├── users.js     # Kullanıcı yönetimi API'si
│   │   │   ├── documents.js # Evrak yönetimi API'si
│   │   │   └── reports.js   # Raporlama API'si
│   │   ├── middleware/      # Express middleware'leri
│   │   └── utils/           # Yardımcı fonksiyonlar
│   ├── database/            # Veritabanı scripts
│   │   ├── setup.sql        # Veritabanı kurulum
│   │   ├── seeders.js       # Örnek veri oluşturma
│   │   └── seed-runner.js   # Seed çalıştırıcı
│   ├── uploads/             # Dosya yüklemeleri
│   ├── package.json
│   └── env.example
└── README.md
🚀 Kurulum ve Çalıştırma
📋 Önkoşullar
Node.js v16 veya üzeri
npm veya yarn
MySQL 5.7+ veya 8.0+
1️⃣ Depoyu Klonlayın
git clone <repository-url>
cd evrak_takip_sistemi
2️⃣ Backend Kurulumu
Bağımlılıkları Yükleyin
cd backend
npm install
MySQL Veritabanını Hazırlayın
# MySQL'e bağlanın
mysql -u root -p

# Veritabanını oluşturun
CREATE DATABASE evrak_takip CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# Veya setup script'ini kullanın
mysql -u root -p < database/setup.sql
Çevre Değişkenlerini Ayarlayın
cp env.example .env
.env dosyasını düzenleyin:

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=evrak_takip
DB_USER=root
DB_PASSWORD=your_mysql_password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRE=7d

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=uploads/

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
Veritabanı Tablolarını Oluşturun ve Örnek Verileri Yükleyin
npm run db:seed
Backend Sunucusunu Başlatın
npm run dev
Backend http://localhost:3000 adresinde çalışacaktır.

3️⃣ Frontend Kurulumu
Bağımlılıkları Yükleyin
cd frontend
npm install
Frontend Sunucusunu Başlatın
npm run dev
Frontend http://localhost:5173 adresinde çalışacaktır.

👥 Test Kullanıcıları
Sistem kurulumu sonrası aşağıdaki test kullanıcılarını kullanabilirsiniz:

Rol	E-posta	Şifre	Açıklama
Admin	admin@evraktakip.com	admin123	Sistem yöneticisi - Tam erişim
Manager	manager@evraktakip.com	manager123	Departman müdürü - Yönetim erişimi
User	user@evraktakip.com	user123	Normal kullanıcı - Sınırlı erişim
🧭 Sistem Sayfaları
Giriş yaptıktan sonra aşağıdaki sayfalar kullanılabilir:

📱 Tüm Kullanıcılar İçin
🏠 Ana Sayfa (Dashboard): İstatistikler ve son evraklar
➕ Evrak Kayıt: Yeni evrak oluşturma
📋 Evrak Listesi: Tüm evrakları görüntüleme, düzenleme ve durumu değiştirme
👤 Profil: Kişisel bilgileri görüntüleme ve düzenleme
👑 Sadece Admin İçin
👥 Kullanıcı Yönetimi: Yeni kullanıcı ekleme ve mevcut kullanıcıları düzenleme
🛠️ Teknolojiler
Frontend
React 19 - Modern UI kütüphanesi
TypeScript - Tip güvenliği ve geliştirici deneyimi
Material-UI (MUI) - Google Material Design bileşenleri
Vite - Hızlı build tool
React Router - Sayfa yönlendirme
React Hooks - Modern state yönetimi
Backend
Node.js - JavaScript runtime environment
Express.js - Web application framework
MySQL - İlişkisel veritabanı
Sequelize - Object-Relational Mapping (ORM)
bcryptjs - Şifre hash'leme
JWT - JSON Web Token kimlik doğrulama
Helmet - Güvenlik middleware
CORS - Cross-Origin Resource Sharing
Morgan - HTTP request logger
📚 API Dokümantasyonu
🔐 Kimlik Doğrulama Endpoints
POST   /api/auth/login     # Kullanıcı girişi
POST   /api/auth/register  # Kullanıcı kaydı (gelecek)
POST   /api/auth/refresh   # Token yenileme (gelecek)
📄 Evrak Yönetimi Endpoints
GET    /api/documents              # Evrak listesi
GET    /api/documents/:id          # Belirli evrak detayı
POST   /api/documents              # Yeni evrak oluşturma
PUT    /api/documents/:id          # Evrak güncelleme
DELETE /api/documents/:id          # Evrak silme
PATCH  /api/documents/:id/status   # Evrak durumu güncelleme
GET    /api/documents/stats/dashboard # Dashboard istatistikleri
👥 Kullanıcı Yönetimi Endpoints (Admin)
GET    /api/users          # Kullanıcı listesi
GET    /api/users/:id      # Belirli kullanıcı detayı
POST   /api/users          # Yeni kullanıcı oluşturma
PUT    /api/users/:id      # Kullanıcı güncelleme
📊 Raporlama Endpoints (Mock Data)
GET    /api/reports/stats     # Genel istatistikler (TODO - mock veri döner)
GET    /api/reports/documents # Evrak raporları (TODO - mock veri döner)
🏥 Sistem Endpoints
GET    /health             # Sistem sağlık kontrolü
📊 Veri Modelleri
User (Kullanıcı)
{
  id: Integer,
  name: String,
  email: String (unique),
  password: String (hashed),
  role: Enum('admin', 'manager', 'user'),
  department: String,
  position: String,
  phone: String,
  status: Enum('active', 'inactive', 'suspended'),
  lastLoginAt: Date
}
Document (Evrak)
{
  id: Integer,
  documentNumber: String (unique),
  title: String,
  description: Text,
  type: Enum('incoming', 'outgoing', 'internal'),
  category: String,
  priority: Enum('low', 'normal', 'high', 'urgent'),
  status: Enum('draft', 'pending', 'in_progress', 'completed', 'rejected', 'archived'),
  sender: String,
  recipient: String,
  documentDate: Date,
  receivedDate: Date,
  dueDate: Date,
  tags: Array,
  createdById: Integer,
  assignedToId: Integer
}
🎨 Kullanıcı Rolleri ve İzinler
👑 Admin (admin)
Kullanıcı yönetimi (oluşturma, düzenleme)
Tüm evraklara erişim
Evrak onaylama/reddetme/iade etme
Dashboard görüntüleme
🏢 Manager (manager)
Evrak oluşturma ve düzenleme
Evrak onaylama/reddetme/iade etme
Dashboard görüntüleme
Profil yönetimi
👤 User (user)
Evrak oluşturma
Kendi evraklarını düzenleme
Evrak görüntüleme
Profil yönetimi
Not: Mevcut sistem bu 3 ana rol ile çalışmaktadır. Viewer ve Approver rolleri henüz aktif değildir.

🧪 Test ve Geliştirme
Backend Test
cd backend
npm test
Frontend Build
cd frontend
npm run build
Production Build
# Backend
cd backend
npm start

# Frontend
cd frontend
npm run preview
🔧 Geliştirme Notları
✅ Tamamlanan Özellikler
Kullanıcı Yönetimi: Giriş, kayıt, profil düzenleme
Evrak CRUD: Oluşturma, düzenleme, görüntüleme, silme
Dashboard: Temel istatistikler ve son evraklar
Rol Sistemi: Admin, Manager, User rolleri
Evrak Durumu: Onaylama, reddetme, iade etme
🔒 Güvenlik Güncellemeleri
✅ İkili şifre hash'leme sorunu çözüldü
✅ E-posta normalizasyonu (trim, toLowerCase) eklendi
✅ Aktif hesap kontrolü eklendi
✅ CORS güvenliği yapılandırıldı
🛠️ Teknik Stack
✅ MySQL veritabanı desteği
✅ Sequelize ORM entegrasyonu
✅ Bcrypt şifre güvenliği
✅ JWT kimlik doğrulama
✅ Responsive Material-UI tasarımı
✅ TypeScript tip güvenliği
⚠️ Bilinen Sınırlamalar
Raporlama sistemi henüz mock veri kullanıyor
Dosya ekleme özelliği mevcut değil
Kullanıcı silme işlemi frontend'de tanımlı ama backend'de endpoint yok
Gelişmiş arama ve filtreleme özellikleri sınırlı
📈 Gelecek Özellikler
🚧 Planlanan Özellikler
 Raporlama Sistemi: Şu anda mock veri dönen raporlama endpoint'lerinin gerçek verilerle entegrasyonu
 Dosya Ekleri: Evraklara dosya ekleme ve indirme özelliği
 Gelişmiş Filtreleme: Evrak listesinde detaylı arama ve filtreleme
 Kullanıcı Silme: Admin panelinde kullanıcı silme özelliği
 E-posta Bildirimleri: Evrak durumu değişikliklerinde otomatik bildirimler
🔧 Teknik İyileştirmeler
 Unit ve entegrasyon testleri
 REST API dokümantasyonu (Swagger)
 Docker containerization
 Redis cache desteği
 Audit log (işlem geçmişi)
 Error boundaries ve hata yönetimi
🤝 Katkıda Bulunma
Bu repository'yi fork edin
Feature branch oluşturun (git checkout -b feature/amazing-feature)
Değişikliklerinizi commit edin (git commit -m 'Add some amazing feature')
Branch'inizi push edin (git push origin feature/amazing-feature)
Pull Request oluşturun
Geliştirme Kuralları
TypeScript kullanın
Material-UI design system'ını takip edin
Türkçe kullanıcı arayüzü dili kullanın
Responsive tasarım prensiplerini uygulayın
API response formatlarını tutarlı tutun
📄 Lisans
Bu proje staj projesi olarak geliştirilmiştir ve eğitim amaçlıdır.

📞 Destek ve İletişim
🐛 Bug Raporu: GitHub Issues kullanın
💡 Özellik İsteği: GitHub Issues kullanın
📧 Direk İletişim: Proje maintainer'ları ile iletişime geçin
demirmuhammedesat@gmail.com
🙏 Teşekkürler
Bu projeyi geliştirirken kullandığımız açık kaynak kütüphanelere ve topluluk katkılarına teşekkür ederiz:

React.js topluluğu
Material-UI ekibi
Node.js ve Express.js geliştiricileri
MySQL ve Sequelize maintainer'ları
Evrak Takip Sistemi - Modern kurumsal evrak yönetimi için geliştirildi ❤️

Bu proje, Türkiye Cumhuriyeti Cumhurbaşkanlığı İletişim Başkanlığı Bilgi İşlem Dairesi'nde yapılan staj sürecinde geliştirilmiştir.

9) 📝 Belgem - Cumhurbaşkanlığı İletişim Başkanlığı
Belgem Logo
Modern Web Tabanlı Metin Düzenleyici

HTML5 CSS3 JavaScript License

🌟 Genel Bakış
Belgem, Cumhurbaşkanlığı İletişim Başkanlığı'nın 2025 Yaz Stajyerleri tarafından geliştirilmiş modern bir web tabanlı metin düzenleyicidir. Kurumsal ihtiyaçları karşılayan, güvenli ve kullanıcı dostu bir metin düzenleme aracı sunmaktır.

👥 Geliştirici Ekibi
Muhammed Esat Demir
Mert Ali Dalkır
Ecem Efe
Gizem Küçük
Hanife Köse
Şevval Soylu
✨ Temel Özellikler
📝 Metin Düzenleme
Zengin Metin Editörü: Modern WYSIWYG editör
Çoklu Sayfa Desteği: Otomatik sayfalama sistemi
Undo/Redo: Gelişmiş geri al/ileri al sistemi
Otomatik Kaydetme: Çalışmanızı kaybetmeyin
🎨 Biçimlendirme
Font Seçenekleri: Times New Roman, Arial, Calibri ve daha fazlası
Metin Stilleri: Kalın, italik, altı çizili, üstü çizili
Renk Paleti: Metin ve vurgu renkleri
Hizalama: Sol, orta, sağ, iki yana yaslama
Listeler: Madde işaretli ve numaralı listeler
📄 Sayfa Yönetimi
Dinamik Sayfalar: Otomatik sayfa ekleme/çıkarma
Sayfa Düzeni: Dikey/yatay yönlendirme
Kenar Boşlukları: Ayarlanabilir kenar boşlukları
Üstbilgi/Altbilgi: Özelleştirilebilir başlık ve altbilgi
🖼️ Medya ve İçerik
Resim Ekleme: Drag & drop resim yükleme
Tablo Oluşturma: Dinamik tablo editörü
Şekiller: Geometrik şekil çizim araçları
Bağlantılar: Hyperlink ekleme ve yönetimi
📤 Dışa Aktarma
PDF Export: Yüksek kaliteli PDF oluşturma
DOCX Export: Microsoft Word uyumlu belgeler
HTML Export: Web uyumlu format
Yazdırma: Doğrudan tarayıcı yazdırma
📥 İçe Aktarma
PDF Import: PDF dosyalarını metin olarak içe aktarma
DOCX Import: Word belgelerini açma
Metin Import: TXT dosyası desteği
🎭 Şablonlar
Hazır Şablonlar: Dilekçe, tutanak, rapor şablonları
Özel Şablonlar: Kendi şablonlarınızı kaydedin
Dinamik Alanlar: Otomatik doldurulabilen form alanları
🌙 Kullanıcı Deneyimi
Karanlık Mod: Göz dostu karanlık tema
Responsive Tasarım: Mobil ve tablet uyumlu
Zoom Kontrolü: %50-200 arası yakınlaştırma
Tam Ekran Modu: Dikkat dağıtmayan yazma ortamı
🚀 Kurulum ve Çalıştırma
Sistem Gereksinimleri
Modern Web Tarayıcısı: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
RAM: 4GB minimum, 8GB önerilen
Depolama: 50MB boş alan
İnternet: CDN kaynakları için (ilk yükleme)
Hızlı Başlangıç
Projeyi İndirin

git clone https://github.com/your-username/belgem.git
cd belgem
Dosya Yapısını Kontrol Edin

belgem/
├── cib_word/
│   ├── index.html
│   ├── script.js
│   └── styles.css
├── image/
│   └── logo1.png
├── README.md
└── requirements.txt
Uygulamayı Çalıştırın

Seçenek 1: Doğrudan Tarayıcıda

belgem/cib_word/index.html dosyasını çift tıklayın
Seçenek 2: Yerel Web Sunucusu (Önerilen)

# Python ile
cd belgem
python -m http.server 8000
# http://localhost:8000/cib_word/ adresine gidin

# Node.js ile
npx http-server belgem -p 8000
# http://localhost:8000/cib_word/ adresine gidin

# VS Code Live Server uzantısı ile
# index.html'e sağ tıklayın → "Open with Live Server"
📖 Kullanım Kılavuzu
Temel Kullanım
Yeni Belge Oluşturma

Uygulama açıldığında otomatik olarak boş bir belge oluşturulur
"Buraya yazmaya başlayın..." alanına tıklayarak yazmaya başlayın
Metin Biçimlendirme

Metni seçin ve üst şeritteki araçları kullanın
Klavye kısayolları: Ctrl+B (kalın), Ctrl+I (italik), Ctrl+U (altı çizili)
Sayfa Yönetimi

Metin uzadıkça otomatik olarak yeni sayfalar eklenir
"+" butonuyla manuel sayfa ekleyebilirsiniz
Dosya İşlemleri

Kaydetme: Giriş sekmesinden "PDF İndir" veya "Word İndir"
Açma: "İçe Aktar" butonuyla PDF/DOCX dosyalarını açabilirsiniz
Gelişmiş Özellikler
Şablon Kullanımı

"Şablonlar" sekmesine tıklayın
İstediğiniz şablonu seçin ve "Uygula" butonuna basın
Dinamik alanları doldurun
Tablo Ekleme

"Ekle" sekmesinden "Tablo" butonuna tıklayın
Satır/sütun sayısını belirleyin
Tablo stilini seçin
Resim Ekleme

"Ekle" sekmesinden "Resim" butonuna tıklayın
Dosya seçin veya sürükle-bırak yapın
Boyutları ayarlayın
🛠️ Teknik Detaylar
Kullanılan Teknolojiler
Kategori	Teknoloji	Versiyon	Açıklama
Frontend	HTML5/CSS3/JavaScript	ES6+	Modern web teknolojileri
UI Framework	Vanilla JS	-	Bağımlılık-free yaklaşım
Icons	Font Awesome	6.0.0	Vektör ikon kütüphanesi
PDF Generation	jsPDF	2.5.1	Client-side PDF oluşturma
PDF Generation	PDFMake	0.2.7	Gelişmiş PDF özellikleri
DOCX Generation	docx	7.8.2	Microsoft Word belgeleri
File Handling	FileSaver.js	2.0.5	Dosya indirme işlemleri
Alerts/Modals	SweetAlert2	11.x	Modern kullanıcı arayüzü
HTML to PDF	html2pdf.js	0.10.1	HTML'den PDF dönüştürme
Screenshot	html2canvas	1.4.1	DOM screenshot alma
PDF Reading	PDF.js	2.16.105	PDF dosyası okuma
DOCX Reading	Mammoth.js	1.4.2	Word belgesi okuma
Mimari Yapı
├── Presentation Layer (UI)
│   ├── HTML Templates
│   ├── CSS Styling
│   └── Interactive Components
│
├── Application Layer
│   ├── Document Management
│   ├── File Operations
│   ├── Template System
│   └── Export/Import Logic
│
├── Data Layer
│   ├── Local Storage
│   ├── Session Management
│   └── Template Data
│
└── External Services
    ├── CDN Libraries
    ├── Font Resources
    └── Icon Libraries
Performans Özellikleri
Lazy Loading: Büyük kütüphaneler ihtiyaç halinde yüklenir
Debounced Operations: Otomatik kaydetme ve sayfa kontrolü optimize edildi
Memory Management: Undo/Redo işlemleri için sınırlı history
Efficient DOM: Virtual scrolling ve sayfa bazlı rendering
🔧 Geliştirici Kılavuzu
Proje Yapısı
belgem/
├── cib_word/
│   ├── index.html          # Ana HTML dosyası
│   ├── script.js           # JavaScript mantığı (17,000+ satır)
│   └── styles.css          # CSS stilleri (7,000+ satır)
├── image/
│   └── logo1.png          # Kurum logosu
├── README.md              # Bu dosya
└── requirements.txt       # Bağımlılık listesi
Önemli Sınıflar ve Fonksiyonlar
Ana Sınıf: CIBWordProcessor
class CIBWordProcessor {
    constructor()           // Başlatma
    init()                 // Olay dinleyicilerini kurma
    setupEventListeners()  // UI etkileşimleri
    observeEditors()       // Editör gözlemleme
    saveToHistory()        // Undo/Redo sistemi
    updateWordCount()      // Kelime/karakter sayımı
    exportToPDF()          // PDF dışa aktarma
    exportToDocx()         // DOCX dışa aktarma
}
Temel Fonksiyonlar
formatText(command) - Metin biçimlendirme
insertTable() - Tablo ekleme
insertImage() - Resim ekleme
addPage() - Sayfa ekleme
applyTemplate() - Şablon uygulama
Özelleştirme
Yeni Şablon Ekleme
// script.js içinde templates dizisine ekleyin
const newTemplate = {
    kategori: "Resmi",
    baslik: "Yeni Şablon",
    metin: "Şablon içeriği..."
};
Tema Özelleştirme
/* styles.css içinde CSS değişkenlerini düzenleyin */
:root {
    --primary-color: #your-color;
    --secondary-color: #your-color;
}
🐛 Bilinen Sorunlar ve Çözümler
Yaygın Sorunlar
CORS Hatası

Sorun: file:// protokolü ile açıldığında bazı özellikler çalışmaz
Çözüm: Yerel web sunucusu kullanın
Font Yükleme Sorunu

Sorun: İnternet bağlantısı olmadığında fontlar yüklenmez
Çözüm: Fontları yerel olarak indirin ve CSS'i güncelleyin
PDF Oluşturma Hatası

Sorun: Çok büyük belgeler PDF'e dönüştürülemiyor
Çözüm: Belgeyi bölümlere ayırın veya görsel kalitesini düşürün
Tarayıcı Uyumluluğu
Özellik	Chrome	Firefox	Safari	Edge
Temel Editör	✅	✅	✅	✅
PDF Export	✅	✅	⚠️	✅
DOCX Export	✅	✅	✅	✅
Drag & Drop	✅	✅	✅	✅
Clipboard API	✅	✅	⚠️	✅
Açıklama: ⚠️ = Kısıtlı destek, manuel test gerekebilir

📱 Mobil Uyumluluk
Belgem responsive tasarım prensiplerine uygun olarak geliştirilmiştir:

Tablet: Tam özellik desteği
Telefon: Temel düzenleme özellikleri
Touch: Dokunmatik ekran optimize edildi
🔒 Güvenlik
Client-Side: Tüm işlemler tarayıcıda gerçekleşir
Veri Gizliliği: Belgeler sunucuya gönderilmez
Local Storage: Veriler yerel olarak saklanır
HTTPS: Üretim ortamında HTTPS kullanın
📊 Performans Metrikleri
İlk Yükleme: ~2-3 saniye (CDN bağımlı)
Sayfa Ekleme: <100ms
PDF Oluşturma: 1-5 saniye (belge boyutuna bağlı)
Otomatik Kaydetme: Her 30 saniye
Memory Usage: ~50-100MB (belge boyutuna bağlı)
Geliştirme Kuralları
Kod Standardı: ESLint ve Prettier kullanın
Commit Mesajları: Conventional Commits formatını takip edin
Testler: Yeni özellikler için test yazın
Dokümantasyon: README'yi güncel tutun
📄 Lisans
Bu proje MIT Lisansı altında lisanslanmıştır. Detaylar için LICENSE dosyasına bakın.

📞 İletişim
Cumhurbaşkanlığı İletişim Başkanlığı

Web: iletisim.gov.tr
E-posta: info@iletisim.gov.tr
Geliştirici Ekibi

E-posta: demirmuhammedesat@gmail.com
Belgem © 2025 - Cumhurbaşkanlığı İletişim Başkanlığı
Modern Web Tabanlı Metin Düzenleyici

10) Basit Anket Sonuç Analizcisi
Cumhurbaşkanlığı İletişim Başkanlığı tasarım kılavuzuna uygun olarak geliştirilmiş, Excel ve CSV dosyalarından anket verilerini analiz eden Python uygulaması.

Özellikler
Excel (.xlsx) ve CSV (.csv) dosya desteği
Gelişmiş istatistiksel analizler:
Sayısal veriler: Ortalama, medyan, mod, standart sapma, varyans, çarpıklık, basıklık, Q1, Q3, IQR
Kategorik veriler: Frekans, yüzde, entropi, Gini katsayısı
Dinamik grafik sistemi: Bar Chart ve Pie Chart arasında gerçek zamanlı geçiş
Akıllı veri işleme: Otomatik veri tipi algılama, NaN değer işleme seçenekleri
Çoklu sekme arayüzü: İstatistikler, Grafikler ve Dışa Aktar sekmeleri
Excel ve PNG dışa aktarma: Detaylı raporlar ve grafik görselleri
Cumhurbaşkanlığı İletişim Başkanlığı tasarım standartları
Türkçe arayüz
Kurulum
Python 3.8 veya üzeri sürümünün yüklü olduğundan emin olun
Gerekli kütüphaneleri yükleyin:
pip install -r requirements.txt
Kullanım
Hızlı Başlangıç
Uygulamayı başlatın:

python survey_analyzer.py
Örnek veri oluşturun (isteğe bağlı):

python create_sample_data.py
Dosya seçin ve analiz edin

Temel İşlemler
"Gözat" butonu ile Excel/CSV dosyanızı seçin
Analiz ayarlarını yapılandırın (NaN işleme, grafik türü)
"Analiz Et" butonuna tıklayın
Sonuçları farklı sekmelerde inceleyin
Dışa aktarın (Excel raporu ve PNG grafikleri)
Gereksinimler
Python 3.8+
pandas - Veri analizi
matplotlib - Grafik oluşturma
openpyxl - Excel dosya desteği
Pillow - Görsel işleme
numpy - Sayısal hesaplamalar
tkinter - GUI (Python ile birlikte gelir)
Test
Uygulamayı test etmek için:

python test_survey_analyzer.py
Proje Yapısı
anket_analizcisi/
├── survey_analyzer.py          # Ana uygulama
├── create_sample_data.py       # Örnek veri oluşturucu
├── test_survey_analyzer.py     # Test dosyası
├── requirements.txt            # Bağımlılıklar
├── README.md                   # Bu dosya
├── KULLANIM_KILAVUZU.md       # Detaylı kullanım kılavuzu
├── setup.py                    # Kurulum dosyası
└── ornek-cikti/               # Örnek çıktılar
    ├── anket_analiz_sonuclari.xlsx
    └── *.png (grafik dosyaları)
Özellik Detayları
İstatistiksel Analizler
Sayısal veriler için 15+ farklı istatistik
Kategorik veriler için frekans analizi ve çeşitlilik ölçüleri
Otomatik veri tipi algılama ve uygun analiz seçimi
Grafik Sistemi
Gerçek zamanlı güncelleme: Sütun ve grafik türü değişiklikleri anında yansır
Dinamik sekme yönetimi: Grafikler sadece "Grafikler" sekmesinde gösterilir
Yüksek kaliteli çıktı: PNG formatında kaydetme
Veri İşleme
Akıllı NaN işleme: Satır silme veya ortalama ile doldurma seçenekleri
Veri tipi koruması: Analiz sırasında orijinal veri yapısı korunur
Büyük dosya desteği: Optimize edilmiş bellek kullanımı
Lisans
Bu proje Cumhurbaşkanlığı İletişim Başkanlığı tarafından geliştirilmiştir.

Sürüm Geçmişi
v1.1.0 (2024)
Gerçek zamanlı grafik güncelleme sistemi
Gelişmiş istatistiksel hesaplamalar (entropi, Gini katsayısı)
Akıllı veri tipi algılama ve işleme
Çoklu sekme arayüzü
Excel ve PNG dışa aktarma
Kapsamlı test sistemi
Performans optimizasyonları
v1.0.0 (2024)
İlk sürüm
Temel istatistiksel analizler
Bar ve Pie chart grafikleri
İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

11) 🏷️ Barkod Oluşturucu
Python ile geliştirilmiş, komut satırından (CLI) çalışan barkod oluşturucu uygulaması.

✨ Özellikler
Kullanıcıdan barkod türü ve veri girişi
Desteklenen barkod türleri: EAN13, Code128, ISBN13
PNG formatında barkod görseli oluşturma
Otomatik dosya isimlendirme (tarih/saat ile)
Geçersiz veri girişinde uyarı
Başarı ve hata mesajları
Fonksiyonel ve sade Python kodu
📋 Desteklenen Barkod Türleri
Tür	Açıklama	Format
EAN13	European Article Number	12-13 haneli sayı
Code128	Code 128	Alfanümerik veri
ISBN13	International Standard Book Number	13 haneli, 978/979 ile başlayan
🚀 Kurulum
Gerekli kütüphaneleri yükleyin:
pip install -r requirements.txt
📖 Kullanım
python barkod-olusturucu.py
Adımlar:

Barkod türünü seçin (1-3)
İstenen veriyi girin
Barkod görseli otomatik olarak aynı dizine kaydedilir
Dosya adı: barcode_YYYYMMDD_HHMMSS.png formatında olur
📝 Örnekler
EAN13 Barkod
Veri: 123456789012
Dosya: barcode_20240101_123456.png
Code128 Barkod
Veri: HELLO123
Dosya: barcode_20240101_123456.png
ISBN13 Barkod
Veri: 9781234567890
Dosya: barcode_20240101_123456.png
🐛 Sorun Giderme
Import hatası:
pip install -r requirements.txt komutunu tekrar çalıştırın.
Dosya bulunamadı:
Doğru dizinde olduğunuzdan emin olun.
Barkod oluşturma hatası:
Veri formatını kontrol edin (ör. EAN13 için 12-13 haneli sayı).
📂 Dosya Yapısı
barkod-olusturucu/
├── barkod-olusturucu.py   # CLI uygulaması
├── requirements.txt       # Bağımlılıklar
└── README.md              # Bu dosya
Onaylarsanız bu güncellenmiş README dosyasını projenize ekleyebilirim!

📄 Lisans
Bu proje MIT lisansı ile lisanslanmıştır.

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

12) Salt Okunur Belge Kilitleyici
Cumhurbaşkanlığı İletişim Başkanlığı tasarım kılavuzuna uygun olarak geliştirilmiş, Word (.docx) ve Excel (.xlsx) belgelerini parola korumalı salt okunur sürümlere dönüştüren Python uygulaması.

🎯 Özellikler
Word Belgesi Dönüştürme: .docx dosyalarını yüksek kaliteli parola korumalı PDF'e dönüştürme
Excel Belgesi Koruması: .xlsx dosyalarını kapsamlı parola koruması ile salt okunur hale getirme
Grafiksel Kullanıcı Arayüzü: Tkinter tabanlı modern ve kullanıcı dostu arayüz
Kurumsal Tasarım: Cumhurbaşkanlığı İletişim Başkanlığı renk paleti ve tasarım standartları
Lokal Çalışma: İnternet bağımlılığı olmayan tamamen yerel uygulama
Güvenli Parola Sistemi: En az 6 karakterli güçlü parola koruması
🚀 Kurulum
Otomatik Kurulum (Önerilen)
python setup.py
Manuel Kurulum
Python 3.8+ sürümünün kurulu olduğundan emin olun

Harici Yazılım Gereksinimleri:

Windows: Microsoft Word yüklü olmalıdır
Linux/macOS: LibreOffice veya OpenOffice yüklü olmalıdır
Python kütüphanelerini yükleyin:

pip install -r requirements.txt
Kurulumu test edin:

python test_app.py
📝 Kullanım
Uygulamayı Başlatma
python main.py
Kullanım Adımları
Dosya Seçimi: "Gözat" butonuna tıklayarak .docx veya .xlsx dosyası seçin
Parola Belirleme: En az 6 karakterli güçlü parola girin
Dönüştürme: "Salt Okunur'a Dönüştür" butonuna tıklayın
Sonuç: Korumalı dosya orijinal dosyanın bulunduğu dizine kaydedilir
🔒 Güvenlik
Yerel İşlem: Tüm işlemler yerel olarak gerçekleştirilir
Veri Güvenliği: Kullanıcı verileri hiçbir şekilde dışa aktarılmaz
Parola Güvenliği: En az 6 karakterli güçlü parolalar güvenli şekilde işlenir
Word Koruması: Word belgeleri yüksek kaliteli parola korumalı PDF'e dönüştürülür
Excel Koruması: Excel dosyaları kapsamlı parola koruması ile korunur
🛠️ Sorun Giderme
Word Dönüştürme Sorunları
Hata: "Microsoft Word veya LibreOffice bulunamadı"
Çözüm: Microsoft Word (Windows) veya LibreOffice (Linux/macOS) yükleyin
Excel Koruması Sorunları
Hata: "Excel dosyası korunamadı"
Çözüm: Dosyanın başka bir uygulamada açık olmadığından emin olun
Parola Sorunları
Hata: "Parola en az 6 karakter olmalıdır"
Çözüm: Daha uzun ve güçlü bir parola girin
Genel Sorunlar
Hata: "Dosya erişim izni reddedildi"
Çözüm: Dosya yolunda özel karakter olmadığından emin olun
📋 Proje Yapısı
belge-kilitleyici/
├── main.py                 # Ana GUI uygulaması
├── utils.py                # Dönüştürme fonksiyonları
├── config.py               # Tasarım sabitleri
├── setup.py                # Otomatik kurulum scripti
├── test_app.py             # Test scripti
├── requirements.txt        # Python kütüphaneleri
├── README.md               # Bu dosya
├── PROJE_YAPISI.md        # Detaylı teknik dokümantasyon
├── image/                  # Görseller
│   ├── logoo1.png         # Cumhurbaşkanlığı logosu
│   └── flag_of_turkiye.png # Türk bayrağı
└── test_files/            # Test dosyaları
🏛️ Geliştirici
Bu uygulama Cumhurbaşkanlığı İletişim Başkanlığı tasarım kılavuzuna uygun olarak geliştirilmiştir.

Detaylı teknik bilgiler için: PROJE_YAPISI.md

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

13) Kişisel Veri Maskeleme Aracı
Bu proje, metin, PDF ve DOCX dosyalarındaki kişisel verileri (TCKN, IBAN, telefon numarası, e-posta) tespit edip maskelemek için geliştirilmiş bir masaüstü uygulamasıdır.

Özellikler
Geniş Dosya Desteği: TXT, PDF ve DOCX formatındaki dosyaları işleyebilir.
Kapsamlı Veri Tespiti: TC Kimlik Numarası, IBAN, telefon numarası ve e-posta adreslerini tanır.
Esnek Maskeleme: Metin tabanlı dosyalar için karakter değiştirme, PDF'ler için ise karartma (redaction) yöntemini kullanır.
Kullanıcı Dostu Arayüz: Basit ve anlaşılır bir arayüze sahiptir.
Detaylı Raporlama: Maskelenen veriler hakkında ayrıntılı bir rapor sunar.
Kurulum
Proje Dosyalarını İndirin: Bu repoyu klonlayın veya ZIP olarak indirin.

Gerekli Kütüphaneleri Yükleyin: Terminal veya komut istemcisini proje dizininde açın ve aşağıdaki komutu çalıştırın:

pip install -r requirements.txt
Kullanım
Uygulamayı başlatmak için terminalde aşağıdaki komutu çalıştırın:

python data_masker.py
Dosya Seçin: "Gözat" butonuna tıklayarak maskelemek istediğiniz dosyayı seçin.
Veri Türlerini Seçin: Maskelemek istediğiniz veri türlerini (TCKN, IBAN vb.) işaretleyin.
Çıktı Klasörü Seçin: "Çıktı Klasörü Seç" butonu ile maskelenmiş dosyanın kaydedileceği yeri belirtin.
Maskeleme İşlemini Başlatın: "MASKELE" butonuna tıklayarak işlemi başlatın.
İşlem tamamlandığında, sonuçla ilgili bir bilgilendirme mesajı ve durum panelinde detaylı bir rapor gösterilecektir.

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

14) Metin Temizleyici/Standardlaştırıcı
Bu uygulama, gelen metin verilerini (isimler, adresler vb.) büyük/küçük harf dönüşümü, boşluk temizleme gibi işlemlerle standarda uygun hale getiren, yerel çalışan bir masaüstü uygulamasıdır. Veritabanlarına veri girişi öncesi veri kalitesini artırmayı hedefler.

Özellikler
Yerel Çalışma: Uygulama tamamen yerel olarak çalışır, internet bağlantısı gerektirmez
Kurumsal Tasarım: Cumhurbaşkanlığı İletişim Başkanlığı kurumsal kimlik rehberine uygun tasarım
Gelişmiş Metin Temizleme: Büyük/küçük harf dönüşümü, boşluk temizleme, karakter standardizasyonu
Türkçe Karakter Desteği: Ç, Ğ, İ, Ö, Ş, Ü karakterleri tam destekleniyor
Kullanıcı Dostu Arayüz: Sade ve basit kullanım
Önizleme Özelliği: Değişikliklerin sarı renkte vurgulanması
Dosya İndirme: Temizlenmiş metni dosya olarak kaydetme
Panoya Kopyalama: Tek tıkla panoya kopyalama
Kurulum
Gereksinimler
Python 3.7 veya üzeri
Pillow kütüphanesi
Adımlar
Bağımlılıkları yükleyin:

pip install -r requirements.txt
Gerekli logo dosyalarını indirin:

logoo1.png - İletişim Başkanlığı logosu
flag_of_turkiye.png - Türk Bayrağı
Logo dosyalarını image/ klasörüne yerleştirin

Uygulamayı çalıştırın:

python text_cleaner.py
Kullanım
Metin Girişi: Üst kısımdaki "Giriş Metni" alanına temizlenecek metni yazın
Temizleme: "Metni Temizle ve Standardlaştır" butonuna tıklayın
Önizleme: "Önizleme" alanında değişikliklerin sarı renkte vurgulandığını görün
Sonuç: Alt kısımdaki "Standardlaştırılmış Metin" alanında temizlenmiş metni görün
Kopyalama: 📋 butonuna tıklayarak metni panoya kopyalayın
İndirme: "İndir" butonu ile temizlenmiş metni dosya olarak kaydedin
Temizleme: "Temizle" butonu ile tüm alanları temizleyebilirsiniz
Metin Temizleme İşlemleri
Uygulama şu işlemleri gerçekleştirir:

Büyük/Küçük Harf Dönüşümü: Tüm metni küçük harfe dönüştürür
Boşluk Temizleme: Birden fazla boşluğu tek boşluğa indirger
Noktalama Düzenleme: Noktalama işaretlerinin etrafındaki boşlukları düzenler
Karakter Temizliği: Sadece harfler, rakamlar ve yaygın noktalama işaretlerini korur
Türkçe Karakter Desteği: Ç, Ğ, İ, Ö, Ş, Ü karakterlerini destekler
Cümle Başı Büyük Harf: Nokta, ünlem ve soru işaretinden sonra büyük harf
Parantez Düzenleme: Parantezlerin etrafındaki boşlukları düzenler
Eğik Çizgi Düzenleme: Eğik çizgiden önceki fazla boşlukları kaldırır
Tasarım Özellikleri
Renk Paleti
Ana Arka Plan: eggplant-900 (#161013)
Metin Renkleri: eggplant-100 (#e3dadf) ve eggplant-200 (#c8b6c0)
Ana Eylem Butonları: red-DEFAULT (#FE1616)
Gri Alanlar: gray-500 (#818284)
Yeşil Buton: green (#28A745) - İndir butonu için
Sarı Vurgu: yellow (#FFFF00) - Değişiklikleri göstermek için
Logo Entegrasyonu
Header: İletişim Başkanlığı logosu (sol üst)
Footer: İletişim Başkanlığı logosu (sol alt) ve Türk Bayrağı (sağ alt)
Pencere Özellikleri
Boyut: 1000x680 piksel (sabit boyut)
Konumlandırma: Ekranın ortasında, görev çubuğunun üstünde
Responsive: Farklı ekran boyutlarında otomatik ortalanma
Teknik Detaylar
GUI Çerçevesi: tkinter (Python standart kütüphanesi)
Görüntü İşleme: Pillow (PIL)
Metin İşleme: Python string metodları ve düzenli ifadeler (re modülü)
Karşılaştırma: difflib modülü ile karakter bazlı karşılaştırma
Dosya İşlemleri: tkinter filedialog ile dosya kaydetme
Pano İşlemleri: tkinter clipboard ile panoya kopyalama
Özellikler
Önizleme Sistemi
Değişikliklerin sarı renkte vurgulanması
Hem boşluk hem karakter değişikliklerinin gösterimi
Gerçek zamanlı karşılaştırma
Dosya İşlemleri
Temizlenmiş metni .txt dosyası olarak kaydetme
Dosya adı ve konumu seçme imkanı
UTF-8 encoding desteği
Kullanıcı Deneyimi
Devre dışı butonlar (İndir butonu başlangıçta devre dışı)
Hover efektleri
Hata mesajları
Başarı bildirimleri
Sorun Giderme
Logo Yüklenemiyor
Eğer logolar yüklenemezse, konsola hata mesajı yazdırılır ve placeholder metinler gösterilir. Logo dosyalarının image/ klasöründe ve doğru isimle olduğundan emin olun.

Pillow Kurulum Sorunu
pip install --upgrade Pillow
Ekran Boyutu Sorunu
Uygulama 1000x680 piksel boyutunda tasarlanmıştır. Daha küçük ekranlarda sorun yaşanabilir. Bu durumda text_cleaner.py dosyasındaki window_width ve window_height değerlerini değiştirebilirsiniz.

Lisans
Bu proje Cumhurbaşkanlığı İletişim Başkanlığı için geliştirilmiştir.

Versiyon
v1.1 - Görsel yolları güncellendi, ekran boyutu optimize edildi

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

15) PDF Ayrıştırıcı (GUI)
Bu Python uygulaması, PDF dosyalarındaki belirli sayfa aralığını yeni bir PDF olarak ayırmanızı sağlar. Tüm işlemler kullanıcı dostu bir pencere (GUI) üzerinden yapılır.

Özellikler
PDF dosyasını pencere üzerinden seçme
Başlangıç ve bitiş sayfası giriş kutuları
PDF'yi ayırma işlemi tek tıkla
Sonuç ve hata mesajları pencere üzerinden
"İndir" butonu ile çıktı dosyasını istediğiniz klasöre kolayca kaydetme
Orijinal dosya bozulmaz
Kurulum
Python 3.7 veya üzeri yüklü olmalı
Gerekli kütüphaneleri yükleyin:
pip install -r requirements.txt
Not: Tkinter, Python'un standart kütüphanesidir. Ekstra kurulum gerekmez.

Kullanım
python pdf_ayristirici.py
Açılan pencerede:

"PDF Seç" ile dosyanızı seçin
Başlangıç ve bitiş sayfası girin
"PDF'yi Ayır" butonuna tıklayın
İşlem başarılıysa "İndir" butonu ile dosyayı istediğiniz yere kaydedin
Çıktı
Varsayılan olarak, çıktı dosyası uygulamanın olduğu klasöre kaydedilir (örn: ayiklanmis_sayfalar_2_6.pdf)
"İndir" butonu ile farklı bir klasöre kopyalayabilirsiniz
Lisans
MIT

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

16)PDF Çevirici
Bu proje, kullanıcıların Word, Excel, PowerPoint ve resim dosyalarını kolayca PDF formatına dönüştürmelerini sağlayan web tabanlı bir araçtır. Modern, hızlı ve kullanıcı dostu bir arayüze sahiptir.

Özellikler
Sürükle ve Bırak: Dosyaları kolayca yüklemek için sürükle ve bırak desteği sunar.
Dosya Önizleme: Yüklenen dosyanın adını, boyutunu ve türünü gösteren önizleme.
Hızlı Dönüştürme: Arka planda verimli bir şekilde çalışan dönüştürme işlemi.
Otomatik İndirme: Dönüştürme işlemi tamamlandığında PDF dosyası otomatik olarak indirilir.
Geniş Format Desteği: Popüler ofis ve resim dosyası formatlarını destekler.
Modern Arayüz: Tailwind CSS ile oluşturulmuş şık ve duyarlı tasarım.
Desteklenen Formatlar
Microsoft Word: .doc, .docx
Microsoft Excel: .xls, .xlsx
Microsoft PowerPoint: .ppt, .pptx
Resim Dosyaları: .png, .jpg, .jpeg
Kurulum ve Çalıştırma
Bu projenin çalışabilmesi için sisteminizde bazı bağımlılıkların kurulu olması gerekmektedir.

Gereksinimler
Python 3.x: Python'u indirin
Microsoft Office: Bu uygulama, .doc, .docx, .xls, .xlsx, .ppt, .pptx dosyalarını dönüştürmek için comtypes kütüphanesini kullanır. Bu nedenle, projenin çalışacağı makinede Microsoft Office (Word, Excel, PowerPoint) yüklü olmalıdır.
Git: Versiyon kontrolü için gereklidir.
Kurulum Adımları
Projeyi klonlayın:

git clone https://ciblab.dev/Besinci-Kat/pdf_donusturucu.git
cd pdf_donusturucu
Gerekli Python kütüphanelerini yükleyin:

pip install Flask comtypes-client Pillow python-dotenv
Uygulamayı Çalıştırma
Uygulamayı başlatmak için aşağıdaki komutu terminalde çalıştırın:

python app.py
Uygulama varsayılan olarak http://127.0.0.1:5000 adresinde çalışmaya başlayacaktır. Tarayıcınızdan bu adrese giderek PDF Çevirici'yi kullanabilirsiniz.

Kullanılan Teknolojiler
Backend:

Flask: Web sunucusu ve uygulama iskeleti.
comtypes: Microsoft Office otomasyonu için.
Pillow: Resim işleme ve PDF'e dönüştürme için.
Frontend:

HTML5
Tailwind CSS: Hızlı ve modern arayüz tasarımı için.
JavaScript: Dinamik kullanıcı etkileşimleri için.
İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

17) Video Üzerine Kurumsal Logo/İçerik Ekleme Aracı
Bu uygulama, kamu kurumları için eğitim ve bilgilendirme videolarına otomatik olarak kurumsal logo, tarih ve özel başlıklar/metinler eklemek için geliştirilmiştir. Cumhurbaşkanlığı İletişim Başkanlığı kurumsal kimlik rehberine uygun olarak tasarlanmıştır.

🎯 Özellikler
🎬 Video İşleme: MP4, AVI, MOV formatlarını destekler
🏢 Logo Ekleme: PNG formatında şeffaf logoları destekler
📝 Metin Ekleme: Özelleştirilebilir başlık ve alt başlık
📅 Tarih Ekleme: Gerçek tarih seçici ile otomatik tarih ekleme
🎨 Kurumsal Tasarım: Cumhurbaşkanlığı kurumsal kimlik rehberine uygun
⚙️ Esnek Konumlandırma: Logo ve metinler için çeşitli konum seçenekleri
🔊 Ses Koruma: Orijinal video sesini korur
🖼️ Önizleme: Gerçek zamanlı önizleme paneli
📊 İlerleme Takibi: Video işleme sürecini takip etme
🔧 Font Desteği: Windows Arial fontu ile uyumlu metin oluşturma
🚀 Kurulum
Gereksinimler
Python 3.8 veya üzeri
Windows 10/11 (Arial font desteği için)
Adımlar
Projeyi klonlayın:

git clone https://ciblab.dev/Besinci-Kat/video-icerik-ekleme
cd video-icerik-ekleme
Gerekli kütüphaneleri yükleyin:

pip install -r requirements.txt
💻 Kullanım
Uygulamayı çalıştırın:

python video_overlay_tool.py
Dosya Seçimi:

Video dosyasını seçin (MP4, AVI, MOV)
Logo dosyasını seçin (PNG formatı önerilir)
Çıktı klasörünü belirleyin
İçerik Ayarları:

Başlık metni girin
Alt başlık metni girin (opsiyonel)
Tarih ekleme seçeneğini işaretleyin
Tarih seçici ile istediğiniz tarihi seçin
Konumlandırma:

Logo konumu: Sol Üst, Sağ Üst, Sol Alt, Sağ Alt
Metin konumu: Sol Üst, Sağ Üst, Sol Alt, Sağ Alt, Orta
Tarih konumu: Sol Üst, Sağ Üst, Sol Alt, Sağ Alt
Boyut ve Şeffaflık:

Logo boyutunu ayarlayın (%1-50)
Logo şeffaflığını ayarlayın (0-100%)
Font boyutunu ayarlayın (16-72px)
Video Oluştur:

"Video Oluştur" butonuna tıklayın
İşlem durumunu takip edin
Tamamlandığında çıktı klasöründe videoyu bulun
🛠️ Teknik Detaylar
Kullanılan Teknolojiler
MoviePy: Video işleme ve birleştirme
PIL (Pillow): Metin görüntü oluşturma
NumPy: Array dönüşümleri
Tkinter: Kullanıcı arayüzü
tkcalendar: Tarih seçici
Font Desteği
Windows Arial fontu (C:/Windows/Fonts/arial.ttf)
Fallback olarak sistem varsayılan fontu
Türkçe karakter desteği
Video Formatları
Giriş: MP4, AVI, MOV, MKV
Çıkış: MP4 (H.264 codec)
Ses: AAC codec ile korunur
📁 Proje Yapısı
video-icerik-ekleme/
├── video_overlay_tool.py    # Ana uygulama
├── requirements.txt         # Python bağımlılıkları
├── README.md               # Bu dosya
├── assets/                 # Kurumsal varlıklar
├── image/                  # Örnek logolar
│   ├── flag_of_turkiye.png
│   └── logoo1.png
├── ornek_cikti/            # Çıktı videoları
└── ornek_video/            # Örnek videolar
🔧 Sorun Giderme
Metin Görünmüyor
Font dosyasının mevcut olduğundan emin olun
Metin alanlarının dolu olduğunu kontrol edin
Tarih checkbox'ının işaretli olduğunu kontrol edin
Video İşleme Hatası
MoviePy kütüphanesinin yüklü olduğundan emin olun
Video dosyasının bozuk olmadığını kontrol edin
Yeterli disk alanı olduğundan emin olun
Logo Görünmüyor
Logo dosyasının PNG formatında olduğunu kontrol edin
Logo boyutunun çok küçük olmadığından emin olun
Logo konumunun video dışında kalmadığını kontrol edin
📝 Lisans
Bu proje kamu kurumları için geliştirilmiştir. Cumhurbaşkanlığı İletişim Başkanlığı kurumsal kimlik rehberine uygun olarak tasarlanmıştır.

🤝 Katkıda Bulunma
Projeyi fork edin
Feature branch oluşturun (git checkout -b feature/AmazingFeature)
Değişikliklerinizi commit edin (git commit -m 'Add some AmazingFeature')
Branch'inizi push edin (git push origin feature/AmazingFeature)
Pull Request oluşturun
İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

18) XML/JSON Veri Dönüştürücü
Bu proje, yapısal veri formatları olan XML ve JSON dosyalarını, daha yaygın kullanılan CSV ve Excel (XLSX) formatlarına dönüştürmek için geliştirilmiş bir masaüstü uygulamasıdır. Kullanıcı dostu bir arayüz (GUI) sayesinde teknik bilgisi olmayan kullanıcıların bile kolayca veri dönüşümü yapabilmesini hedefler.

Projenin Amacı
Bu aracın temel amacı, farklı sistemler arasında veri alışverişi yaparken karşılaşılan format uyumsuzluğu sorununu çözmektir. Özellikle kamu kurumları veya farklı departmanlar arasında veri paylaşımı yapılırken, bir sistemin ürettiği XML veya JSON çıktısını, Excel gibi ofis programlarında analiz etmek veya başka bir sisteme CSV olarak aktarmak gerekebilir. Bu dönüştürücü, bu süreci otomatikleştirerek zaman kazandırır ve manuel hataları önler.

Temel Özellikler
Format Desteği: XML ve JSON kaynak dosyalarını CSV ve Excel (XLSX) hedeflerine dönüştürür.
Dinamik Alan Tespiti: XML ve JSON dosyalarındaki tüm veri alanlarını (sütunları) otomatik olarak algılar ve tabloya ekler. Eksik alanlar boş bırakılır.
Kullanıcı Dostu Arayüz: Tkinter ile oluşturulmuş basit ve anlaşılır bir grafik arayüze sahiptir.
Otomatik Format Algılama: Seçilen dosyanın uzantısına (.xml veya .json) göre kaynak formatı otomatik olarak ayarlar.
Hata Yönetimi: Geçersiz dosya formatı, boş dosya veya eksik seçim gibi durumlarda kullanıcıyı bilgilendirir.
Türkçe Karakter Desteği: Çıktı dosyalarında (özellikle Excel) Türkçe karakterlerin doğru görüntülenmesini sağlar.
Kurulum ve Çalıştırma
Depoyu Klonlayın:

git clone https://ciblab.dev/muhammedesat_demir/xml-json_veridonusturucu.git
cd xml-json_veridonusturucu
Gerekli Kütüphaneleri Yükleyin: Projenin ihtiyaç duyduğu kütüphaneleri requirements.txt dosyasını kullanarak yükleyin.

pip install -r requirements.txt
Uygulamayı Çalıştırın:

python veri_donusturucu.py
Nasıl Kullanılır?
Kaynak Dosyayı Seçin: "Kaynak Dosya Seç" butonuna tıklayarak dönüştürmek istediğiniz .xml veya .json uzantılı dosyayı seçin.
Hedef Formatı Belirleyin: Hangi formata (CSV veya Excel) dönüştürmek istediğinizi seçin.
Hedef Dosyayı Kaydedin: "Hedef Dosya Kaydet" butonuna tıklayarak dönüştürülmüş verinin nereye ve hangi isimle kaydedileceğini belirtin.
Dönüştürün: "Dönüştür" butonuna basarak işlemi başlatın. İşlem durumu arayüzdeki mesaj alanında görüntülenecektir.
Desteklenen Veri Yapıları
Dönüştürücünün doğru çalışması için kaynak dosyalarınızın aşağıdaki gibi yapılandırılmış olması beklenir:

XML Örneği
Veri kayıtları bir kök etiket altında listelenmelidir. Kayıt etiketi (<evrak>) dinamik olarak algılanır.

<?xml version="1.0" encoding="UTF-8"?>
<evraklar>
    <evrak>
        <id>1</id>
        <ad>Ahmet Yılmaz</ad>
        <yas>25</yas>
    </evrak>
    <evrak>
        <id>2</id>
        <ad>Ayşe Demir</ad>
        <yas>30</yas>
    </evrak>
</evraklar>
JSON Örneği
Veri, bir dizi (liste) içinde yer alan nesnelerden (sözlüklerden) oluşmalıdır.

[
    {
        "id": 1,
        "ad": "Ahmet Yılmaz",
        "yas": 25
    },
    {
        "id": 2,
        "ad": "Ayşe Demir",
        "yas": 30
    }
]
Sorun Giderme ve İpuçları
Türkçe Karakter Sorunları: Eğer CSV dosyasını Excel'de açtığınızda Türkçe karakterler bozuk görünüyorsa, Excel'in "Veri > Metinden/CSV'den" seçeneğini kullanarak dosyayı UTF-8 kodlamasıyla içe aktarın. En sorunsuz deneyim için doğrudan Excel (XLSX) formatını tercih edin.
"Veri Bulunamadı" Hatası: Bu hata, XML dosyasında veri kayıtları bulunamadığında veya JSON dosyasının içi boş olduğunda ortaya çıkar. Dosya yapılarınızı kontrol edin.
Büyük Dosyalar: Çok büyük dosyaların dönüştürülmesi biraz zaman alabilir. Lütfen işlem tamamlanana kadar bekleyin.
Gereksinimler
Python 3.7+
pandas
openpyxl
tkinter (Python standart kütüphanesi)
Lisans
Bu proje MIT Lisansı ile lisanslanmıştır.

İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com

19) Zamanlayıcı/Hatırlatıcı Uygulaması
Cumhurbaşkanlığı İletişim Başkanlığı Tasarım Kılavuzu'na uygun olarak geliştirilmiş, sistem tepsisi ikonu ile çalışan zamanlayıcı ve hatırlatıcı uygulaması.

🎯 Özellikler
Sistem Tepsisi İkonu: Ana pencere yerine sistem tepsisi ikonu ile çalışır
Zamanlanmış Görevler: Belirli saatlerde otomatik olarak çalışan görevler
Görsel Bildirimler: Windows native bildirimleri ve Tkinter pop-up'ları
Sesli Bildirim: Görev tetiklendiğinde uyari_ses.wav dosyası ile sesli uyarı (simpleaudio ile)
Veri Kalıcılığı: Görevler JSON formatında kaydedilir ve uygulama yeniden başlatıldığında korunur
Kurumsal Tasarım: Cumhurbaşkanlığı İletişim Başkanlığı renk paleti kullanılır
Hata Yönetimi: Kapsamlı hata kontrolü ve kullanıcı geri bildirimi
🚀 Kurulum
Gereksinimler
Python 3.7 veya üzeri
Windows işletim sistemi (sistem tepsisi ve ses desteği için)
Adım 1: Kütüphaneleri Kurun
pip install -r requirements.txt
Adım 2: Ses Dosyasını Ekleyin
Proje klasörüne 16-bit PCM WAV formatında, adı tam olarak uyari_ses.wav olan bir ses dosyası ekleyin.
(Öneri: Audacity veya online converter ile dönüştürün.)
Adım 3: Uygulamayı Çalıştırın
python main.py
📋 Kullanım
Sistem Tepsisi Menüsü
Uygulama başlatıldığında sistem tepsisi ikonu görünür. İkona sağ tıklayarak menüye erişebilirsiniz:

Yeni Görev Ekle...: Yeni bir zamanlanmış görev ekler
Planlanmış Görevleri Göster: Mevcut görevleri listeler ve yönetir
Şimdi Tetikle (Test): Tüm görevleri hemen çalıştırır (test için)
Çıkış: Uygulamayı kapatır
Görev Ekleme
Sistem tepsisi ikonuna sağ tıklayın
"Yeni Görev Ekle..." seçeneğini seçin
Görev açıklamasını girin
Zamanı HH:MM formatında girin (örn: 14:30)
"Ekle" butonuna tıklayın
Görev Yönetimi
Görevleri Görüntüleme: "Planlanmış Görevleri Göster" menüsünden
Görev Silme: Listeden görevi seçip "Seçili Görevi Sil" butonuna tıklayın
Görev Yenileme: "Yenile" butonu ile listeyi güncelleyin
🎨 Tasarım
Renk Paleti
Uygulama, Cumhurbaşkanlığı İletişim Başkanlığı Tasarım Kılavuzu'na uygun renkler kullanır:

Ana Arka Plan: #161013 (eggplant-900)
Metinler: #e3dadf (eggplant-100)
İkincil Metinler: #c8b6c0 (eggplant-200)
Vurgu Rengi: #FE1616 (red-DEFAULT)
Gri Tonları: #818284, #1a1a1a
Tasarım İlkeleri
Sade Tasarım: Basit ve kullanıcı dostu arayüz
Performans: Gereksiz animasyonlardan kaçınma
Lokal Çalışma: İnternet bağımlılığı olmayan yapı
📁 Proje Yapısı
zamanlıyıcı-hatırlatıcı/
├── main.py                 # Ana uygulama dosyası
├── config.py              # Konfigürasyon ve renk paleti
├── data_manager.py        # Veri yönetimi
├── task_manager.py        # Görev yönetimi
├── notification_manager.py # Bildirim sistemi
├── system_tray.py         # Sistem tepsisi yönetimi
├── requirements.txt       # Gerekli kütüphaneler
├── README.md             # Bu dosya
├── tasks.json            # Görev verileri (otomatik oluşur)
├── app.log               # Uygulama logları (otomatik oluşur)
└── uyari_ses.wav         # Sesli bildirim dosyası (kullanıcı ekler)
🔧 Teknik Detaylar
Kullanılan Kütüphaneler
schedule: Zamanlanmış görevler için
pystray: Sistem tepsisi ikonu için
Pillow: Görsel işlemler için
tkinter: GUI bileşenleri için
simpleaudio: Sesli bildirim için (WAV desteği)
threading: Arka plan işlemleri için
Mimari
Modüler Yapı: Her işlev ayrı modülde
Thread Güvenliği: Zamanlayıcı ayrı thread'de çalışır
Veri Kalıcılığı: JSON formatında görev saklama
Hata Yönetimi: Kapsamlı exception handling
🐛 Sorun Giderme
Yaygın Sorunlar
Kütüphane Hatası: pip install -r requirements.txt komutunu çalıştırın
Sistem Tepsisi Görünmüyor: Windows bildirim ayarlarını kontrol edin
Görevler Çalışmıyor: Sistem saatinin doğru olduğundan emin olun
Sesli Bildirim Çalmıyor: uyari_ses.wav dosyasının gerçekten 16-bit PCM WAV formatında olduğundan emin olun. (Audacity veya online converter ile dönüştürün.)
Log Dosyası
Hata ayıklama için app.log dosyasını kontrol edin.

📝 Lisans
Bu proje Cumhurbaşkanlığı İletişim Başkanlığı için geliştirilmiştir.

🤝 Katkıda Bulunma
Projeyi fork edin
Feature branch oluşturun (git checkout -b feature/AmazingFeature)
Değişikliklerinizi commit edin (git commit -m 'Add some AmazingFeature')
Branch'inizi push edin (git push origin feature/AmazingFeature)
Pull Request oluşturun
İletişim
ai@iletisim.gov.tr
demirmuhammedesat@gmail.com
Versiyon: 1.0.0
Son Güncelleme: 2025

20) Castlyo Platform
Profesyonel casting ve yetenek eşleştirme platformu. Oyuncu, model, müzisyen gibi yetenekler ile film, dizi, reklam ajansları arasında güvenli ve etkili bir köprü kurar.

🚀 Özellikler
Güvenli Platform: KVKK uyumlu, SSL şifreli, güvenli ödeme sistemi
Akıllı Eşleştirme: Yapay zeka destekli algoritma ile hızlı ve doğru eşleştirme
Geniş Ağ: 500+ yetenek ve 50+ doğrulanmış ajans
Kullanıcı Dostu: Modern ve responsive tasarım
7/24 Erişim: Dünyanın her yerinden erişilebilir
Mobil Uygulama: iOS ve Android desteği
🏗️ Teknoloji Stack
Frontend
Next.js 14 - React framework
TypeScript - Tip güvenliği
Tailwind CSS - Styling
Radix UI - UI bileşenleri
Lucide React - İkonlar
Backend
NestJS - Node.js framework
TypeScript - Tip güvenliği
Drizzle ORM - Veritabanı ORM
PostgreSQL - Ana veritabanı
Redis - Cache ve session
JWT - Kimlik doğrulama
Veritabanı
PostgreSQL - Ana veritabanı
Drizzle ORM - Type-safe ORM
Migrations - Otomatik veritabanı yönetimi
DevOps
Docker - Containerization
Turbo - Monorepo build tool
ESLint - Kod kalitesi
Prettier - Kod formatı
📁 Proje Yapısı
castlyo-platform/
├── apps/
│   ├── web/                 # Next.js frontend
│   └── api/                 # NestJS backend
├── packages/
│   ├── database/            # Veritabanı şeması ve bağlantısı
│   ├── types/               # Paylaşılan TypeScript tipleri
│   └── ui/                  # Paylaşılan UI bileşenleri
├── scripts/                 # Yardımcı scriptler
├── docker-compose.yml       # Geliştirme ortamı
└── turbo.json               # Monorepo konfigürasyonu
🚀 Kurulum
Gereksinimler
Node.js 18+
npm 10+
Docker Desktop
PostgreSQL
1. Projeyi klonlayın
git clone https://github.com/your-username/castlyo-platform.git
cd castlyo-platform
2. Bağımlılıkları yükleyin
npm install
3. Ortam değişkenlerini ayarlayın
copy dev.env .env
# .env dosyasını düzenleyin
4. Veritabanını başlatın
# Docker ile PostgreSQL ve Redis
docker-compose up -d

# Veya manuel olarak PostgreSQL kurun
5. Veritabanı şemasını oluşturun
npm run db:generate
npm run db:push
6. Projeyi başlatın
# Tüm servisleri başlat
npm run dev

# Veya ayrı ayrı
npm run dev:web      # Frontend (port 3000)
npm run dev:api      # Backend (port 3001)
🌐 Erişim
Frontend: http://localhost:3000
Backend API: http://localhost:3001
Database: localhost:5432
Redis: localhost:6379
📚 API Dokümantasyonu
API dokümantasyonu için Swagger UI'ya erişin:

Swagger: http://localhost:3001/api/docs
🧪 Test
# Tüm testleri çalıştır
npm run test

# Belirli bir paketi test et
npm run test --filter=@castlyo/api
npm run test --filter=@castlyo/web
🏗️ Geliştirme
Yeni paket ekleme
npm run turbo gen package
Yeni uygulama ekleme
npm run turbo gen app
Veritabanı migration
npm run db:generate
npm run db:push
npm run db:migrate
📦 Build
# Tüm paketleri build et
npm run build

# Belirli bir paketi build et
npm run build --filter=@castlyo/web
npm run build --filter=@castlyo/api
🐳 Docker
Geliştirme ortamı
docker-compose up -d
Production ortamı
docker-compose -f docker-compose.prod.yml up -d
🔧 Konfigürasyon
Ortam Değişkenleri
DATABASE_URL - PostgreSQL bağlantı URL'i
REDIS_HOST - Redis host adresi
JWT_SECRET - JWT şifreleme anahtarı
SMTP_* - E-posta ayarları
AWS_* - AWS S3 ayarları
Veritabanı
PostgreSQL 15+
Drizzle ORM ile type-safe sorgular
Otomatik migration sistemi
🤝 Katkıda Bulunma
Fork yapın
Feature branch oluşturun (git checkout -b feature/amazing-feature)
Commit yapın (git commit -m 'Add amazing feature')
Push yapın (git push origin feature/amazing-feature)
Pull Request oluşturun
📄 Lisans
Bu proje MIT lisansı altında lisanslanmıştır. Detaylar için LICENSE dosyasına bakın.

📞 İletişim
Website: https://castlyo.com
Email: info@castlyo.com
Telefon: +90 (212) XXX XX XX
🙏 Teşekkürler
Next.js - React framework
NestJS - Node.js framework
Drizzle - TypeScript ORM
Tailwind CSS - CSS framework
Radix UI - UI bileşenleri
Castlyo - Yetenekler ve Fırsatlar Buluşuyor 🎭✨
- bu projeyi lokalde başarıyla çalıştırdım, sonra canlıya alırken patlattım, yani bir arkadaş için yapıyordum, ben patlayınca başkası yaptı ve siteyi canlıya aldı, şuan proje çalışmıyor, revize gerekiyor ama yapıp çalıştırmıştım lokalde, canlıya almaya çalıştığım süreçte yaptığım değişikliklerle patlatmıştım.

21) Türkçe Metin Seslendirme (Google Gemini TTS)
Türkçe metinleri Google Gemini TTS API'si kullanarak seslendiren basit bir web uygulaması. Metinleri kadın (Kore) veya erkek (Orus) sesiyle seslendirebilir ve oluşturulan ses dosyalarını dinleyip indirebilirsiniz.

Özellikler
📝 Türkçe metin girişi
🎤 İki ses seçeneği: Kadın (Kore) ve Erkek (Orus)
🔊 Oluşturulan ses dosyasını tarayıcıda dinleme
💾 Ses dosyasını indirme
📁 Ses dosyaları ses_kayitlari klasörüne otomatik kaydedilir
🔢 Sıra numaralı dosya kaydetme (output.wav, output2.wav, ...)
Kurulum
Projeyi klonlayın veya indirin.

Gerekli paketleri yükleyin:

pip install -r requirements.txt
Çalıştırma
app.py dosyasını açın ve API_KEY değişkenine kendi Google Genai API anahtarınızı yazın:

API_KEY = "BURAYA_OWN_GOOGLE_GENAI_API_KEYINIZI_YAZIN"
Uygulamayı başlatın:

python app.py
Tarayıcınızda şu adresi açın:

http://127.0.0.1:5000
API Key Hakkında
Bu proje Google Gemini TTS API'sini kullanır. Uygulamayı çalıştırmadan önce:

Google Genai API anahtarınızı alın.
app.py dosyasındaki API_KEY değişkenine kendi anahtarınızı yazın.
Önemli: API anahtarınızı GitHub'a yüklemeyin. Güvenlik için .gitignore dosyasına app.py ekleyebilir veya API anahtarınızı ayrı bir yapılandırma dosyasında tutabilirsiniz.
Dosya Yapısı
ses_donusumu/
├── app.py                 # Ana Flask uygulaması
├── requirements.txt        # Python bağımlılıkları
├── README.md              # Bu dosya
├── templates/
│   └── index.html         # Web arayüzü
└── ses_kayitlari/         # Oluşturulan ses dosyaları (otomatik oluşturulur)
Kullanım
Web arayüzünde metin alanına seslendirmek istediğiniz Türkçe metni yazın.
Ses seçimi dropdown'ından "Kadın" veya "Erkek" sesini seçin.
"Seslendir" butonuna tıklayın.
Oluşturulan ses dosyası sayfada gösterilir ve tarayıcıda dinlenebilir veya indirilebilir.

22) basit ve ilk denemem olan mobil projem
✈️ Uçuş Rezervasyon Sistemi
Gerçek zamanlı uçuş arama, sepet yönetimi, ödeme işlemleri ve PDF bilet oluşturma özelliklerine sahip Full-Stack Mobil Uçuş Rezervasyon Sistemi.

React Native Expo Node.js MySQL Express.js

📋 İçindekiler
Proje Hakkında
Özellikler
Kullanılan Teknolojiler
Proje Mimarisi
Kurulum ve Çalıştırma
Yapılandırma
API Endpoints
Veritabanı Şeması
📖 Proje Hakkında
Bu proje, kullanıcıların uçuş araması yapabildiği, sepete uçuş ekleyebildiği, yolcu ve ödeme bilgilerini girebildiği ve rezervasyon sonrasında PDF formatında e-bilet alabildiği kapsamlı bir mobil uygulama sistemidir.

Proje, Service-Oriented Architecture (Servis Odaklı Mimari) prensiplerine uygun olarak geliştirilmiştir. API çağrıları merkezi bir servis katmanında yönetilmekte, UI bileşenleri yeniden kullanılabilir şekilde tasarlanmakta ve tüm yapılandırma ayarları tek bir noktadan kontrol edilmektedir.

✨ Özellikler
Özellik	Açıklama
Dinamik Uçuş Arama	Kalkış, varış ve tarih bazlı gerçek zamanlı arama (7 günlük pencere)
Akıllı Veritabanı	CURDATE() fonksiyonu ile dinamik uçuş verileri
Sepet Sistemi	Uçuş seçimi ve sepet yönetimi
Yolcu Bilgileri	Ad, soyad, TC kimlik, telefon ve e-posta girişi
Ödeme Formu	Kart numarası, son kullanma tarihi ve CVV alanları
PDF Bilet Oluşturma	expo-print ile profesyonel e-bilet tasarımı
Bilet Paylaşımı	expo-sharing ile PDF kaydetme ve paylaşma
Form Validasyonu	Client ve server tarafında kapsamlı doğrulama
🛠 Kullanılan Teknolojiler
Mobil Uygulama (Frontend)
Teknoloji	Kullanım Amacı
React Native	Cross-platform mobil uygulama geliştirme
Expo	Geliştirme platformu ve build araçları
React Navigation	Ekranlar arası navigasyon yönetimi
expo-print	PDF oluşturma
expo-sharing	Dosya paylaşım fonksiyonları
Backend (Sunucu)
Teknoloji	Kullanım Amacı
Node.js	JavaScript runtime environment
Express.js	RESTful API framework
MySQL	İlişkisel veritabanı yönetim sistemi
mysql2	Promise tabanlı MySQL driver
CORS	Cross-origin kaynak paylaşımı
🏗 Proje Mimarisi
Proje, modüler ve ölçeklenebilir bir mimari ile tasarlanmıştır. Her katman kendi sorumluluğuna sahiptir:

ucus-rezervasyon-sistemi/
│
├── 📄 .gitignore                    # Git ignore kuralları
├── 📄 README.md                     # Proje dokümantasyonu
│
├── 📁 backend/                      # Node.js REST API
│   ├── 📄 database.sql             # MySQL şema ve seed verileri
│   ├── 📄 index.js                 # Express sunucu ve endpoint'ler
│   ├── 📄 package.json             # Sunucu bağımlılıkları
│   └── 📄 package-lock.json
│
└── 📁 mobile-app/                   # React Native (Expo) Uygulaması
    ├── 📄 App.js                   # Uygulama giriş noktası
    ├── 📄 app.json                 # Expo yapılandırması
    ├── 📄 babel.config.js          # Babel yapılandırması
    ├── 📄 package.json             # Mobil bağımlılıklar
    │
    └── 📁 src/                      # Kaynak kodlar
        │
        ├── 📁 components/           # Yeniden Kullanılabilir UI Bileşenleri
        │   ├── 📄 index.js         # Barrel export
        │   ├── 📄 FlightCard.js    # Uçuş kartı bileşeni
        │   ├── 📄 CartSummary.js   # Sepet özeti bileşeni
        │   ├── 📄 FormInput.js     # Form input bileşeni
        │   └── 📄 LoadingSpinner.js # Yükleme göstergesi
        │
        ├── 📁 screens/              # Uygulama Ekranları
        │   ├── 📄 index.js         # Barrel export
        │   ├── 📄 SearchScreen.js  # Uçuş arama ekranı
        │   ├── 📄 FlightResultsScreen.js # Sonuç listesi
        │   └── 📄 CheckoutScreen.js # Ödeme ve bilet ekranı
        │
        ├── 📁 services/             # API Servis Katmanı
        │   ├── 📄 index.js         # Barrel export
        │   └── 📄 api.js           # Tüm API çağrıları
        │
        ├── 📁 navigation/           # Navigasyon Yapılandırması
        │   ├── 📄 index.js         # Barrel export
        │   └── 📄 AppNavigator.js  # Stack navigator
        │
        ├── 📁 constants/            # Sabitler ve Yapılandırma
        │   ├── 📄 index.js         # Barrel export
        │   ├── 📄 config.js        # API URL ve ayarlar
        │   └── 📄 theme.js         # Renkler, fontlar, spacing
        │
        └── 📁 utils/                # Yardımcı Fonksiyonlar
            ├── 📄 index.js         # Barrel export
            ├── 📄 formatters.js    # Tarih, fiyat formatlama
            └── 📄 validators.js    # Form validasyonları
Mimari Açıklamalar
Katman	Dosya/Klasör	Sorumluluk
Services	src/services/api.js	Tüm HTTP istekleri merkezi olarak yönetilir
Screens	src/screens/	Her ekran kendi dosyasında, temiz ve okunabilir
Components	src/components/	Yeniden kullanılabilir UI parçaları
Constants	src/constants/	Tüm sabitler tek noktadan yönetilir
Utils	src/utils/	Formatlama ve validasyon fonksiyonları
🚀 Kurulum ve Çalıştırma
Ön Gereksinimler
Node.js v16 veya üzeri
MySQL Server 8.0+
Expo CLI (npm install -g expo-cli)
Expo Go uygulaması (mobil cihazınızda)
1️⃣ Veritabanı Kurulumu
MySQL komut satırını veya MySQL Workbench'i açın ve aşağıdaki komutu çalıştırın:

mysql -u root -p < backend/database.sql
Veya database.sql dosyasının içeriğini MySQL istemcinize kopyalayıp yapıştırın.

Not: Veritabanı adı ucak_rezervasyon olarak oluşturulacaktır.

2️⃣ Backend Kurulumu
# Backend klasörüne git
cd backend

# Bağımlılıkları yükle
npm install

# Sunucuyu başlat
npm start
Sunucu http://localhost:3000 adresinde çalışmaya başlayacaktır.

MySQL Bağlantı Ayarları
backend/index.js dosyasında veritabanı bilgilerinizi güncelleyin:

const pool = mysql.createPool({
  host: 'localhost',
  user: 'root',           // MySQL kullanıcı adınız
  password: 'sifreniz',   // MySQL şifreniz
  database: 'ucak_rezervasyon',
  port: 3306
});
3️⃣ Mobil Uygulama Kurulumu
# Mobile-app klasörüne git
cd mobile-app

# Bağımlılıkları yükle
npm install

# Expo geliştirme sunucusunu başlat
npx expo start
Ardından:

Android Emulator: a tuşuna basın
iOS Simulator: i tuşuna basın
Fiziksel Cihaz: Expo Go uygulaması ile QR kodu tarayın
⚙️ Yapılandırma
API URL Ayarı
API bağlantı ayarları src/constants/config.js dosyasında merkezi olarak yönetilir:

// src/constants/config.js

const DEV_MODE = true;

/**
 * API Yapılandırması
 *
 * Android Emulator için: '10.0.2.2' kullanın
 * iOS Simulator için: 'localhost' kullanın
 * Fiziksel Cihaz için: Bilgisayarınızın IP adresini kullanın
 */
const LOCAL_IP = '192.168.1.34';  // <- Buraya kendi IP adresinizi yazın
const API_PORT = 3000;

export const API_BASE_URL = DEV_MODE
  ? `http://${LOCAL_IP}:${API_PORT}`
  : 'https://api.production-server.com';
IP Adresinizi Bulma
İşletim Sistemi	Komut
Windows	Komut İstemi'nde ipconfig yazın, IPv4 Address satırına bakın
macOS / Linux	Terminal'de ifconfig yazın
Ortam Bazlı IP Tablosu
Çalıştırma Ortamı	LOCAL_IP Değeri
Android Emulator	10.0.2.2
iOS Simulator	localhost
Fiziksel Cihaz (Wi-Fi)	192.168.x.x (bilgisayarınızın IP'si)
📡 API Endpoints
Backend sunucusu aşağıdaki endpoint'leri sunar:

Metod	Endpoint	Açıklama	Parametreler
GET	/cities	Tüm şehirleri listeler	-
GET	/search	Uçuş arama	origin, destination, date
POST	/book	Rezervasyon oluşturma	JSON body
Örnek İstekler
Şehirleri Listele:

curl http://localhost:3000/cities
Uçuş Ara:

curl "http://localhost:3000/search?origin=2&destination=4&date=2025-01-15"
Rezervasyon Oluştur:

curl -X POST http://localhost:3000/book \
  -H "Content-Type: application/json" \
  -d '{
    "passenger_name": "Ahmet",
    "passenger_surname": "Yılmaz",
    "tc_no": "12345678901",
    "phone": "05551234567",
    "email": "ahmet@example.com",
    "flight_id": 1
  }'
🗄 Veritabanı Şeması
Tablolar
Tablo	Açıklama
CITIES	Şehirler (İSTANBUL, ANKARA, İZMİR, ANTALYA)
AIRLINES	Havayolu firmaları (THY, PEGASUS, ANADOLUJET)
FLIGHTS	Uçuş seferleri (dinamik 7 günlük veri)
BOOKINGS	Yolcu rezervasyonları
Entity-Relationship Diyagramı
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   CITIES    │         │   FLIGHTS   │         │  AIRLINES   │
├─────────────┤         ├─────────────┤         ├─────────────┤
│ id (PK)     │◄───────┤│ origin_city │         │ id (PK)     │
│ name        │         │ dest_city   │├────────►│ name        │
└─────────────┘         │ airline_id  ││         │ logo_code   │
                        │ departure   ││         └─────────────┘
                        │ arrival     ││
                        │ price       ││
                        │ id (PK)     │◄─────────┐
                        └─────────────┘          │
                                                 │
                        ┌─────────────┐          │
                        │  BOOKINGS   │          │
                        ├─────────────┤          │
                        │ id (PK)     │          │
                        │ name        │          │
                        │ surname     │          │
                        │ tc_no       │          │
                        │ phone       │          │
                        │ email       │          │
                        │ flight_id   │──────────┘
                        │ created_at  │
                        └─────────────┘
🔐 Güvenlik Özellikleri
Input Validasyonu: Client ve server tarafında form doğrulama
SQL Injection Koruması: Parametreli sorgular kullanımı
CORS Yapılandırması: Kontrollü API erişimi
TC Kimlik Doğrulama: 11 haneli format kontrolü
📂 Dosya Detayları
Servis Katmanı (src/services/api.js)
Tüm API çağrıları bu dosyada merkezi olarak yönetilir:

const FlightAPI = {
  getCities: async () => { ... },
  searchFlights: async (origin, destination, date) => { ... },
  createBooking: async (bookingData) => { ... },
};
Tema Yapılandırması (src/constants/theme.js)
Uygulama genelinde kullanılan renkler ve stiller:

export const COLORS = {
  primary: '#1e88e5',
  secondary: '#2e7d32',
  background: '#f5f5f5',
  // ...
};
🤝 Katkıda Bulunma
Projeyi fork edin
Feature branch oluşturun (git checkout -b feature/YeniOzellik)
Değişikliklerinizi commit edin (git commit -m 'Yeni özellik eklendi')
Branch'e push edin (git push origin feature/YeniOzellik)
Pull Request açın
📝 Lisans
Bu proje MIT Lisansı altında lisanslanmıştır.

⭐ Bu projeyi beğendiyseniz yıldız vermeyi unutmayın!

23) BERTurk ile Türkçe Otel Yorumları Duygu Analizi. Web scraping ile oluşturulan doğal veri seti, sınıf dengesizliği (class imbalance) optimizasyonu ve XAI çalışmaları. (Bitirme Projesi)
bu proje tamamlanmadı(iki kişi olarak bu proje üzerinde çalışmaya devam ediyoruz. gerekli detayı bilgilerim/Bitirme2_IlerlemeRaporu_AhmetDulger_MuhammedEsatDemir.pdf pdfinde görüntüleyebilirsin)

24) Orbacle Web
Mistik bir kehanet uygulaması - Magic 8-Ball'dan ilham alınarak geliştirilmiş interaktif web deneyimi.

Proje Hakkında
Orbacle, kullanıcıların sorularına mistik bir küre aracılığıyla cevap aldığı bir fal/kehanet uygulamasıdır. Animasyonlu bir küre arayüzü, soru geçmişi takibi ve çoklu dil desteği sunar.

Teknolojiler
React 18 - UI framework
TypeScript 5 - Tip güvenliği
Vite 5 - Build tool ve dev server
Framer Motion 11 - Animasyonlar
i18next - Çoklu dil desteği (Türkçe/İngilizce)
Özellikler
Animasyonlu mistik küre arayüzü
20 farklı kehanet cevabı
Soru-cevap geçmişi (localStorage'da saklanır, max 50 kayıt)
Türkçe ve İngilizce dil desteği
Karanlık tema, mistik görsel efektler
Responsive tasarım
Kurulum
# Bağımlılıkları yükle
npm install

# Geliştirme sunucusunu başlat
npm run dev
Uygulama http://localhost:3000 adresinde açılacaktır.

Diğer Komutlar
# Prodüksiyon build
npm run build

# Build önizleme
npm run preview

# Lint kontrolü
npm run lint
Proje Yapısı
src/
├── components/          # React bileşenleri
│   ├── MysticalOrb      # Animasyonlu küre
│   ├── AnswerCard       # Cevap kartı
│   ├── QuestionInput    # Soru girişi
│   ├── HistoryPanel     # Geçmiş paneli
│   └── LanguageSwitcher # Dil değiştirici
├── utils/               # Yardımcı fonksiyonlar
│   ├── getRandomAnswer  # Rastgele cevap seçici
│   └── history          # Geçmiş yönetimi
├── i18n/                # Dil dosyaları
│   ├── en.json          # İngilizce
│   └── tr.json          # Türkçe
├── styles/              # CSS dosyaları
├── App.tsx              # Ana uygulama bileşeni
└── main.tsx             # Giriş noktası
Tarayıcı Desteği
Uygulama tamamen istemci tarafında çalışır. localStorage desteği olan modern tarayıcılarda sorunsuz çalışır.

25)📺 YouTube Channels Management API
YouTube kanallarının veritabanında listelenmesi, filtrelenmesi, sıralanması ve dosya formatlarında import/export işlemlerini sağlayan RESTful Web API.

🎯 Proje Özeti
Bir içerik yönetim ajansı, danışmanlık verdiği içerik üreticilerine ait YouTube kanallarının bilgilerini düzenli olarak dış kaynaklardan (Excel, CSV dosyaları) toplamakta ve bu verileri bir API üzerinden yönetmektedir. Bu API sayesinde ekip üyeleri filtreleme, sıralama ve veri sorgulama işlemlerini kolayca yapabilmektedir.

🛠 Teknoloji Stack
Teknoloji	Versiyon	Kullanım
.NET	8.0	Framework
C#	12.0	Programlama Dili
PostgreSQL	13+	Veritabanı
Entity Framework Core	8.0	ORM
FluentValidation	11+	Veri Doğrulama
CsvHelper	30+	CSV İşleme
ClosedXML	0.22+	Excel İşleme
Swagger/OpenAPI	6.4+	API Dokumentasyonu
📐 Mimari Yapı
Proje Clean Architecture prensiplerine göre 4 katmanlı yapıda düzenlenmiştir:

┌─────────────────────────────────────────┐
│  PRESENTATION LAYER (API)               │
│  Controllers, HTTP Endpoints            │
│  📍 src/API/Controllers/                │
└────────────────┬────────────────────────┘
                 │ (DTO)
                 ↓
┌─────────────────────────────────────────┐
│  APPLICATION LAYER                      │
│  Services, DTOs, Validators, Queries    │
│  📍 src/Application/                    │
└────────────────┬────────────────────────┘
                 │ (Entity)
                 ↓
┌─────────────────────────────────────────┐
│  DOMAIN LAYER                           │
│  Entities, Business Logic, Interfaces   │
│  📍 src/Domain/                         │
└────────────────┬────────────────────────┘
                 │ (DbContext)
                 ↓
┌─────────────────────────────────────────┐
│  INFRASTRUCTURE LAYER                   │
│  Database, Repositories, EF Core        │
│  📍 src/Infrastructure/                 │
└────────────────┬────────────────────────┘
                 │ (SQL)
                 ↓
┌─────────────────────────────────────────┐
│  DATABASE LAYER                         │
│  PostgreSQL                             │
└─────────────────────────────────────────┘
Katmanlar Arası İletişim
Presentation → Application: DTO aracılığıyla
Application → Domain: Entity ve Interface aracılığıyla
Domain → Infrastructure: Dependency Injection aracılığıyla
Infrastructure → Database: EF Core aracılığıyla
📁 Proje Klasör Yapısı
YouTubeChannels.Api/
├── src/
│   ├── API/
│   │   ├── Controllers/
│   │   │   └── ChannelsController.cs      # REST endpoint'leri
│   │   └── Middleware/
│   │       └── ErrorHandlingMiddleware.cs # Global hata yönetimi
│   │
│   ├── Application/
│   │   ├── DTOs/
│   │   │   ├── CreateChannelDto.cs       # Kanal oluşturma isteği
│   │   │   ├── GetChannelDto.cs          # Kanal döndürme modeli
│   │   │   ├── UpdateChannelDto.cs       # Kanal güncelleme isteği
│   │   │   └── ImportResultDto.cs        # Import sonucu
│   │   ├── Services/
│   │   │   ├── IChannelService.cs        # Servis kontratı
│   │   │   └── ChannelService.cs         # İş mantığı
│   │   ├── Validators/
│   │   │   └── CreateChannelValidator.cs # Veri doğrulama kuralları
│   │   └── Queries/
│   │       └── SearchChannelQuery.cs     # Arama parametreleri
│   │
│   ├── Domain/
│   │   ├── Entities/
│   │   │   └── Channel.cs                # Kanal entity
│   │   └── Interfaces/
│   │       └── IChannelRepository.cs     # Repository kontratı
│   │
│   └── Infrastructure/
│       ├── Data/
│       │   └── AppDbContext.cs           # EF Core DbContext
│       └── Repositories/
│           └── ChannelRepository.cs      # Veri erişim implementasyonu
│
├── Migrations/
│   ├── 20260310091644_InitialCreate.cs
│   ├── 20260310091644_InitialCreate.Designer.cs
│   └── AppDbContextModelSnapshot.cs
│
├── appsettings.json                       # Üretim ayarları
├── appsettings.Development.json           # Geliştirme ayarları
├── Program.cs                             # Uygulama başlangıcı
└── README.md
🗃 Veri Modeli
Channel Entity
public class Channel
{
    public int ChannelId { get; set; }           // Birincil Anahtar
    public string ChannelName { get; set; }      // Kanal Adı (required, max 255)
    public string Category { get; set; }         // Kategori (required)
    public long Subscribers { get; set; }        // Abone Sayısı
    public bool IsActive { get; set; }           // Aktif/Pasif Durumu
    public DateTime CreatedAt { get; set; }      // Oluşturulma Tarihi (otomatik)
}
🚀 Kurulum ve Çalıştırma
Ön Koşullar
.NET 8.0 SDK veya daha yeni sürüm
PostgreSQL 13 veya daha yeni sürüm
Visual Studio 2022 / VS Code / JetBrains Rider
Adım 1: Depo Klonlama
git clone https://github.com/muhammedesatdemir/kanal_listesi_yonetimi.git
cd kanal_listesi_yonetimi
Adım 2: Bağımlılıkları Yükleme
dotnet restore
Adım 3: Veritabanı Ayarları
appsettings.Development.json dosyasını düzenle:

{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=YouTubeChannelsDb;Username=postgres;Password=YourPassword"
  }
}
Adım 4: Veritabanı Oluşturma (Migration)
Visual Studio Package Manager Console:
Update-Database
Komut Satırı:
dotnet ef database update
Adım 5: Projeyi Çalıştırma
dotnet run
Çıkış:

info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:5074
      Now listening on: https://localhost:5074
Adım 6: Swagger UI'a Erişim
Tarayıcıda aç:

https://localhost:5074/swagger/index.html
📡 API Endpoint'leri
Endpoint Özeti
#	HTTP	Endpoint	Açıklama
1	GET	/api/channels/{id}	Kanal Detayı
2	GET	/api/channels	Tüm Kanalları Listele
3	GET	/api/channels/search	Arama, Filtreleme, Sıralama
4	POST	/api/channels	Kanal Oluştur
5	PUT	/api/channels/{id}	Kanal Güncelle
6	DELETE	/api/channels/{id}	Kanal Sil
7	POST	/api/channels/import/csv	CSV Dosya Yükleme (File Upload)
8	POST	/api/channels/import/folder/csv	CSV Klasör Taraması (Folder Import)
9	POST	/api/channels/import/excel	Excel Dosya Yükleme (File Upload)
10	POST	/api/channels/import/folder/excel	Excel Klasör Taraması (Folder Import)
11	GET	/api/channels/export/csv	CSV Dışa Aktarma
12	GET	/api/channels/export/excel	Excel Dışa Aktarma
Detaylı Endpoint Açıklamaları
1. Kanal Detayı
Detaylı Endpoint Açıklamaları
1. Kanal Detayı
Get channel by ID
GET /api/channels/{id}
Örnek:

GET /api/channels/1
Response (200 OK):

{
  "channelId": 1,
  "channelName": "Tech Learning Hub",
  "category": "Education",
  "subscribers": 150000,
  "isActive": true,
  "createdAt": "2025-03-10T09:30:00Z"
}
2. Listeleme
Tüm Kanalları Listele
GET /api/channels
Response (200 OK):

[
  {
    "channelId": 1,
    "channelName": "Tech Learning Hub",
    "category": "Education",
    "subscribers": 150000,
    "isActive": true,
    "createdAt": "2025-03-10T09:30:00Z"
  }
]
3. Arama, Filtreleme ve Sıralama
GET /api/channels/search?search={search}&category={category}&sortBy={sortBy}&order={order}&pageNumber={pageNumber}&pageSize={pageSize}
Query Parametreleri:

Parametre	Tür	Açıklama	Örnek
search	string	Kanal adında arama	search=tech
category	string	Kategori filtrelemesi	category=Education
minSubscribers	long	Minimum abone sayısı	minSubscribers=100000
maxSubscribers	long	Maksimum abone sayısı	maxSubscribers=500000
isActive	bool	Aktif kanal filtrelemesi	isActive=true
sortBy	string	Sıralama alanı (name, subscribers, date)	sortBy=subscribers
order	string	Sıralama yönü (asc, desc)	order=desc
pageNumber	int	Sayfa numarası (default: 1)	pageNumber=1
pageSize	int	Sayfa boyutu (default: 10)	pageSize=10
Örnek İstek:

GET /api/channels/search?search=tech&category=Education&sortBy=subscribers&order=desc&pageNumber=1&pageSize=5
Response (200 OK):

[
  {
    "channelId": 1,
    "channelName": "Tech Learning Hub",
    "category": "Education",
    "subscribers": 500000,
    "isActive": true,
    "createdAt": "2025-03-10T09:30:00Z"
  }
]
4. Oluşturma
POST /api/channels
Content-Type: application/json
Request Body:

{
  "channelName": "New Channel",
  "category": "Entertainment",
  "subscribers": 75000,
  "isActive": true
}
Response (201 Created):

1
Validation Kuralları:

channelName: Zorunlu, minimum 3 karakter, maksimum 255 karakter
category: Zorunlu
subscribers: Negatif olamaz
5. Güncelleme
PUT /api/channels/{id}
Content-Type: application/json
Örnek:

PUT /api/channels/1
Request Body:

{
  "channelName": "Updated Channel Name",
  "category": "Education",
  "isActive": false
}
Response (204 No Content):

(Yanıt Gövdesi Yok)
6. Silme
DELETE /api/channels/{id}
Örnek:

DELETE /api/channels/1
Response (204 No Content):

(Yanıt Gövdesi Yok)
7. CSV Import (File Upload)
POST /api/channels/import/csv
Content-Type: multipart/form-data
Form Data:

file: CSV dosyası
CSV Format:

ChannelName,Category,Subscribers,IsActive
Gaming Pro,Entertainment,500000,true
Code Masters,Education,250000,true
News Daily,News,1000000,true
Response (200 OK):

{
  "importedCount": 3,
  "failedCount": 0,
  "errors": null
}
8. CSV Import (Folder Based)
POST /api/channels/import/folder/csv
Query Parametreleri:

Parametre	Tür	Açıklama	Örnek
folderPath	string	CSV dosyalarının bulunduğu klasör yolu	folderPath=C:/uploads/csv
Örnek İstek:

POST /api/channels/import/folder/csv?folderPath=C:/uploads/csv
Response (200 OK):

{
  "importedCount": 15,
  "failedCount": 2,
  "errors": [
    "file1.csv - Row 5: ChannelName must be between 3 and 255 characters",
    "file2.csv - Row 3: Missing required fields (ChannelName or Category)"
  ],
  "filesProcessed": 3
}
Detaylar:

Belirtilen klasördeki tüm .csv dosyaları taranır
Her dosyadan veriler okunur ve veritabanına kaydedilir
Hata durumunda, hangi dosyada hata oluştuğu belirtilir
filesProcessed: İşlenen dosya sayısı
8. Excel Import from Folder
POST /api/channels/import/folder/excel
Query Parametreleri:

Parametre	Tür	Açıklama	Örnek
folderPath	string	Excel dosyalarının bulunduğu klasör yolu	folderPath=C:/uploads/excel
Örnek İstek:

POST /api/channels/import/folder/excel?folderPath=C:/uploads/excel
Response (200 OK):

{
  "importedCount": 25,
  "failedCount": 1,
  "errors": [
    "channels.xlsx - Row 10: Invalid subscriber count format"
  ],
  "filesProcessed": 2
}
Detaylar:

Belirtilen klasördeki tüm .xlsx dosyaları taranır
Her dosyanın ilk sayfasından veriler okunur
Tüm dosyaların verileri veritabanına kaydedilir
filesProcessed: İşlenen dosya sayısı
10. CSV Export
GET /api/channels/export/csv
Response (200 OK):

Dosya İndir: channels_2025-03-10_14-30-45.csv
Detaylar:

Veritabanındaki tüm kanallar CSV formatında dışa aktarılır
Dosya adı otomatik olarak oluşturulur (timestamp ile)
11. Excel Export
GET /api/channels/export/excel
Response (200 OK):

Dosya İndir: channels_2025-03-10_14-30-45.xlsx
Detaylar:

Veritabanındaki tüm kanallar Excel formatında dışa aktarılır
Header satırı kalın olarak formatlanır
Sütun genişlikleri otomatik ayarlanır
Dosya adı otomatik olarak oluşturulur (timestamp ile)
12. Excel Import (File Upload)
POST /api/channels/import/excel
Content-Type: multipart/form-data
Form Data:

file: Excel dosyası (.xlsx)
Excel Format:

ChannelId	ChannelName	Category	Subscribers	IsActive	CreatedAt
(boş)	Gaming Pro	Entertainment	500000	true	(boş)
(boş)	Code Masters	Education	250000	true	(boş)
Response (200 OK):

{
  "importedCount": 2,
  "failedCount": 0,
  "errors": null
}
⚙️ Konfigürasyon
appsettings.Development.json
{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=YouTubeChannelsDb;Username=postgres;Password=YourPassword"
  }
}
Program.cs Konfigürasyonu
// Veritabanı
builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseNpgsql(connectionString));

// Servisler
builder.Services.AddScoped<IChannelRepository, ChannelRepository>();
builder.Services.AddScoped<IChannelService, ChannelService>();

// Validasyon
builder.Services.AddValidatorsFromAssemblyContaining<CreateChannelValidator>();

// API
builder.Services.AddControllers();
builder.Services.AddSwaggerGen();

// Middleware
app.UseMiddleware<ErrorHandlingMiddleware>();
🔍 Error Handling
Tüm hata yanıtları şu formatta döner:

{
  "error": "Hata açıklaması",
  "details": "Detaylı hata mesajı"
}
HTTP Status Codes
Code	Açıklama
200 OK	İstek başarılı
201 Created	Kaynak oluşturuldu
204 No Content	İşlem başarılı, yanıt yok
400 Bad Request	Geçersiz istek (validation hatası)
404 Not Found	Kaynak bulunamadı
500 Internal Server Error	Sunucu hatası
📊 Tasarım Kararları
1. Clean Architecture
Katmanlar arasında açık sorumluluğun ayrılması
Testlenebilirlik ve bakım kolaylığı
2. CQRS Light Pattern
Query (okuma) ve Command (yazma) ayrımı
SearchChannelQuery ile filtreleme yapılandırılması
3. Entity Framework Core
Code-First approach
Migrations ile database versiyonlama
4. Dependency Injection
Loose coupling
Interface tabanlı tasarım
5. FluentValidation
Business rule validasyonu
Kuralları merkezileştirilmiş tutma
6. Bulk File Import
Folder-based import ile birden fazla dosyayı aynı anda işleme
Detaylı hata raporlama (hangi dosya, hangi satırda hata)
İşlenen dosya sayısının takibi
📥 Veri İhraç Yöntemleri Özeti
Yöntem	Endpoint	Açıklama	Avantaj
File Upload - CSV	POST /api/channels/import/csv	Tek CSV dosyası yükleme	Hızlı, basit
File Upload - Excel	POST /api/channels/import/excel	Tek Excel dosyası yükleme	Hızlı, formatlı
Folder - CSV	POST /api/channels/import/folder/csv?folderPath=...	Klasördeki tüm CSV dosyalarını işle	Toplu veri giriş, otomasyon
Folder - Excel	POST /api/channels/import/folder/excel?folderPath=...	Klasördeki tüm Excel dosyalarını işle	Toplu veri giriş, otomasyon
Dosya Yolu Kullanım Örnekleri
Windows:

folderPath=C:\uploads\csv
folderPath=D:\data\channels
Linux/Mac:

folderPath=/home/user/uploads/csv
folderPath=/var/data/channels
🧪 Test Önerileri
Manual Test (Swagger UI)
https://localhost:5074/swagger aç
Tüm endpoint'leri test et
CSV/Excel dosyaları upload et
Export işlemlerini kontrol et
Test Senaryoları
Scenario 1: Kanal Oluştur

POST /api/channels ile kanal oluştur
GET /api/channels ile listelede kontrol et
Scenario 2: Arama ve Filtreleme

GET /api/channels/search?search=tech&sortBy=subscribers ile sorgula
Sonuçların doğruluğunu kontrol et
Scenario 3: File Upload CSV/Excel Import/Export

POST /api/channels/import/csv ile CSV dosyası yükle
POST /api/channels/import/excel ile Excel dosyası yükle
GET /api/channels/export/csv ile CSV olarak dışa aktar
GET /api/channels/export/excel ile Excel olarak dışa aktar
İçeriği karşılaştır
Scenario 4: Folder Based Import (CSV)

C:\uploads\csv klasöründe birden fazla CSV dosyası hazırla
POST /api/channels/import/folder/csv?folderPath=C:\uploads\csv çalıştır
Tüm dosyaların içeriğinin veritabanına kaydedildiğini kontrol et
İçeri hatalarını gözlemle
Scenario 5: Folder Based Import (Excel)

C:\uploads\excel klasöründe birden fazla Excel dosyası hazırla
POST /api/channels/import/folder/excel?folderPath=C:\uploads\excel çalıştır
Tüm dosyaların içeriğinin veritabanına kaydedildiğini kontrol et
filesProcessed sonucunu doğrula
📚 Kaynaklar
Dokumentasyon
Microsoft .NET Documentation
Entity Framework Core
Clean Architecture
CQRS Pattern
Kütüphaneler
FluentValidation
CsvHelper
ClosedXML
📝 Lisans
Bu proje eğitim amaçlı oluşturulmuştur.

👤 Geliştirici
Muhammed Esat Demir
GitHub: @muhammedesatdemir

📞 İletişim ve Destek
Sorular veya geri bildirimler için GitHub Issues üzerinden iletişime geçebilirsiniz.

Son Güncelleme: 10 Mart 2025
Proje Durumu: ✅ %100 Tamamlandı

✅ Tamamlanan Görevler
Görev	Durum
Listeleme & Arama & Filtreleme & Sıralama	✅
CRUD Operasyonları	✅
CSV File Upload Import	✅
Excel File Upload Import	✅
CSV Export	✅
Excel Export	✅
CSV Folder Import	✅
Excel Folder Import	✅
Swagger UI Dokumentasyonu	✅
Error Handling & Validation	✅
Clean Architecture	✅
GitHub Repository	✅
Detaylı Dokümantasyon	✅

26) Script-to-Sub
Turkce video icerikleri icin iki modlu altyazi uretim sistemi. Elinizde hazir script varken referansli hizalama yapar (Mod A), referans metin yoksa Whisper + Gemini Flash ile sifirdan uretir (Mod B). Her iki modun ciktisi ayni modern ASS stiline (Montserrat Black, sari vurgu, 9:16 safe-zone) yonlendirilir ve FFmpeg uzerinden videoya gomulerek Reels/TikTok/Shorts icin hazir .mp4 olarak indirilir.

Mod A — Script-Aware Alignment: Whisper sadece zamanlama kaynagi. Goruntulenen metin her zaman kullanicinin yazdigi script'tir. TDK/ozel isim sadakati maksimum.

Mod B — Autonomous AI Transcription: Kullanici metin girmez. Whisper transkribe eder, Gemini Flash fonetik hatalari / TDK / sayi formatlarini duzeltir, kelime timestamp'leri ile yeniden hizalanir.

One Cikan Ozellikler
Iki calisma modu — Referansli (Mod A) veya bagimsiz AI (Mod B); UI uzerinden tek tiklamayla gecis.
Demucs v4 vokal izolasyonu — htdemucs ile beat/muzik ayristirilir, Whisper temiz vokal uzerinde calisir. CUDA varsa otomatik GPU.
faster-whisper large-v3-turbo — Kelime seviyesinde word_timestamps=True, stable-ts ile stabilizasyon.
Gemini Flash refinement (Mod B) — Ozel isim fonetigi, TDK noktalama, sayi formatlama. Uc anahtar + uc model fallback matrisi (3×3 = 9 kombinasyon).
Akilli hata sinfilandirmasi — 429 (quota) vs 503 (overload) vs kalici (auth) ayri politikalarla ele alinir; quota'da hemen model ilerletir, overload'da kisa backoff, auth hatasinda sonraki key'e atlar.
Mod A hizalama motoru — difflib + rapidfuzz fuzzy matching, Turkce stem cikarma, genisletilebilir fonetik sozluk (Coutinho, Ziyech, Icardi, Karagumruk vb.), zamansal tutarlilik filtresi, en yogun ardisik blok secimi, bosluk interpolasyonu.
2-satir kurali — Mod B kelime akisi orantili olarak bolunur: 42 kar / 8 kelime siniri asildiginda sure kelime sayisina gore dagitilarak ikiye ayrilir (recursive). Reels/Shorts'ta 3-satir tasma imkansiz.
Modern ASS render — Montserrat Black 82pt, safe-zone MarginV 410px (1080×1920), ozel isim ve sayilar otomatik sari vurgu (_highlight_words), libass ile piksel-mukemmel.
Hardcoded subtitle — FFmpeg + libass ile tek parca altyazili .mp4; oynatici destegi gerektirmez.
Modern Streamlit arayuzu — Koyu tema, WCAG AA kontrasti, UTF-8 full Turkce karakter destegi, placeholder gorunurlugu, klavye erisilebilirligi (focus-visible), semantik durum renkleri.
Mimari
                     ┌────────────── Mod A (Script-Aware) ──────────────┐
                     │                                                    │
[Video] ──► Demucs ──┤──► faster-whisper ──► enhanced_align_words ──► create_segments
            (htdemucs)     (large-v3-turbo)   (difflib + rapidfuzz +     │    │
                           word_timestamps     Turkce stem + fonetik)    │    ▼
                           = all_words)                                  │  optimize
                     │                                                    │    │
                     └───────── Mod B (Autonomous AI) ─────────┐          │    │
                                                                │         │    │
                                ┌──► full_text ──► Gemini Flash │         │    │
                                │                   refinement  │         │    │
                                │                  (3 key × 3 model)      │    │
                                │                       │                 │    │
                                └─► Whisper all_words ──┴──► enhanced_align_words
                                                              (refined text'i
                                                               script gibi kullanir)
                                                                │         │    │
                                                                ▼         │    ▼
                                                         create_segments ─┘  split_segment
                                                                               (2-satir)
                                                                                  │
                                                                                  ▼
                                                                          rendering/srt
                                                                                  │
                                                                    ┌─────────────┤
                                                                    ▼             ▼
                                                              [output.srt]   rendering/ass
                                                                             (Montserrat,
                                                                              sari vurgu,
                                                                              safe-zone)
                                                                                  │
                                                                                  ▼
                                                                            FFmpeg + libass
                                                                                  │
                                                                                  ▼
                                                                    [output_altyazili.mp4]
Paket Yapisi
altyazi/
├── app.py                       # Thin Streamlit entry (~140 satir)
├── fonts/                       # Montserrat Black .ttf
├── core/
│   ├── config.py                # ASS stili, safe-zone, limitler, Gemini model listesi
│   ├── utils.py                 # Zaman ceviri, Turkce stem, normalize, safe_remove
│   └── phonetic.py              # DEFAULT_PHONETIC_DICT, reverse map builder
├── audio/
│   ├── extract.py               # ffmpeg 16kHz mono WAV
│   └── isolate.py               # Demucs htdemucs (CUDA auto-detect, graceful fallback)
├── transcription/
│   └── whisper_stt.py           # faster-whisper + stable-ts wrapper
├── alignment/
│   ├── tokenize.py              # Script → token listesi
│   └── aligner.py               # enhanced_align_words: fonetik + stem + blok + interpolation
├── refinement/
│   └── gemini.py                # Gemini Flash full-text refinement, multi-key/model fallback
├── segmentation/
│   └── segments.py              # create_segments, optimize_segments, split_segment
├── rendering/
│   ├── srt.py                   # SRT uretimi
│   └── ass.py                   # ASS render, _highlight_words, _split_long_cue, burn_subtitles
├── pipeline/
│   ├── mode_a.py                # run_script_aware_alignment
│   └── mode_b.py                # run_autonomous_transcription
└── ui/
    ├── theme.py                 # CSS + sidebar toggle enjektoru
    ├── sidebar.py               # Mod secimi + Whisper + Demucs + Gemini ayarlari
    ├── inputs.py                # Video + script/context girdileri
    └── results.py               # Metrikler, indirme, segment onizleme, burn
Toplam ~2,500 satir Python, semantik olarak ayrilmis 9 alt modul. Tum yapilandirilabilir sabitler altyazi/core/config.py icinde tek yerde toplandi.

Teknoloji Yigini
Katman	Arac	Amaç
Ses on-isleme	ffmpeg, demucs (htdemucs v4)	Ses cikarma, vokal izolasyonu
STT	faster-whisper + stable-ts (large-v3-turbo)	Konusma tanima, kelime timestamp stabilizasyonu
Mod B refinement	google-genai (Gemini 2.5 Flash / Flash Lite)	Fonetik, TDK, sayi normalize
Hizalama	difflib.SequenceMatcher, rapidfuzz	Script-Whisper eslestirme
Turkce NLP	Ozel stemmer + fonetik sozluk	Eklemeli yapi, ozel isim sadakati
Render	libass (FFmpeg filter)	ASS stilizasyonu, hardcoded subtitle
Arayuz	streamlit	Web tabanli yukleme / onizleme / indirme
Kurulum
git clone https://github.com/muhammedesatdemir/ScriptToSub-AI.git
cd ScriptToSub-AI
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
FFmpeg gereklidir. Windows: winget install Gyan.FFmpeg veya choco install ffmpeg. macOS: brew install ffmpeg.

Gemini API anahtarlari (sadece Mod B icin)
.streamlit/secrets.toml olustur:

GEMINI_API_KEYS = [
    "AIza...birinci",
    "AIza...ikinci",
    "AIza...ucuncu",
]
Listede birden fazla key olabilir; biri quota / 503 yerse siradaki otomatik devreye girer. Ornek sablon: .streamlit/secrets.toml.example.

API key: Google AI Studio.

Calistirma
streamlit run altyazi/app.py
Sonra tarayicida http://localhost:8501.

Mod A kullanimi (referansli)
Sidebar'dan "A — Script-Aware (Referansli)".
Video + script metnini (TXT dosyasi veya manuel) yukle.
Istege bagli: Beat Separation (Demucs) ac (muzik/beat varsa onerilir).
Altyaziyi Olustur → SRT hazir. Altyaziyi Videoya Gom → .mp4 indir.
Mod B kullanimi (referanssiz AI)
Sidebar'dan "B — Autonomous AI (Metinsiz)".
Video yukle; script alani kaybolur, yerine Video Baglami (opsiyonel) gelir — orn. "Futbol analizi", "Teknoloji incelemesi".
Gemini 3 Flash ile duzelt isareti acik olsun (default).
Altyaziyi Olustur.
Gemini Fallback Matrisi
Mod B'de refinement cagrisi uc katmanli fallback kullanir:

for key in GEMINI_API_KEYS:           # 3 anahtar
    for model in GEMINI_MODELS:       # 3 model
        for attempt in (1, 2):        # 503 icin ic retry (2s backoff)
            try: return response
            except Quota (429):       → siradaki modele
            except Overload (503):    → 2s backoff, sonra siradaki modele
            except Auth (401/403):    → siradaki key'e (bu key tamamen bypass)
            except Timeout:           → siradaki modele
Varsayilan model listesi (Nisan 2026):

GEMINI_MODELS = (
    "gemini-2.5-flash-lite",   # en hizli, free-tier bol
    "gemini-2.5-flash",        # ikinci aday
    "gemini-2.0-flash",        # gecici fallback (2026-06-01'de retire)
)
Hepsi dusunce pipeline ham Whisper ciktisina gracefully duser — altyazi yine uretilir, sadece AI refinement olmadan.

Yapilandirma
Tum sabitler altyazi/core/config.py:

Sabit	Varsayilan	Aciklama
DEFAULT_WHISPER_MODEL	large-v3-turbo	Whisper model boyutu
WHISPER_LANGUAGE	tr	Dil kodu
GEMINI_MODELS	2.5-lite, 2.5, 2.0	Fallback sirali tuple
SUBTITLE_FONT_NAME	Montserrat Black	ASS font adi
SUB_PLAY_RES_X/Y	1080 × 1920	9:16 referans cozunurlugu
SUB_MARGIN_V	410	Alt safe-zone (px, PlayResY tabanli)
SUB_FONT_SIZE	82	ASS font boyu
SUB_MAX_CHARS_PER_CUE	42	Bir cue'da maks karakter
SUB_MAX_WORDS_PER_CUE	8	Bir cue'da maks kelime
TR_STOPWORDS	{Ve, Bu, Ben, ...}	_highlight_words icin istisna listesi
Render Ornegi — _highlight_words
>>> _highlight_words("Icardi 2026 yilinda ve bir gol atti")
'{\\c&H00FFFF&}Icardi{\\c&HFFFFFF&} {\\c&H00FFFF&}2026{\\c&HFFFFFF&} yilinda ve bir gol atti'
Icardi ve 2026 sariya boyanir; ve, bir stopword oldugu icin beyaz kalir.

Proje Felsefesi
Modular — Her modul tek is yapar, public API'si __init__.py'de net.
Fonksiyonel — Ic state tutulmaz, pipeline saf fonksiyonlardan olusur.
Graceful degradation — Demucs yoksa duz ses, Gemini dusurse ham Whisper, bir fallback hep ayakta.
Tek kaynak yapilandirma — Tum sabitler core/config.py; stil/limit degistirmek icin tek dosya.
WCAG-aware UI — Koyu tema AA kontrasti, klavye focus-visible, semantik renkler.
Yol Haritasi
 Ozel ASS stil onayarlari (preset'ler: Reels, Shorts, 16:9)
 Batch islem (bir klasordeki tum videolar icin CLI)
 Whisper model onbellegi (aynı video icin cache)
 Web-based phonetic dict editoru (Streamlit sidebar)
 Faster-whisper word confidence skoruyla interpolasyon kalite filtresi
Lisans
MIT. Ayrintilar icin LICENSE dosyasina bakin.

bu projeyi geliştirmeye devam ediyorum, patlayan yerleri olabiliyor(yapay zeka destekli kısmı, yoksa mod a başarılı şekilde çalışıyor)

28) # İçerik Fabrikası — Video Üretim (Resim Pipeline)

## 🇬🇧 English

Bu dökümanın İngilizce versiyonu için [README.md](README.md)

> Not: Bu doküman İngilizce versiyondan türetilmiştir ve zaman zaman geride kalabilir.

## Genel Bakış

İçerik Fabrikası, bir Türk dram dizisi için 9:16 dikey kısa-form video
bölümlerinin üretimini otomatikleştiren yerel bir Streamlit web
uygulamasıdır. Gemini tabanlı bir yazar, Together AI görsel üreteci,
InsightFace yüz-değişim adımı, HTML/CSS konuşma balonu kompozitörü ve
MoviePy video birleştiricisini 4 sekmeli bir arayüzün arkasında zincirler.

Pipeline şu şekilde ilerler:

1. **Story Bible** — Türkçe bir dizi konseptinden Gemini bir macro plan
   üretir (varsayılan 10 sezon × 10 bölüm = 100 bölüm), ayrıca bir karakter
   ve mekan bible'ı hazırlar.
2. **Screenwriter** — seçilen bölüm premise'i, sahne başına
   `visual_prompt`, framing, karakter aksiyonları ve Türkçe diyalog içeren
   render-ready bir YAML senaryosuna (varsayılan 10 sahne) dönüştürülür.
3. **Render Engine** — her sahne Together AI (FLUX.1 Schnell) ile render
   edilir, opsiyonel olarak kanonik bir referansa yüz değişimi yapılır,
   1080×1920'ye sığdırılır ve HTML konuşma balonu ile kompozit edilir.
4. **Video Assembler** — nihai kareler MoviePy ile crossfade geçişleri
   kullanılarak bir MP4'e birleştirilir.

Proje çoklu hikâyeyi destekler; her biri [story_bible/](story_bible/)
altında kendi workspace'inde tutulur.

## Özellikler

- 4 sekmeli Streamlit arayüzü: Story Bible, Screenwriter, Render Engine,
  Video Assembler ([app.py](app.py)).
- `GEMINI_API_KEY_1..3` arasında en fazla 3-anahtarlı rotasyon ve
  retry/backoff ile Gemini 2.5 Flash metin üretimi
  ([src/screenwriter.py](src/screenwriter.py),
  [src/story_bible.py](src/story_bible.py),
  [src/character_bible_generator.py](src/character_bible_generator.py)).
- Yapılandırılabilir sezon ve sezon başına bölüm sayısı üzerinden macro
  plan üretimi (arayüz varsayılanları: 1–20 sezon, sezon başına 5–20
  bölüm).
- Forensic `identity_lock`, `wardrobe_default`, `state_timeline`,
  `aliases` ve `bubble_style` içeren karakter + mekan bible üretimi
  (`characters.yaml` + `locations.yaml`)
  ([src/character_bible_generator.py](src/character_bible_generator.py),
  [src/story_bible.py](src/story_bible.py)).
- Deterministik varsayılanlar ve doğrulama ile bölüm başına render-ready
  senaryo YAML'ı: lokasyon fallback eşlemesi, yasaklı framing'lerin
  yeniden yazılması, `text_safe_zone`'un `top` olarak zorlanması, alias →
  display-name yeniden eşlemesi, time-of-day süreklilik kontrolü
  ([src/screenwriter.py](src/screenwriter.py)).
- 768×1344, 4 adımda, retry ve rastgele seed ile Together AI FLUX.1
  Schnell görsel üretimi
  ([src/image_generator.py](src/image_generator.py)).
- Referans başına kaynak embedding önbelleği ile CPU üzerinde opsiyonel
  InsightFace (`buffalo_l` + `inswapper_128.onnx`) yüz değişimi
  ([src/face_swap.py](src/face_swap.py)).
- Playwright (headless Chromium) ile HTML/CSS konuşma balonu render'ı ve
  konuşmacı başına CSS sınıfı olan Pillow overlay
  ([src/compositor.py](src/compositor.py),
  [assets/balloons/templates/default.html](assets/balloons/templates/default.html)).
- Render edilen ham kareleri, nihai kareleri, konuşmacıyı, metni ve sahne
  süresini listeleyen bölüm başına `manifest.json`
  ([src/episode_manager.py](src/episode_manager.py)).
- Yapılandırılabilir FPS / sahne süresi / crossfade, sessiz çıktı ve
  libx264 codec ile MoviePy 1.0.3 üzerinden MP4 birleştirme
  ([src/video_generator.py](src/video_generator.py)).
- Türkçe-duyarlı slugify, `index.yaml` kaydı ve atomik YAML yazımı ile
  çoklu-hikâye workspace düzeni
  ([src/story_workspace.py](src/story_workspace.py)).
- Tek seferlik legacy migration: eğer eski root-level
  `story_bible/characters.yaml`, `story_bible/macro_plan.yaml` ve
  `story_bible/episodes/` mevcutsa, başlangıçta otomatik olarak yeni bir
  slug dizinine taşınırlar
  ([src/story_workspace.py](src/story_workspace.py)).

## Proje Yapısı

```
video_uretim_resim/
├── app.py                          # Streamlit giriş noktası (4 sekme)
├── config.yaml                     # Proje düzeyi varsayılanlar (video, image, compositor, llm)
├── requirements.txt                # Python bağımlılıkları
├── .env.example                    # Beklenen ortam değişkenleri
├── komut.txt                       # Başlatma komutu hatırlatması: `streamlit run app.py`
│
├── src/
│   ├── story_bible.py              # Tipli bible loader, prompt resolver, MacroPlanGenerator
│   ├── character_bible_generator.py# characters.yaml + locations.yaml için Gemini üreteci
│   ├── screenwriter.py             # Bölüm YAML yazıcısı (Gemini, key rotation, doğrulama)
│   ├── image_generator.py          # Together AI FLUX.1 Schnell backend + portrait fit
│   ├── face_swap.py                # InsightFace inswapper_128 wrapper (CPU)
│   ├── compositor.py               # HTML bubble render (Playwright) + Pillow overlay
│   ├── episode_manager.py          # Sahne sahne render orkestrasyonu + manifest.json
│   ├── video_generator.py          # Crossfade ile MoviePy MP4 birleştiricisi
│   └── story_workspace.py          # Çoklu-hikâye path'leri, slugify, index.yaml, legacy migrate
│
├── story_bible/
│   ├── index.yaml                  # Tüm hikâyelerin kaydı
│   └── <slug>/                     # Hikâye başına bir klasör
│       ├── story_meta.yaml
│       ├── macro_plan.yaml
│       ├── characters.yaml
│       ├── locations.yaml          # opsiyonel
│       └── episodes/
│           └── episode_NNN.yaml
│
├── assets/
│   └── balloons/
│       └── templates/
│           └── default.html        # {{PLACEHOLDERS}} içeren balon HTML şablonu
│
├── ornek_gorseller/                # Referans yüz portreleri + örnek sahne görselleri
├── models/
│   └── insightface/                # buffalo_l (otomatik) + inswapper_128.onnx (manuel)
├── output/                         # Render sonuçları (runtime'da oluşturulur)
│   └── episode_NNN/
│       ├── raw/                    # Balon öncesi kareler
│       ├── frames/                 # Balon sonrası nihai kareler
│       ├── manifest.json
│       └── final_episode_NNN.mp4
├── new/story_bible/                # Ayrı bir dizi için Markdown tasarım notları
│                                   # (pipeline tarafından tüketilmez)
└── venv/                           # Yerel virtual environment (ignore edilir)
```

## Kullanılan Teknolojiler

[requirements.txt](requirements.txt) dosyasından:

- **Streamlit** ≥ 1.30 — web arayüzü
- **Pillow** ≥ 10 — görsel I/O ve kompozit
- **Playwright** ≥ 1.40 — HTML balon render'ı için headless Chromium
- **moviepy** == 1.0.3 — MP4 birleştirme
- **PyYAML** ≥ 6 — story bible / senaryo I/O
- **tqdm** ≥ 4.66
- **requests** ≥ 2.31 — Gemini ve Together AI HTTP çağrıları
- **python-dotenv** ≥ 1 — `.env` yükleme
- **numpy** ≥ 1.24
- **insightface** ≥ 0.7.3, **onnxruntime** ≥ 1.17, **opencv-python** ≥ 4.9 —
  yüz tespiti ve değişimi

Dış servisler:

- Tüm metin üretimi (macro plan, karakter bible, screenwriter) için
  **Google Gemini 2.5 Flash** (`generativelanguage.googleapis.com`).
- Görsel üretimi için **Together AI**
  `black-forest-labs/FLUX.1-schnell-Free`.

## Kurulum

1. Repo'yu klonlayın ve içine `cd` yapın.
2. Bir Python virtual environment oluşturup etkinleştirin.
3. Bağımlılıkları yükleyin:
   ```bash
   pip install -r requirements.txt
   ```
4. Playwright Chromium tarayıcısını yükleyin (compositor tarafından
   gereklidir):
   ```bash
   playwright install chromium
   ```
5. `.env.example` dosyasını `.env` olarak kopyalayın ve anahtarları
   doldurun:
   ```
   GEMINI_API_KEY_1=...
   GEMINI_API_KEY_2=...   # opsiyonel (3 anahtara kadar rotasyon yapılır)
   GEMINI_API_KEY_3=...   # opsiyonel
   TOGETHER_API_KEY=...
   ```
   En az bir Gemini anahtarı gereklidir. `TOGETHER_API_KEY`, Render Engine
   sekmesi için gereklidir.
6. (Opsiyonel, yalnızca Face Swap etkinse) `inswapper_128.onnx` dosyasını
   `models/insightface/` altına manuel olarak indirin. `buffalo_l`
   detector paketi ilk kullanımda otomatik olarak indirilir. İndirme
   URL'leri ve beklenen düzen için
   [src/face_swap.py](src/face_swap.py) dosyasındaki docstring'e bakın.

## Kullanım

Streamlit uygulamasını başlatın:

```bash
streamlit run app.py
```

(Aynı komut [komut.txt](komut.txt) dosyasında da tutulur.)

Arayüzdeki tipik akış:

1. **Story Bible sekmesi** — bir Türkçe dizi konsepti girin, sezon
   sayısını ve sezon başına bölüm sayısını seçin, "Macro Plan Üret"
   tuşuna basın. Bu, `story_bible/<slug>/` altında `macro_plan.yaml`,
   `characters.yaml`, `locations.yaml` ve `story_meta.yaml` içeren yeni
   bir hikâye dizini oluşturur ve bunu `story_bible/index.yaml`'a
   kaydeder.
2. **Screenwriter sekmesi** — aktif hikâyeyi seçin, macro plan'dan bir
   bölüm seçin (veya premise'i elle düzenleyin), sahne sayısını seçin
   (5–15, varsayılan 10) ve "Senaryo Üret" tuşuna basın. Bölüm YAML'ı
   atomik olarak `story_bible/<slug>/episodes/episode_NNN.yaml` dosyasına
   yazılır.
3. **Render Engine sekmesi** — bir bölüm YAML'ı seçin, opsiyonel olarak
   FaceSwap'ı açıp kapatın ve "Render Başlat" tuşuna basın. Kareler
   `output/episode_NNN/raw/` ve `output/episode_NNN/frames/` altına
   yazılır, sonunda da bir `manifest.json` yazılır.
4. **Video Assembler sekmesi** — render edilmiş bir bölüm seçin, FPS /
   sahne süresi / crossfade değerlerini ayarlayın, "Video Üret" tuşuna
   basın. MP4 `output/episode_NNN/final_episode_NNN.mp4` yoluna yazılır
   ve satır içi bir oynatıcıda gösterilir.

## Yapılandırma

- **[.env](.env.example)** — API anahtarları (Gemini x3, Together AI).
  Hem `app.py` hem de üretici modüller tarafından otomatik olarak
  yüklenir.
- **[config.yaml](config.yaml)** — video (1080×1920, 30 fps, 4.5
  sn/sahne, 0.4 sn crossfade, libx264, sessiz), görsel (1080×1920 PNG),
  görsel üretimi (Together AI, FLUX.1 Schnell, 768×1344, 4 adım),
  compositor (Playwright renderer, varsayılan balon şablonu, Noto Sans
  TR, 42 px, max genişlik 820 px, padding 32 px), path'ler ve LLM
  (Gemini 2.5 Flash, üç key env adı) için proje varsayılanlarını
  tanımlar.
  Not: bu değerlerin birkaçı aynı zamanda ilgili modüllerde
  (`GenerationConfig`, `VideoConfig`, `BubbleStyle`,
  `ScreenwriterConfig`) dataclass varsayılanları olarak da sabit
  kodlanmıştır. Streamlit arayüzü bazı ayarları `config.yaml`'dan değil
  kullanıcı girdisinden okur.
- **Hikâye başına YAML dosyaları** — `characters.yaml` (`meta`,
  `characters`, `composition` ile), `locations.yaml`, `macro_plan.yaml`,
  `story_meta.yaml`.

## Mimari / Ana Bileşenler

### Story Bible (`src/story_bible.py`)
Tipli dataclass'ları (`StoryBible`, `Character`, `Location`,
`Composition`, `StateBlock`) ve `load_story_bible`'ı tanımlar;
load_story_bible hem single-file layout'u (legacy) hem de split
`characters.yaml` + `locations.yaml` layout'unu destekler.
`build_scene_prompt`, karakter kimliği, gardırop (bölüm başına
`state_timeline` üzerinden çözülür), lokasyon kimliği+mood'u, composition
varsayılanları ve global style/negative string'lerinden deterministik
şekilde bir positive/negative prompt oluşturur. `MacroPlanGenerator`,
N-sezonluk planı strict JSON olarak üretmek için Gemini'yi çağırır.

### Character Bible Generator (`src/character_bible_generator.py`)
Sabit kodlanmış bir casting-director system prompt'u ile Gemini'yi
çağırır ve `write_bible_files` ile `characters.yaml` + `locations.yaml`
dosyalarını yazar (atomik tmp → `os.replace`). 3-anahtarlı rotasyon +
retry patternini screenwriter ile paylaşır.

### Screenwriter (`src/screenwriter.py`)
`Screenwriter.write_episode`, karakter ve lokasyon bloklarını enjekte
eden, isim disiplinini zorunlu kılan (alias'lar `display_name`'e yeniden
eşlenir), izin verilmeyen framing'leri yasaklayan ve çıktı JSON şemasını
zorunlu kılan büyük bir system prompt oluşturur. Yanıtı aldıktan sonra
yasaklı anahtarları siler, lokasyon fallback'i (`LOCATION_FALLBACK`
tablosu) uygular, eksik alanları varsayılanlarla doldurur, karakterleri
dict formuna normalize eder ve sahneleri doğrular (bilinmeyen anahtarlar,
time-of-day sürekliliği). Nihai YAML atomik olarak yazılır.

### Image Generator (`src/image_generator.py`)
`ImageGenerator`, retry/backoff ile tek bir `_TogetherBackend`'i (FLUX.1
Schnell) sarmalar, bible'dan `build_scene_prompt`'u çağırır, opsiyonel
olarak karakterin `canonical_face`'ine karşı `FaceSwapper.swap`'ı
çalıştırır ve sonucu LANCZOS resize + center crop ile 1080×1920 portrait
boyutuna sığdırır.

### Face Swap (`src/face_swap.py`)
InsightFace `FaceAnalysis(name="buffalo_l", ...)` ve
`inswapper_128.onnx`'i CPU üzerinde lazy olarak yükler. Referans yüz
embedding'lerini resolve edilmiş path başına önbelleğe alır. Varsayılan
olarak yalnızca en büyük tespit edilen yüzü değiştirir; üretilen karede
hiç yüz tespit edilmediğinde sessizce geri düşer.

### Compositor (`src/compositor.py`)
`render_bubble_png`, bir HTML şablonundaki yer tutucuları doldurur ve
Playwright ile 2× device scale'de `.wrap` elementini ekran görüntüsü
olarak alır; bu şeffaf bir PNG üretir. `overlay_bubble`, balonu base
görsel üzerine Pillow ile yapıştırır ve bir PNG yazar. `compose_frame`
tek atışlık yardımcıdır.

### Episode Manager (`src/episode_manager.py`)
Bölüm YAML'ındaki sahneleri iter eder. Her sahne için ilk karakteri
`main_char` olarak alır, aksiyon olarak `characters[0].doing`'i (veya
legacy `action`'ı) kullanır, `visual_prompt`'u `extra` olarak iletir,
`ImageGenerator` ile render eder, ardından (diyalog varsa) bir balon
render edip kompozit eder. Her sahnede balon kuyruk yönünü değiştirir
(tek → sol, çift → sağ). Tüm sahneleri özetleyen bir `manifest.json`
yazar. Streamlit arayüzü için bir progress callback kabul eder.

### Video Generator (`src/video_generator.py`)
`render_episode_video`, bir `EpisodeResult` ya da bir `manifest.json`
yolu kabul eder. Staggered `set_start` ile `ImageClip` klipleri oluşturur,
ilkinden sonraki kliplere `crossfadein` ekler, `CompositeVideoClip` ile
kompozit eder ve MoviePy üzerinden bir MP4 yazar (libx264, ses yok,
6000k bitrate, `preset="medium"`, 4 thread).

### Story Workspace (`src/story_workspace.py`)
`StoryWorkspace.for_slug(slug)`, bir hikâyenin tüm path'lerini açığa
çıkarır. `slugify`, Türkçe karakterleri işler (`ç→c`, `ğ→g`, `ı→i`,
`ö→o`, `ş→s`, `ü→u`). `list_stories` / `register_story`,
`story_bible/index.yaml`'ı okur ve yazar. `migrate_legacy`, başlangıçta
eski root-level dosyaları yeni bir slug dizinine taşır.

## Kısıtlamalar / Mevcut Durum

- Uygulama kesinlikle **yerel bir Streamlit arayüzüdür**; CLI giriş
  noktası ve HTTP API'si yoktur. Tüm orkestrasyon [app.py](app.py)
  içinde gerçekleşir.
- **Ses devre dışıdır.** `VideoConfig.audio` varsayılan olarak `False`
  ve [src/video_generator.py](src/video_generator.py) dosyasındaki
  docstring açıkça "Silent for now — audio comes in the next phase."
  ifadesini kullanır. Konuşma sentezi, müzik veya üretilen MP4'te ses
  kanalı yoktur.
- **Tek görsel backend'i.** Yalnızca Together AI FLUX.1 Schnell
  implemente edilmiştir. [src/image_generator.py](src/image_generator.py)
  içindeki yorum nettir: "Single backend — no fallback chain, no
  Pollinations, no Gemini image." Eğer `TOGETHER_API_KEY` eksikse,
  Render Engine butonu devre dışı bırakılır.
- **FaceSwap manuel kurulum gerektirir.** `inswapper_128.onnx` otomatik
  indirilmez; kullanıcı bunu
  `models/insightface/inswapper_128.onnx` yoluna yerleştirmelidir.
  CPU'da (`CPUExecutionProvider`) çalışır. Karakterin `canonical_face`'i
  yoksa veya yüz tespit edilmezse, swap sessizce atlanır.
- **Balon yerleşimi çoğunlukla sabittir.** `compose_frame` varsayılan
  olarak statik bir `BubblePlacement(x=80, y=140)` kullanır;
  `EpisodeManager` yalnızca kuyruk tarafını (sahne başına sırayla
  sol/sağ) ve x offset'i geçersiz kılar. Konuya dayalı dinamik yerleşim
  yoktur.
- **`config.yaml` kısmen yol göstericidir.** Bazı değerler (örn.
  varsayılan sahne süresi, crossfade, balon yazı tipi boyutu) hem
  modül düzeyinde dataclass varsayılanlarında hem de arayüz
  girdilerinde çoğaltılmıştır ve runtime'da her zaman `config.yaml`'dan
  okunmaz.
- **Legacy migration tek yönlüdür** ve `rmdir` kalan içerik nedeniyle
  başarısız olursa eski `episodes/` dosyalarını silmez; yalnızca
  `OSError`'u yutar.
- **Doğrulama kısmidir.** Screenwriter doğrulaması, bir sahne
  lokasyonu fallback sonrasında bile geçersiz kalırsa uyarı verir
  (raise etmez), ancak bilinmeyen karakter anahtarları, bilinmeyen
  konuşmacılar veya aynı lokasyonda geriye giden time-of-day için
  raise eder.
- **Dil.** Arayüz etiketleri, prompt'lar ve üretilen çıktı Türkçedir;
  İngilizce yalnızca görsel modeli için `visual_prompt` ve
  `characters[].doing` içinde kullanılır.
- **`new/story_bible/`**, ayrı bir dizi ("Yarım Kalan Düğün") için
  Markdown tasarım notları içerir. [src/](src/) içindeki hiçbir kod
  tarafından okunmaz.
- **Windows-odaklı path'ler.** `.env.example` indirme talimatları ve
  face-swap docstring'i PowerShell snippet'leri kullanır; koddaki
  hiçbir şey Windows'a özgü değildir, ancak geliştirme ortamı
  Windows'tur.

29) ## 🇬🇧 English

Bu dokümanın İngilizce versiyonu için [README.md](README.md)

> Not: Bu doküman İngilizce versiyondan türetilmiştir ve zaman zaman geride kalabilir.

---

# Aile Hekimliği Kura Sistemi (kura_web)

**Türkiye Aile Hekimliği ek yerleştirme (kura)** sürecini uygulayan bir
web ve mobil platform. Sistem, resmi hekim listesi (kesin liste) ve boş
pozisyon listesini içeri alır; hekimlerin uygunluklarını doğrulamalarını
ve sıralı tercihlerini göndermelerini sağlar; ardından cascading
(zincirleme) mantıkla deterministik bir yerleştirme simülasyonu
çalıştırarak yerleştirme sonuçlarını üretir.

Depo; üç bağımsız uygulama (backend, frontend, mobile), bir PostgreSQL
şema dökümü ve proje dokümantasyonu içeren bir monorepo yapısındadır.

---

## Genel Bakış

- **Amaç.** Bir ek yerleştirme dönemine ait uçtan uca hekim yerleştirme
  iş akışını yönetmek: resmi hekim listesini (kesin liste) ve boş birim
  listesini (boş pozisyon listesi) içeri almak, hekimlerden sıralı
  tercihleri (1–10) toplamak, sabit bir aralıkta cascading yerleştirme
  simülasyonu çalıştırmak ve sonuçları hem hekimlere hem yöneticilere
  göstermek.
- **Kapsam.** Kayıt, resmi listeye karşı uygunluk eşleştirmesi, tercih
  gönderimi (1–10 arası sıralı seçim), simülasyona katılma / toplu
  çalıştırma / sonuçlar, dönem yaşam döngüsü yönetimi ve CSV ile PDF
  üzerinden admin içeri aktarımları bu kapsamda yer alır.
- **Alan mantığı.** Yerleştirme, sabit bir öncelik sırası
  (`sequence_number` / sıra no) üzerinde **tam yeniden hesaplanan
  cascading atama** olarak modellenmiştir. Daha yüksek öncelikli bir
  hekim taşındığında, ayrıldığı birim aynı toplu çalıştırma içinde daha
  düşük öncelikli hekimler için boşa geçer.

Proje, dolu bir PostgreSQL şeması ve kapsamlı bir test takımı olan
çalışır durumda bir uygulamadır. Kullanıcıya dönük metinler ve alan
terimleri Türkçedir; kod ve API şeması İngilizcedir.

---

## Özellikler

Yalnızca kod tabanında fiilen bulunan özellikler listelenmiştir.

### Kimlik doğrulama ve hesaplar
- E-posta + parola ile kayıt ve giriş (JWT; access + refresh token).
- Üç rol: `SUPER_ADMIN`, `ADMIN`, `PHYSICIAN`.
- Parola değiştirme (oturum açmış kullanıcı için), şifremi unuttum /
  parola sıfırlama akışı, süreli tokenlar, e-posta ve IP bazlı
  rate-limit.
- Oturum (session) takibi için ayrı tablo ve süresi dolan oturumların
  temizliği.
- Üretim koruması: `ENVIRONMENT=production` olduğunda JWT secret’ın
  ayarlanmış ve en az 32 karakter olması gerekir; uygulama varsayılan
  secret ile başlamayı reddeder.

### Hekim akışı
- İçeri aktarılmış resmi listeye karşı normalleştirilmiş ad, soyad ve
  opsiyonel aile hekimliği birim numarası ile uygunluk doğrulaması.
  Benzersiz eşleşme, çoklu eşleşme seçimi ve eşleşme yok senaryoları
  desteklenir.
- Claim (talep) denemesi kayıtları; IP, user-agent ve eşleşme skoru
  (match score) saklanır.
- Tercih yönetimi: taslak kaydet, gönder, durum görüntüle; dönem başına
  1–10 arası kurum sıralaması.
- Mevcut atamayı ve simülasyon durumunu özetleyen dashboard.
- Kurum listeleme ve harita görünümü (ilçe / birim doluluk).
- Mevcut dönem için en son simülasyon sonucunun görüntülenmesi.

### Yönetici (admin) akışı
- Açık durum yaşam döngüsüne sahip dönem CRUD işlemleri
  (`DRAFT → ACTIVE → CLOSED → COMPLETED`) ve aynı anda yalnızca tek bir
  aktif dönem olmasını garanti eden veritabanı kısıtı.
- CSV veya PDF üzerinden hekim ve kurum içeri aktarımı (Sağlık
  Bakanlığı’nın resmi PDF’leri için `pdfplumber` tabanlı ayrıştırma).
  İçeri aktarımlar önizleme verisi ve onay / iptal adımlarıyla birlikte
  `ImportBatch` kayıtları olarak tutulur.
- Sayım ve özet istatistiklerin yer aldığı admin dashboard.
- İlçe ve birim doluluğunu gösteren admin harita görünümü.
- Tüm yerleştirme sonuçları için sayfalanmış ve filtrelenebilir görünüm.
- Zamanlayıcıya ek olarak manuel simülasyon tetikleme.

### Simülasyon
- Yapılandırılabilir aralıkta (varsayılan 10 dakika) cascading
  yerleştirme algoritmasını çalıştıran arka plan zamanlayıcısı.
- Her birimi kapasite 1 olarak ele alan, hekimleri `sequence_number`
  sırasında işleyen ve beş yerleştirme sonucu döndüren tam yeniden
  hesaplamalı (full-recompute) algoritma: `PLACED_VACANT`,
  `PLACED_VACATED`, `STAYED_ORIGINAL`, `UNPLACED_NO_PREF`,
  `UNPLACED_ALL_OCCUPIED`.
- `SimulationRequest` mekanizması sayesinde hekimlerin her çalıştırmaya
  açıkça katılması gerekir; dönem başına hekim başına yalnızca bir
  aktif isteği mümkün kılan kısmi (partial) benzersiz indeks
  kullanılır.
- Başlangıç kurtarması: öldürülen bir süreçten kalmış `running`
  durumundaki simülasyonlar, zamanlayıcı başlamadan önce sıfırlanır.
- Salt okunur görünümler için önbelleklenmiş tercih analizi yüzeyi
  (`analysis` modülü).

### Kesişen bileşenler
- Üretimde yapılandırılmış JSON loglama, geliştirmede okunabilir log.
- Veritabanı erişilebilirliği, aktif dönem varlığı, sürüm ve ortam
  bilgisini raporlayan `/health` uç noktası.
- Merkezi istisna işleyiciler: `ConflictError → 409`,
  `IntegrityError → 409` ve yakalanmamış `Exception → 500` (loglanır).
- Açık CORS origin allowlist; üretimde joker (`*`) origin reddedilir.
- Admin dosya yüklemeleri için istek başına yükleme boyutu sınırı
  (varsayılan 50 MB).
- Admin eylemlerini kapsayan audit log tablosu.

---

## Proje Yapısı

```
kura_web/
├── backend/                         Python / FastAPI API
│   ├── app/
│   │   ├── main.py                  FastAPI app, router’lar, lifespan, CORS, handler’lar
│   │   ├── config.py                Pydantic-settings yapılandırması (.env)
│   │   ├── database.py              Async SQLAlchemy engine / session
│   │   ├── core/                    Kesişen yardımcılar
│   │   │   ├── audit.py             Audit log kaydı
│   │   │   ├── errors.py            Alan istisna sınıfları
│   │   │   ├── mailer.py            Giden e-posta (Resend API)
│   │   │   ├── observability.py     JSON logger, log yardımcıları
│   │   │   ├── pdf_parser.py        Resmi listeler için PDF ayrıştırma
│   │   │   ├── rate_limit.py        IP / kullanıcı bazlı rate-limit
│   │   │   └── utils.py             Türkçe ad normalleştirme
│   │   ├── models/                  SQLAlchemy ORM modelleri (user, physician,
│   │   │                            institution, period, preference,
│   │   │                            simulation, simulation_request,
│   │   │                            placement, import_batch,
│   │   │                            password_reset_token, claim_attempt,
│   │   │                            audit_log, preference_analysis,
│   │   │                            session, system_config, base)
│   │   ├── modules/
│   │   │   ├── auth/                Kayıt, giriş, parola akışları
│   │   │   ├── admin/               Dönem / hekim / kurum yönetimi,
│   │   │   │                        içeri aktarma orkestrasyonu
│   │   │   ├── physician/           Uygunluk, tercihler, dashboard,
│   │   │   │                        public period uç noktası
│   │   │   ├── simulation/          Engine, scheduler, analysis_builder,
│   │   │   │                        join / status / run / results router
│   │   │   └── analysis/            Önbelleklenmiş tercih analizi (salt okunur)
│   │   ├── alembic/                 Alembic migrasyonları
│   │   ├── scripts/run_simulation.py  Manuel simülasyon CLI’si
│   │   └── tests/                   pytest test takımı (async + engine)
│   ├── requirements.txt             Çalışma zamanı bağımlılıkları
│   ├── requirements-dev.txt         Test / dev bağımlılıkları
│   ├── pyproject.toml               Paket metadata ve tool yapılandırması
│   └── alembic.ini                  Migrasyon yapılandırması
│
├── frontend/                        Next.js 14 web uygulaması (App Router)
│   └── src/app/                     Sayfalar: /, login, register,
│                                    forgot-password, reset-password,
│                                    dashboard, eligibility, preferences,
│                                    simulation, results, profile, map,
│                                    privacy, terms, support,
│                                    admin/{login,dashboard,periods,profile,
│                                    results,map,official-lists}
│
├── mobile/                          React Native uygulaması (Expo 54)
│   └── src/                         Feature bazlı ekranlar:
│                                    auth, dashboard, eligibility,
│                                    preferences, simulation, results,
│                                    map, profile, admin (dashboard,
│                                    periods, uploads, results, map,
│                                    profile), paylaşılan UI
│
├── docs/
│   ├── project/                     architecture-patterns.md,
│   │                                ai-development-rules.md,
│   │                                coding-standards.md,
│   │                                engineering-principles.md,
│   │                                security-guidelines.md
│   └── standards/                   project-structure.md
│
├── scripts/                         Depo genelinde yardımcı script’ler
├── logo/                            Marka varlıkları
├── kura-database.sql                PostgreSQL şema / veri dökümü
├── 171-ek-...pdf, aile-hekimligi-... .pdf   Kaynak resmi belgeler
├── MASTER_IMPLEMENTATION_PLAN_v5_FINAL.md   Uygulama planı
├── eski_planlar/                    Arşivlenmiş planlama dokümanları
├── package.json                     Kök workspace işaretçisi (babel-preset-expo dev dep)
└── .gitignore
```

---

## Kullanılan Teknolojiler

### Backend (`backend/`)
- FastAPI (`fastapi>=0.109.0`) üzerinde Python ve Uvicorn.
- SQLAlchemy 2.0 async (`sqlalchemy[asyncio]>=2.0.25`), PostgreSQL için
  `asyncpg`.
- Doğrulama ve ortam değişkeni tabanlı yapılandırma için Pydantic v2 ve
  `pydantic-settings`.
- JWT için `python-jose[cryptography]`, parola hash’i için
  `passlib[bcrypt]`, multipart yüklemeler için `python-multipart`.
- Migrasyonlar için Alembic.
- Resmi hekim ve pozisyon listelerinin PDF ayrıştırması için
  `pdfplumber`.
- Test: `pytest`, `pytest-asyncio`, `httpx`, `aiosqlite`.

### Frontend (`frontend/`)
- React 18 üzerine Next.js 14.1.0 ve App Router.
- TypeScript.
- Tailwind CSS 3.4, `class-variance-authority`, `clsx`, `tailwind-merge`.
- Formlar için `react-hook-form` + `zod` (`@hookform/resolvers`
  üzerinden).
- İstemci tarafı state için `zustand`.
- HTTP istemcisi olarak `axios`.
- Animasyonlu arka planlar için `@tsparticles/*`, ikonlar için
  `lucide-react`.
- Testler Node’un yerleşik test runner’ı ile çalışır
  (`node --test __tests__/*.test.mjs`).

### Mobile (`mobile/`)
- Expo SDK 54 üzerinde React Native 0.81.5.
- React 19 ve TypeScript.
- React Navigation 7 (native stack + bottom tabs).
- Çevrimdışı dostu önbellekleme için TanStack React Query 5 ve
  `@tanstack/query-async-storage-persister`.
- `zustand`, `react-hook-form`, `zod`, `axios`.
- Kod tabanında kullanılan Expo modülleri: `expo-secure-store`,
  `expo-local-authentication`, `expo-document-picker`, `expo-haptics`,
  `expo-image`, `expo-font`, `expo-linking`, `expo-splash-screen`,
  `expo-status-bar`, `expo-screen-capture`, `expo-constants`,
  `@react-native-async-storage/async-storage`,
  `@react-native-community/datetimepicker`.
- İkonlar: `lucide-react-native`.
- Testler Node’un yerleşik test runner’ı ile çalışır.

### Kök
- Depo kökündeki `package.json` dev bağımlılığı olarak
  `babel-preset-expo` içeren minimal bir workspace işaretçisidir.
- `kura-database.sql`, tam şemayı (users, physicians, institutions,
  periods, preferences, simulations, simulation_requests, placements,
  import_batches, sessions, password_reset_tokens, claim_attempts,
  audit_logs, preference_analysis, `alembic_version` vb. tabloları)
  içeren bir PostgreSQL dökümüdür.

---

## Kurulum

Üç uygulama birbirinden bağımsız şekilde kurulur ve çalıştırılır.

### Ön gereksinimler
- Backend için Python 3.11+.
- Frontend ve mobil için Node.js 18+.
- PostgreSQL 15+ (sunulan şema dökümü ile uyumlu).
- Mobil cihaz build’leri için Expo CLI / EAS CLI (isteğe bağlı).

### 1. Backend

```bash
cd backend
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate

pip install -r requirements.txt
pip install -r requirements-dev.txt   # opsiyonel, testler için

# Ortam değişkenlerini kopyala ve düzenle
cp .env.example .env

# Migrasyonları uygula
alembic upgrade head

# API’yi çalıştır (geliştirme)
uvicorn app.main:app --reload
```

FastAPI etkileşimli dokümantasyonu `/docs` ve `/redoc` altında sunar.

Alternatif olarak, depo içinde sıfırdan migrasyon çalıştırmak yerine
boş bir veritabanına doğrudan yüklenebilen `kura-database.sql`
PostgreSQL dökümü de bulunur.

### 2. Frontend

```bash
cd frontend
npm install
# NEXT_PUBLIC_API_URL değerini backend adresine göre ayarlayın
npm run dev        # geliştirme sunucusu
npm run build      # üretim build’i
npm run start      # üretim build’ini çalıştır
npm run test       # Node test runner
```

### 3. Mobile

```bash
cd mobile
npm install
npm run start          # Expo dev sunucusu (Metro)
npm run android        # Android’de build + çalıştır
npm run ios            # iOS’ta build + çalıştır (yalnızca macOS)
npm run web            # Expo web hedefi
npm run test           # Node test runner
```

`mobile/eas.json` EAS Build için mevcuttur ve `mobile/app.json` Expo
yapılandırmasını içerir.

---

## Kullanım

### Yönetici (admin) akışı
1. `ADMIN` / `SUPER_ADMIN` rolünde bir hesap oluşturulur. Rol ataması,
   veritabanı / `app/modules/admin` içindeki admin akışları aracılığıyla
   yapılır.
2. `POST /api/admin/periods` ile yeni dönem `Draft` durumunda oluşturulur.
3. Resmi hekim listesi (kesin liste) ve boş pozisyon listesi yüklenir —
   CSV veya resmi PDF olarak. Backend veriyi bir `ImportBatch` olarak
   stage eder, önizleme döner ve yalnızca açık onaydan sonra commit eder.
4. Dönem aktive edilir. Veritabanı aynı anda yalnızca bir aktif döneme
   izin verir.
5. Admin dashboard, harita ve sonuç ekranları aracılığıyla gelen
   tercih gönderimleri ve simülasyon çalıştırmaları izlenir.
6. Simülasyon manuel tetiklenebilir (admin uç noktası veya
   `backend/app/scripts/run_simulation.py`) ya da zamanlayıcıya
   bırakılabilir.
7. Yerleştirme kesinleştiğinde dönem `ACTIVE → CLOSED → COMPLETED`
   geçişleriyle sonlandırılır.

### Hekim akışı
1. Kayıt ol ve giriş yap.
2. Ad, soyad ve opsiyonel aile hekimliği birim numarası ile uygunluk
   doğrulaması yap. Birden fazla aday eşleşirse doğru kayıt seçilir;
   tek eşleşmede otomatik olarak talep edilir (claim).
3. Kurumlara göz at, taslak tercihleri kaydet (en fazla 10 sıralı
   tercih), ardından gönder.
4. Bir sonraki simülasyon toplu çalıştırmasına katıl. Zamanlayıcı
   `SIMULATION_INTERVAL_MINUTES` dakikada bir çalışır ve toplu
   çalıştırmadaki her hekim için bir `Placement` kaydı üretir.
5. En son sonuç dashboard / sonuçlar sayfasında görüntülenir.

---

## Yapılandırma

Backend yapılandırması `app/config.py` tarafından ortam değişkenlerinden
(bir `.env` dosyası üzerinden) yüklenir. Temel ayarlar:

| Değişken | Varsayılan | Amaç |
|---|---|---|
| `ENVIRONMENT` | `development` | `development` / `production` / `test`. Fail-fast kontrollerini yönetir. |
| `DATABASE_URL` | `postgresql+asyncpg://postgres:postgres@localhost:5432/kura_web` | Async PostgreSQL URL’si. |
| `JWT_SECRET_KEY` | varsayılan placeholder | Üretimde ≥ 32 karakter olmak zorunda; varsayılan reddedilir. |
| `JWT_ALGORITHM` | `HS256` | JWT imzalama algoritması. |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `15` | Access token TTL. |
| `REFRESH_TOKEN_EXPIRE_DAYS` | `7` | Refresh token TTL. |
| `ALLOWED_ORIGINS` | `http://localhost:3000` | Virgülle ayrılmış CORS origin’leri. Üretimde zorunlu ve joker kabul etmez. |
| `CLAIM_RATE_LIMIT_ATTEMPTS` | `5` | Pencere başına claim denemesi. |
| `CLAIM_RATE_LIMIT_WINDOW_MINUTES` | `15` | Claim denemeleri için pencere. |
| `PASSWORD_RESET_TOKEN_EXPIRE_MINUTES` | `15` | Parola sıfırlama tokenı TTL. |
| `PASSWORD_RESET_RATE_LIMIT_PER_EMAIL_PER_HOUR` | `5` | E-posta başına saatlik sınır. |
| `PASSWORD_RESET_RATE_LIMIT_PER_IP_PER_HOUR` | `10` | IP başına saatlik sınır. |
| `FRONTEND_URL` | `http://localhost:3000` | Parola sıfırlama linkleri için. |
| `RESEND_API_KEY` | *(boş)* | Resend API anahtarı. |
| `MAIL_FROM` | `Aile Hekimliği Kura Sistemi <no-reply@kurasistem.local>` | Gönderen adres. |
| `SIMULATION_INTERVAL_MINUTES` | `10` | Zamanlayıcı aralığı. |
| `SLOW_QUERY_THRESHOLD_MS` | `100` | Yavaş sorgu log eşiği. |
| `DEFAULT_PAGE_SIZE` | `50` | Varsayılan sayfa boyutu. |
| `MAX_PAGE_SIZE` | `100` | Maksimum sayfa boyutu. |
| `MAX_UPLOAD_SIZE_MB` | `50` | Admin yüklemeleri için üst sınır. |

Frontend yapılandırması, Next.js’in `.env.local`’den okuduğu tek bir
`NEXT_PUBLIC_API_URL` değeridir; backend adresini işaret eder.

Mobil yapılandırma `mobile/src/config/api.ts` ve Expo’nun
`app.json` / `eas.json` dosyaları üzerinden yürütülür.

---

## Mimari / Temel Bileşenler

Sistem; backend tarafından HTTP JSON API’leri üzerinden haberleşen üç
dağıtılabilir birime ayrılmıştır.

### Backend
- **`app/main.py`** — FastAPI uygulaması. Şunları kurar:
  - Açık allowlist ile CORS middleware.
  - İstisna işleyiciler (`ConflictError → 409`,
    `IntegrityError → 409`, yakalama genel `Exception → 500`).
  - Üretimde yapılandırılmış JSON loglama.
  - (1) `running` durumda takılı kalmış simülasyonları toparlayan ve
    (2) simülasyon zamanlayıcısını başlatıp durduran lifespan hook.
  - `/health` uç noktası ve `/` kök uç noktası.
- **`app/modules/auth`** — Kayıt, giriş, refresh, profil, parola
  değiştirme, şifremi unuttum / sıfırlama. JWT bearer bağımlılıkları
  `current user`, `active user` ve `admin user` kısayollarını sunar.
- **`app/modules/admin`** — Dönem yaşam döngüsü, hekim ve kurum CRUD,
  `ImportBatch` üzerinden CSV / PDF içeri aktarımı (önizleme → onay →
  insert), admin dashboard / harita / sonuç görünümleri.
- **`app/modules/physician`** — İçeri aktarılmış resmi listeye karşı
  uygunluk eşleştirmesi, tercih kaydet / gönder / durum, dashboard,
  kurum listesi, public period uç noktası.
- **`app/modules/simulation`** — Yerleştirme engine’i, scheduler,
  istek yaşam döngüsü (`join`, `status`, `run`, `results`), admin
  dashboard ve `preference_analysis` önbelleğini dolduran analiz
  builder.
- **`app/modules/analysis`** — Önbelleklenmiş tercih analizini sunan
  salt okunur uç noktalar.
- **`app/core`** — Audit kaydı, alan hataları, mailer, observability
  (JSON formatter, hata loglama), PDF ayrıştırma, rate-limit, Türkçeye
  özgü ad normalleştirme.
- **`app/models`** — Şema dökümündeki her tablo için SQLAlchemy ORM
  modelleri.
- **`app/alembic/versions`** — Şema evrimini kapsayan migrasyonlar.

### Frontend
- **Next.js App Router** sayfaları `frontend/src/app/` altında;
  kullanıcı (hekim) akışı (`/dashboard`, `/eligibility`,
  `/preferences`, `/simulation`, `/results`, `/map`, `/profile`) ve
  admin akışı (`/admin/...`) olarak ayrılır; ayrıca kimlik doğrulama
  (`/login`, `/register`, `/forgot-password`, `/reset-password`) ve
  statik (`/privacy`, `/terms`, `/support`) sayfaları bulunur.
- **`src/lib/api.ts`** — Axios HTTP istemcisi, base URL
  `NEXT_PUBLIC_API_URL`’den alınır; auth header yönetimi burada.
- Kimlik doğrulama ve UI state için **Zustand** store’ları.
- Form state ve doğrulama için **React Hook Form + Zod**.
- Stil: **Tailwind CSS**; `@tsparticles` ile animasyonlu parçacık arka
  planı.

### Mobile
- `mobile/src/features/*` altında feature odaklı dizin düzeni:
  `auth`, `dashboard`, `eligibility`, `preferences`, `simulation`,
  `results`, `map`, `profile` ve `admin/*` (dashboard, periods,
  uploads, results, map, profile, shared).
- Bir `RootNavigator`; auth ve authenticated stack’ler arasında geçiş
  yapar. Authenticated stack bir bottom tab navigator içerir; rol bazlı
  olarak admin tab’ları koşullu gösterilir.
- Uygulama açılışları arası önbelleklenmiş server state için
  **AsyncStorage persister ile TanStack React Query**.
- Tokenlar **`expo-secure-store`** ile saklanır; geniş istemci state’i
  **AsyncStorage** üzerinde tutulur. `expo-local-authentication` ile
  biyometrik kilit açma desteklenir.
- Admin yüklemeleri için `expo-document-picker` kullanılır.

---

## Veri Akışı / Çalışma Zamanı Akışı

### İçeri aktarma akışı (admin)
1. Admin ilgili admin import uç noktasına bir CSV veya PDF yükler.
2. Backend girdiyi ayrıştırır (CSV reader veya `pdfplumber`),
   satırları çıkarır ve bunları `pending` durumunda bir `ImportBatch`
   olarak — önizleme verisi ve satır bazlı hatalar ile birlikte —
   saklar.
3. Admin önizlemeyi inceler ve onaylar veya iptal eder.
4. Onay durumunda backend, stage edilmiş satırları bir transaction
   içinde `Physician` / `Institution` tablolarına yazar ve batch’i
   `confirmed` olarak işaretler.

### Tercih akışı (hekim)
1. Uygunluk doğrulaması; normalleştirilmiş ad eşleştirmesi ve
   opsiyonel birim numarası ile oturum açmış kullanıcıyı bir
   `Physician` kaydına bağlar. Denemeler `claim_attempts` tablosuna
   yazılır.
2. Tercihler dönem ve hekime göre anahtarlanmış `Preference` satırları
   olarak kaydedilir; `preference_order` 1’den en fazla 10’a kadar
   gider.
3. Gönderme işlemi tercihleri submitted olarak işaretler; ardından UI
   simülasyona katılma seçeneğini açar.

### Simülasyon akışı
1. Her hekim aktif bir `SimulationRequest` oluşturur. Partial unique
   indeks `(physician, period)` başına en fazla bir aktif isteğe izin
   verir.
2. Zamanlayıcı `SIMULATION_INTERVAL_MINUTES` dakikada bir çalışır.
   Aktif istekleri alır, dönem snapshot’ından kurumları ve mevcut
   atamaları yükler ve bunları `app/modules/simulation/engine.py`
   içindeki engine’e iletir.
3. Engine, hekimleri `sequence_number`’a göre artan sırada işler ve
   her biri için tercihleri sırayla dener:
   - Hedef birim boşsa atanır.
   - Hedef birim, bu toplu çalıştırmada zaten taşınmış bir hekim
     tarafından tutuluyorsa atanır (birim artık boştur — cascading).
   - Aksi halde sıradaki tercihe geçilir.
   - Hiçbir tercih uymazsa hekim orijinal biriminde kalır (varsa),
     yoksa yerleşmemiş olarak kaydedilir.
4. Yeni bir `Simulation` kaydı oluşturulur; her hekim için bir
   `Placement` satırı `PLACED_VACANT`, `PLACED_VACATED`,
   `STAYED_ORIGINAL`, `UNPLACED_NO_PREF`, `UNPLACED_ALL_OCCUPIED`
   değerlerinden biriyle etiketlenir ve önbelleklenmiş tercih analizi
   yenilenir.
5. Başlangıçta hâlâ `running` durumundaki simülasyonlar (örn. bir
   öldürülen worker’dan kalan) sıfırlanır; aksi hâlde partial unique
   indeks bir sonraki toplu çalıştırmayı bloklar.

### Dönem yaşam döngüsü
- `DRAFT` — içeri aktarma ve kurulum.
- `ACTIVE` — tercihler ve simülasyonlar kabul edilir.
- `CLOSED` — yeni tercih alınmaz; simülasyon hâlâ sonlandırılabilir.
- `COMPLETED` — sonuçlar kilitlenir.

Veritabanı düzeyinde bir kısıt aynı anda yalnızca bir `ACTIVE` döneme
izin verir.

---

## Sınırlamalar / Mevcut Durum

Bu notlar doğrudan depodan gözlemlenebilir olanı yansıtır.

- **Workspace manager’sız monorepo.** Kökteki `package.json` yalnızca
  `babel-preset-expo` dev bağımlılığını içerir; her uygulama
  (`backend`, `frontend`, `mobile`) bağımsız şekilde kurulur.
- **Depo içinde Docker veya CI yapılandırması yok.** `Dockerfile`,
  `docker-compose.yml` veya `.github/workflows` bulunmuyor. Next.js
  uygulaması standalone çıktıyla uyumlu şekilde yapılandırılmıştır,
  ancak paketlenmiş bir container build’i sunulmamaktadır.
- **Veritabanı başlatma.** Depo hem Alembic migrasyonlarını
  (`backend/app/alembic/versions`) hem de bir tam PostgreSQL dökümünü
  (`kura-database.sql`) içerir. İkisi şema oluşturmanın alternatif
  yollarıdır.
- **E-posta gönderimi.** Giden e-postalar `app/core/mailer.py`
  üzerinden Resend API ile entegredir. `RESEND_API_KEY` ayarlı değilse
  e-posta gönderimi etkin değildir.
- **Yerelleştirme.** Kullanıcıya dönük metinler, alan terimleri ve
  commit geçmişi Türkçedir; kod tanımlayıcıları, API şeması ve `docs/`
  içindeki dâhili dokümantasyon İngilizcedir.
- **Kaynak PDF’ler ve planlama belgeleri** (`171-ek-...pdf`,
  `aile-hekimligi-...pdf`, `MASTER_IMPLEMENTATION_PLAN_v5_FINAL.md`,
  `eski_planlar/`, `Yeni Metin Belgesi.txt`, `komut.txt`,
  `mobil_sonrası_prompt.txt`, `pdf_yukleme_hatasi_prompt.txt`) referans
  materyali olarak depoya dâhildir; çalışma zamanında gerekli
  değillerdir.
- **Simülasyon zamanlaması.** Zamanlayıcı, FastAPI lifespan hook
  tarafından başlatılan, süreç içi bir async döngüdür. Dağıtık bir iş
  sistemi değildir; birden fazla API sürecini eşzamanlı çalıştırmak
  hangi sürecin zamanlayıcıya sahip olduğunun koordine edilmesini
  gerektirir.
- **Kök `scripts/`, `logo/`, `.vscode/`, `.pytest_cache/`** ve `docs/`
  dizinleri mevcuttur ancak çalışan uygulamaların kapsamı dışındadır.

---

## Notlar

- Simülasyon her çalıştırmada bir **tam yeniden hesaplamadır** — her
  toplu çalıştırma, dönemin ilk durumundan (içeri aktarılmış resmi
  listeler ve mevcut atamalar) başlar ve yerleştirmeleri öncelik
  sırasında yeniden oynatır.
- Yerleştirme sonuçları açık bir reason (sebep) koduyla birlikte
  üretilir; böylece downstream görünümler (admin sonuçlar, hekim
  dashboard’u) taşınan, orijinal biriminde kalan ve yerleştirilemeyen
  hekimleri birbirinden ayırt edebilir.
- `ImportBatch`, toplu içeri aktarmalar için bütünlük sınırıdır:
  admin önizlemeyi açıkça onaylamadıkça satırlar asla ana tablolara
  yazılmaz; batch, satır bazlı hataları denetleme için saklar.
- `/health` uç noktası her zaman HTTP 200 döner ve veritabanı
  durumunu payload içinde bir alan olarak raporlar; böylece geçici bir
  veritabanı aksaklığı yük dengeleyicinin uygulamayı trafikten
  düşürmesine yol açmaz.

30) ## 🇬🇧 English

Bu dokümanın İngilizce versiyonu için [README.md](README.md)

> Not: Bu doküman İngilizce versiyondan türetilmiştir ve zaman zaman geride kalabilir.

---

# Orbacle

Orbacle, Expo ve React Native ile geliştirilmiş minimalist bir mobil uygulamadır. Kullanıcı bir soru yazar, animasyonlu kristal küreye dokunur ve yerel, önceden tanımlanmış bir cevap havuzundan rastgele seçilmiş kısa bir cevap alır. Davranış olarak bir "magic 8-ball" deneyimine benzer; koyu mor/menekşe tonlu bir tasarım, animasyonlu görsel efektler, titreşimli geri bildirim, yerel geçmiş kaydı ve tam İngilizce/Türkçe yerelleştirme sunar.

Uygulama tamamen çevrimdışıdır: herhangi bir sunucuyla veya üçüncü taraf API ile iletişim kurmaz. Tüm cevaplar, geçmiş kayıtları ve tercihler cihaz üzerinde saklanır.

## Genel Bakış

- Tek amaçlı uygulama: soru sor, cevabı göster.
- Üç sekmeli yapı: **Ana Sayfa**, **Geçmiş**, **Ayarlar**.
- İki dil: İngilizce ve Türkçe. İlk açılışta cihaz diline göre otomatik algılanır, ayarlardan değiştirilebilir.
- Cevaplar, uygulamayla birlikte paketlenen iki statik JSON havuzundan gelir (`src/data/answers.en.json`, `src/data/answers.tr.json`); her biri 363 girdi içerir.
- Kürenin görseli, `react-native-reanimated` ile yönetilen animasyonlu gradyanlar, sis katmanları, parıltı noktaları ve zemin sisi ile katmanlanmış bir kristal küre görüntüsünden oluşur.
- Geçmiş, yerel olarak `AsyncStorage` üzerinde tutulur ve en son 20 kayıtla sınırlıdır.
- [app.json](app.json) içinde tanımlı platform hedefleri: Android (`com.orbacle.app`) ve iOS (`com.orbacle.app`, tablet desteği kapalı). Çalışma dizinin adı `orbacle_android` ve repoda Expo Application Services (EAS) yapılandırması bulunur; bu, birincil odak noktasının Android derlemeleri olduğuna işaret eder.

## Özellikler

Mevcut koddan doğrulanan, uygulanmış özellikler:

- **Soru akışı**: kullanıcı bir soru yazar (en fazla 200 karakter, tek satır), küreye dokunur ve ~1,4 saniyelik animasyonlu bir açılış sekansının ardından aktif dil havuzundan bir cevap gösterilir.
- **Tekrar etmeyen rastgele seçim**: `getRandomAnswer`, aynı oturum içinde aynı cevabı üst üste döndürmekten kaçınır.
- **Boş giriş koruması**: alan boşken küreye dokunulduğunda satır içi bir uyarı ("Lütfen önce bir soru girin") gösterilir ve uyarı tipi bir titreşim tetiklenir.
- **Animasyonlu küre**: idle nabız (ölçek + aura opaklığı), dokunmayla tetiklenen parıltı, patlama halkası ve ölçek sekansı; buna ek olarak ambiyans sisi katmanları ve parıltı noktaları ([src/components/Orb.tsx](src/components/Orb.tsx) ve [src/hooks/useOrbAnimation.ts](src/hooks/useOrbAnimation.ts) içinde uygulanır).
- **Titreşim geri bildirimi** (`expo-haptics` üzerinden): küreye dokunmada orta şiddette bir etki, cevap açılışında başarı bildirimi, boş girişte uyarı bildirimi. Ayarlar'dan açılıp kapatılabilir.
- **Cevap kartı**: fade-in animasyonu ile görünür; `react-native Share.share` aracılığıyla işletim sistemi paylaşım sayfasını açan bir paylaş eylemi içerir.
- **Geçmiş**:
  - Açılışta her Q&A (soru, cevap, zaman damgası, dil) otomatik olarak kaydedilir.
  - En yeni en üstte olmak üzere 20 öğeyle sınırlı bir yuvarlanan liste.
  - Bozuk girdiler okuma sırasında filtrelenir ve temiz liste yeniden yazılır.
  - Ayarlar'da bir onay modalıyla korunan "Geçmişi Temizle" eylemi.
- **Yerelleştirme (i18n)**:
  - İngilizce (`src/i18n/en.json`) ve Türkçe (`src/i18n/tr.json`) için string tabloları.
  - Senkron `t(key)` fonksiyonu olan dil context sağlayıcısı (`I18nProvider`); İngilizceye ve ardından ham anahtara geri düşer.
  - İlk çalıştırmada `NativeModules` üzerinden cihaz dili algılama ve saklama.
  - Ayarlar'da **İngilizce** ile **Türkçe** arasında manuel geçiş.
- **Tarih biçimlendirme**: `formatDate`, aktif dile göre `tr-TR` veya `en-US` ile `toLocaleDateString` kullanır.
- **Ayarlar ekranı**:
  - Dil seçici (EN / TR).
  - Titreşim anahtarı (saklanır).
  - Onay modallı "Geçmişi Temizle" eylemi.
  - Uygulama açıklaması, katkı bilgileri ve sürüm dizgesinin yer aldığı "Hakkında" bölümü.
- **Hata sınırı** ([src/components/ErrorBoundary.tsx](src/components/ErrorBoundary.tsx)): tüm uygulamayı sarar ve yakalanan bir render hatası durumunda sıfırlama eylemi içeren bir "Something went wrong" ekranı gösterir.
- **Splash gecikmesi**: i18n sağlayıcısı `ready` duruma geçene kadar Expo'nun native splash ekranı `expo-splash-screen` ile tutulur; böylece çeviri öncesi bir "siyah ekran" yanıp sönmesi önlenir.
- **Güvenli alan duyarlı düzen**: her ekran `useSafeAreaInsets` değerlerini dikkate alır; alt sekme çubuğu yüksekliği alt güvenli alan değeri kadar uzatılır.
- **Duyarlı küre boyutu**: küre boyutu = `min(300, round(pencere_genişliği * 0.7))` px.
- **Erişilebilirlik detayları**: küre `Pressable`'ında `accessibilityRole`/`accessibilityLabel`/`accessibilityState`; dekoratif parçacıklar ve sis katmanları `accessibilityElementsHidden` olarak işaretlenir; başlık/slogan/yardım metinlerinde `maxFontSizeMultiplier` üst sınırı; sekme ikonlarında `allowFontScaling={false}`.

## Proje Yapısı

```
orbacle_android/
├── App.tsx                         Kök bileşen: sağlayıcılar, splash geciktirme, alt sekme navigatörü
├── app.json                        Expo uygulama manifestosu (ad, ikonlar, splash, android/ios yapılandırması, eklentiler)
├── eas.json                        EAS Build profilleri (development, preview, production) ve submit yapılandırması
├── babel.config.js                 babel-preset-expo + react-native-reanimated/plugin
├── tsconfig.json                   expo/tsconfig.base'i genişletir; strict mode açık
├── package.json                    Komut dosyaları ve bağımlılıklar
├── assets/
│   ├── icon.png                    iOS / genel uygulama ikonu
│   ├── android-icon.png            Android eski ikon
│   ├── adaptive-foreground.png     Android adaptive icon ön planı
│   ├── splash.png                  Splash görseli (arka plan #0a0a1a)
│   └── orb.webp                    Orb bileşeninin kullandığı kristal küre görseli
└── src/
    ├── components/
    │   ├── Orb.tsx                 Aura/parıltı/patlama/sis/parıltı katmanlı kristal küre görseli
    │   ├── QuestionInput.tsx       Stil verilmiş, 200 karakterle sınırlı tek satır metin girişi
    │   ├── AnswerCard.tsx          Açılan cevabı gösteren fade-in kart
    │   ├── PrimaryButton.tsx       Ghost / filled düğme varyantları (filled LinearGradient kullanır)
    │   ├── LanguageSwitcher.tsx    İki seçenekli EN/TR segmented control
    │   ├── ConfirmModal.tsx        Yıkıcı işlemleri onaylamak için kullanılan saydam modal
    │   └── ErrorBoundary.tsx       Sıfırlama eylemli class component hata sınırı
    ├── constants/
    │   ├── colors.ts               Palet (arka plan, primary/mor, gradyanlar, küre tonları vb.)
    │   ├── spacing.ts              Boşluk skalası ve borderRadius skalası
    │   └── typography.ts           Metin stili ön ayarları (hero, title, subtitle, body, caption, answer)
    ├── data/
    │   ├── answers.en.json         363 İngilizce cevap dizgesi
    │   └── answers.tr.json         363 Türkçe cevap dizgesi
    ├── hooks/
    │   └── useOrbAnimation.ts      Reanimated shared value'ları + açılış sekansı + temizleme referansları
    ├── i18n/
    │   ├── index.ts                I18nProvider, useI18n hook'u, ready bayrağı
    │   ├── en.json                 İngilizce UI dizgeleri
    │   └── tr.json                 Türkçe UI dizgeleri
    ├── screens/
    │   ├── HomeScreen.tsx          Başlık, slogan, küre, giriş → cevap → paylaş / yeni soru
    │   ├── HistoryScreen.tsx       Saklanan Q&A girdilerinin FlatList'i (boş durum dahil)
    │   └── SettingsScreen.tsx      Dil, titreşim anahtarı, geçmişi temizle, hakkında
    ├── storage/
    │   ├── historyStorage.ts       Geçmiş için AsyncStorage CRUD (doğrulama ve 20 öğe üst sınırı ile)
    │   └── settingsStorage.ts      Dil ve titreşim bayrağı için AsyncStorage okuma/yazma
    ├── types/
    │   ├── history.ts              HistoryItem tipi
    │   └── language.ts             Language = 'en' | 'tr'
    └── utils/
        ├── formatDate.ts           Yerel ayara duyarlı tarih/saat biçimlendirici
        ├── getDeviceLanguage.ts    NativeModules üzerinden cihaz dilini algılar; 'en' | 'tr' ile eşler
        └── getRandomAnswer.ts      Ardışık tekrar olmadan rastgele seçim yapan yardımcı
```

## Kullanılan Teknolojiler

[package.json](package.json) üzerinden:

- **React 19.1.0** ve **React Native 0.81.5**.
- **Expo SDK 54** (`expo ^54.0.0`), managed workflow. Hermes JS motoru etkin (`"jsEngine": "hermes"`, bkz. `app.json`).
- **Expo modülleri**: `expo-splash-screen`, `expo-status-bar`, `expo-haptics`, `expo-linear-gradient`, `expo-asset`.
- **Navigasyon**: `@react-navigation/native` ve `@react-navigation/bottom-tabs` (v7); `react-native-screens` ve `react-native-safe-area-context` ile birlikte.
- **Animasyonlar**: `react-native-reanimated ~4.1.1` ve `react-native-worklets 0.5.1` (Reanimated babel eklentisi [babel.config.js](babel.config.js) içinde yapılandırılmıştır).
- **Yerel depolama**: `@react-native-async-storage/async-storage 2.2.0`.
- **TypeScript 5.9.2** (strict, `expo/tsconfig.base`'i genişletir).
- **Derleme hattı**: Expo Application Services (EAS) — bkz. [eas.json](eas.json). CLI ≥ 18.3.0 gerekli, `appVersionSource: remote`, üç derleme profili (`development`, `preview`, `production`; production'da `autoIncrement: true`).

## Kurulum

Önkoşullar:

- Node.js ve npm.
- Expo ile uyumlu güncel bir araç zinciri (Expo SDK 54 managed'dır; lokal bir Android/iOS native projesi gerekmez).
- EAS derlemeleri için: EAS CLI (`npm install -g eas-cli`) ve bir Expo hesabı.

Bağımlılıkları kurun:

```bash
npm install
```

## Kullanım

Kullanılabilir komutlar [package.json](package.json) içinde tanımlanmıştır:

```bash
# Expo geliştirme sunucusunu başlat (Metro bundler + geliştirici menüsü)
npm run start

# Bağlı bir Android cihaz/emülatörde çalıştır (Expo managed)
npm run android

# iOS'ta çalıştır (macOS gerekir)
npm run ios

# Expo web hedefini başlat
npm run web
```

Üretim / önizleme derlemeleri, [eas.json](eas.json) içinde tanımlanan profillerle EAS aracılığıyla üretilir:

```bash
eas build --profile development
eas build --profile preview
eas build --profile production
```

Son git geçmişi, projenin native Android derleme klasöründen Expo managed splash/derleme akışına geçtiğini gösterir:

- `remove android native build, switch to expo managed splash, update splash design`

## Yapılandırma

Yapılandırma tamamen beyanname dosyaları üzerinden ifade edilir; kaynak ağacında çalışma zamanı `.env` kullanımı yoktur.

- [app.json](app.json):
  - `name: "Orbacle"`, `slug: "orbacle"`, `version: "1.0.0"`.
  - `orientation: portrait`, `userInterfaceStyle: dark`, `jsEngine: hermes`.
  - Splash: `./assets/splash.png`, `resizeMode: contain`, arka plan `#0a0a1a`.
  - Android: paket `com.orbacle.app`, adaptive icon ön planı `./assets/adaptive-foreground.png`, adaptive arka plan `#0a0a1a`, `permissions: []`.
  - iOS: bundle id `com.orbacle.app`, `supportsTablet: false`.
  - Eklentiler: `expo-asset`, `expo-splash-screen`.
  - EAS entegrasyonu için `extra.eas.projectId` tanımlıdır.
- [eas.json](eas.json): üç derleme profili (`development`, `preview`, `production`) ve boş bir `submit.production` yer tutucusu.
- [babel.config.js](babel.config.js): `babel-preset-expo` + `react-native-reanimated/plugin`.
- [tsconfig.json](tsconfig.json): `expo/tsconfig.base`'i genişletir; `strict: true`.
- Kullanıcıya yönelik ayarlar (AsyncStorage üzerinde saklanır):
  - `@orbacle_language`: `"en" | "tr"`.
  - `@orbacle_haptics`: `"true" | "false"` (öntanımlı olarak açık; `"false"` dizgesi dışındaki her değer açık kabul edilir).
  - `@orbacle_history`: en fazla 20 `HistoryItem` girdisi içeren JSON dizisi.

## Mimari / Temel Bileşenler

[App.tsx](App.tsx) içindeki en üst düzey kompozisyon:

```
ErrorBoundary
└── SafeAreaProvider
    └── I18nProvider
        └── NavigationContainer
            ├── StatusBar (style="light")
            └── AppNavigator
                └── Tab.Navigator (alt sekmeler: Home / History / Settings)
```

- **ErrorBoundary**: en dış katmanda class component hata sınırı; render hatası yakalandığında "Try again" sıfırlama düğmeli bir yedek ekran gösterir.
- **I18nProvider** ([src/i18n/index.ts](src/i18n/index.ts)):
  - AsyncStorage'dan saklı dili okur; yoksa cihaz dilini algılayıp yazar.
  - Context üzerinden `{ language, t, setLanguage, ready }` sunar.
  - `ready`, ilk render'ı kapılar; kök bileşen, `ready === true` olana kadar Expo splash'in kapatılmasını erteler.
- **AppNavigator**: alt sekme navigatörü. Sekme ikonları `allowFontScaling={false}` ile render edilen emoji glifleridir (`🔮`, `📜`, `⚙️`); aktif/pasif ton renkleri `colors.primaryLight` ve `colors.textMuted`'ten gelir; sekme yüksekliği `56 + altGüvenliAlan`.
- **HomeScreen** ([src/screens/HomeScreen.tsx](src/screens/HomeScreen.tsx)):
  - Etkileşim durumunu tutar (`question`, `answer`, `isAnimating`, `emptyHint`).
  - Animasyon kontrolü için `useOrbAnimation`, cevap seçimi için `getRandomAnswer(language)` kullanır.
  - Açığa çıkan her cevabı `addHistoryItem` aracılığıyla geçmişe yazar.
  - `KeyboardAvoidingView` (iOS `padding`, Android varsayılan) ve bir `LinearGradient` arka plan kullanır.
- **HistoryScreen** ([src/screens/HistoryScreen.tsx](src/screens/HistoryScreen.tsx)): odaklanıldığında (`useFocusEffect` ile) geçmişi tekrar okur ve bir `FlatList` render eder; liste boşsa boş durum görünümünü gösterir.
- **SettingsScreen** ([src/screens/SettingsScreen.tsx](src/screens/SettingsScreen.tsx)): dil seçici, titreşim anahtarı, `ConfirmModal`'lı geçmişi temizle düğmesi ve statik hakkında/katkı/sürüm bloğu.
- **Orb** ([src/components/Orb.tsx](src/components/Orb.tsx)) şu öğeleri birleştirir:
  - Dört adet sürüklenen `Mist` katmanı (asimetrik ölçekleme, sinüs easing, opaklık interpolasyonu).
  - Kademeli gecikmelerle yedi `Sparkle` noktası ve sequence tabanlı opaklık/ölçek.
  - İki eş merkezli aura katmanı (dış + iç) ve radyal benzeri gradyanlar.
  - Bir parıltı güçlendirici, dokunma patlaması katmanı ve bir zemin sisi elipsi.
  - Ortada, erişilebilirlik öznitelikleri olan bir `Pressable` içine sarılmış bir `Image` (`assets/orb.webp`).
- **useOrbAnimation** ([src/hooks/useOrbAnimation.ts](src/hooks/useOrbAnimation.ts)):
  - Beş Reanimated shared value (`idlePulse`, `glowIntensity`, `burstScale`, `orbScale`, `isRevealing`).
  - Sinüs ile interpolasyona uğrayan sonsuz bir idle nabız döngüsü.
  - `triggerReveal(onComplete)`, 1400 ms süren sıralı bir açılış sekansı (parıltı atışları, ölçek yaylanmaları, patlama halkası) çalıştırır ve ardından `onComplete`'i bir `setTimeout` ile çağırır; bu zamanlayıcının referansı sökümde veya sıfırlamada temizlenir.
  - `resetAnimation`, bekleyen zamanlayıcıyı temizler ve parıltı/açılış durumunu sönümler.
- **PrimaryButton**: iki varyant — `filled` (gradyan) ve `ghost` (kenarlıklı yarı saydam). Öntanımlı varyant `ghost`.
- **LanguageSwitcher**: aktif/pasif stillere sahip iki bölmeli EN/TR kontrolü.
- **QuestionInput**: tek satır `TextInput` (en fazla 200 karakter), paletteki yer tutucu ve seçim rengi, Android'e özel dikey padding ayarı.
- **AnswerCard**: etiket + tırnak içinde cevabı gösteren fade-in kart.
- **ConfirmModal**: arka plana dokunarak iptal eden, iptal/onayla düğmeli, tam ekran saydam modal.

## Veri Akışı / Çalışma Zamanı Akışı

Soğuk başlatma:

1. Modül yüklemesi sırasında (React mount olmadan önce) `SplashScreen.preventAutoHideAsync()` çağrılır ve native splash ekranı tutulur.
2. `App`, `ErrorBoundary → SafeAreaProvider → I18nProvider → NavigationContainer → AppNavigator` ağacını render eder.
3. `I18nProvider`'ın effect'i `@orbacle_language`'i okur. Set edilmişse o dili benimser; aksi halde cihaz yerel ayarını `en` veya `tr` olarak eşleyip saklar. Her iki durumda da `ready` `true` olur.
4. `AppNavigator`, `!ready` iken `null` döndürür; sonra sekme navigatörünü render eder ve `SplashScreen.hideAsync()` ile splash'i gizler.

Soru etkileşimi (`HomeScreen` üzerinde):

1. Kullanıcı, `QuestionInput`'a bir soru yazar ve küreye dokunur.
2. Trimlenmiş soru boşsa → `enter_question` ipucunu göster, (açıksa) uyarı titreşimi tetikle, çık.
3. Değilse: klavyeyi kapat, `isAnimating = true` yap, önceki animasyonu/cevabı sıfırla.
4. `@orbacle_haptics`'i oku; açıksa orta şiddette bir titreşim gönder.
5. `triggerReveal` açılış animasyonunu başlatır ve geri çağrısını 1400 ms sonrasına planlar.
6. Tamamlandığında: aktif dil havuzundan tekrarsız rastgele bir cevap seç, state'e yaz, (açıksa) başarı titreşimi gönder ve `addHistoryItem` AsyncStorage'a yeni bir `HistoryItem` yazar.
7. `HomeScreen`, yardım metni + girişi `AnswerCard` + eylemler (`share`, `ask_another`) ile değiştirir.
8. `share`, yerelleştirilmiş bir önekle `Share.share`'i kullanır; `ask_another`, `answer`/`question`'ı temizler ve animasyonu sıfırlar.

Geçmiş etkileşimi:

- Odaklanıldığında `HistoryScreen`, `getHistory()`'i çağırır; bu da `@orbacle_history`'i okur, her girdiyi doğrular (`id`, `question`, `answer` dizge; `timestamp` sayı; `language` `{en, tr}`'den biri), bozuk girdileri atar (ve temizlenmiş listeyi tekrar yazar) ve tipli bir dizi döndürür.
- Her satır; soruyu, tırnaklı cevabı ve yerel ayara göre biçimlendirilmiş tarihi render eder.
- Ayarlar'daki "Geçmişi Temizle", `ConfirmModal`'ı açar; onaylandığında `clearHistory()` anahtarı siler.

Ayarlar etkileşimi:

- `getHapticsEnabled()`, mount sırasında anahtara ilk değerini verir (anahtar yoksa veya okunamıyorsa öntanımlı `true`).
- Anahtar değişince `saveHapticsEnabled(value)` tetiklenir.
- `LanguageSwitcher`, `setLanguage(lang)`'i çağırır; bu da context state'ini günceller ve AsyncStorage'a yazar.

## Sınırlamalar / Mevcut Durum

Doğrudan koddan ve repo durumundan çıkarılan gözlemler:

- **Tasarım olarak tamamen çevrimdışı**: ağ çağrısı, uzak yapılandırma, analitik veya kilitlenme raporlama entegrasyonu yoktur. `ErrorBoundary`, ileride Sentry/Crashlytics eklenebileceğine dair bir yorum içerir ancak şu anda böyle bir entegrasyon bulunmamaktadır.
- **Yalnızca Expo managed workflow**: repoda `android/` veya `ios/` native proje dizini yoktur; derlemeler `expo run:*` veya EAS ile üretilir. Son commit'lerden biri, native Android derleme dizininin açıkça kaldırıldığını gösterir.
- **Geçmiş üst sınırı**: 20 öğe olarak sabit kodlanmıştır; taşma durumunda eski girdiler sessizce düşer.
- **Cevap havuzları**: dil başına 363 girdiyle sabittir ve JS bundle'ıyla birlikte paketlenir. Çalışma zamanında cevapları genişletme, güncelleme veya sunucudan çekme mekanizması yoktur.
- **Dil kapsamı**: tam olarak iki dil (`en`, `tr`). Türkçe dışındaki cihaz yerel ayarları İngilizceye geri düşer.
- **Tek satırlık giriş**: sorular 200 karakterle sınırlıdır ve birden fazla satıra yayılamaz.
- **Otomatik test yoktur**: repo herhangi bir test dosyası, test çalıştırıcı yapılandırması veya CI iş akışı içermez.
- **Web hedefi**: bir `web` komutu mevcuttur (`expo start --web`), ancak uygulama mobil için ayarlıdır (sekme çubuğu, güvenli alanlar, titreşim, native paylaşım sayfası). Web davranışı kodda açıkça doğrulanmamıştır.
- **iOS cihaz dili algılama**: doğrudan `NativeModules.SettingsManager` kullanılır; bu tek dil algılama yoludur ve `expo-localization`'a dayanmaz.
- **Global rastgele durum**: `getRandomAnswer`, `lastAnswer`'ı modül düzeyinde bir değişkende tutar; bu nedenle "ardışık tekrar yok" koruması tek bir uygulama oturumundaki tüm çağrılar arasında paylaşılır ve süreç yeniden başlatıldığında sıfırlanır.

31) ## 🇬🇧 English

Bu dokümanın İngilizce versiyonu için [README.md](README.md)

> Not: Bu doküman İngilizce versiyondan türetilmiştir ve zaman zaman geride kalabilir.

---

# Vardiya Planı (Zekra)

Vardiyalı çalışanlar için yerel-öncelikli (local-first) bir React Native /
Expo mobil uygulaması. Uygulama; tek bir kullanıcının kendi vardiya
rotasyonunu aylık bir takvim üzerinde görmesini, planlamasını ve
düzenlemesini sağlar. Tüm veriler yalnızca cihazda saklanır; giriş, bulut
senkronizasyonu veya herhangi bir backend yoktur.

Depo kök dizininde planlama dokümanları ve örnek bir ekran görüntüsü
bulunur. Gerçek uygulama kaynak kodu [vardiya-plani/](vardiya-plani/)
klasörü içindedir.

---

## Genel Bakış

**Ne yapıyor**

- Ana sayfada bugünün vardiyasını gösterir ("bugün sabah, öğle, gece mi,
  izin mi?"), 7 günlük bir önizleme ve küçük bir "akıllı ipucu" satırı
  sunar (ör. üst üste çalışılan gün sayısı, yaklaşan izin günü).
- Döngüsel bir şablondan
  (ör. `OFF1, OFF2, 0715, 0715, 1523, 1523, 2307, 2307`) ay boyutunda
  vardiya dağılımı üretir ve ay geçişlerinde döngü sürekliliğini korur.
- İstenen bir tarih aralığını tek vardiya ile doldurma veya aynı
  aralığı bir şablonla yeniden üretme (revize) imkânı verir.
- Tek bir günü elle düzenlemeye izin verir: vardiya değiştirme, o güne
  özel başlangıç/bitiş saati, not ekleme ve günü "kilitli" işaretleme
  (otomatik işlemler bu günü atlar).
- Program şablonlarını yönetmeye imkân tanır (4 hazır sistem şablonu
  gelir: `BYG-A1`, `BYG-B1`, `BYG-C1`, `BYG-D1`; kullanıcı yeni şablon
  ekleyebilir, düzenleyebilir, silebilir).
- Açık ve koyu temayı destekler; arayüz Türkçedir ve Türkçeye duyarlı
  metin işleme içerir (`i/İ/ı/I`, aylar, haftanın günleri).
- Mevcut planı Excel ile açılabilen UTF-8 CSV olarak dışa aktarır ve
  sistem paylaşım sayfası üzerinden paylaşır (`expo-sharing`).
- Tüm yerel verileri silip varsayılanlara döndürmeyi destekler.

**Kapsam**

- Tek kullanıcı, tek cihaz, çevrim dışı.
- Türkçe arayüz (etiketler, ay/gün isimleri, CSV başlıkları Türkçedir).
- Expo aracılığıyla Android ve iOS'u hedefler; `app.json` içinde bir web
  hedefi tanımlı olsa da birincil kullanım senaryosu değildir.
- Mevcut kodda açıkça **yer almayan** şeyler: kimlik doğrulama, hesap,
  bulut senkronizasyonu, takım yönetimi, OCR, AI/ML veya uygulama verisi
  için herhangi bir ağ I/O'su.

---

## Özellikler

Aşağıdaki özellikler kaynak kodda mevcuttur.

### Ana Sayfa (`app/(tabs)/index.tsx`)
- Türkçe tarih biçimi ile "Bugünün vardiyası" büyük kart
  (`TodayShiftCard`).
- 7 günlük yakın tarihler şeridi (`UpcomingDays`).
- Akıllı ipucu satırı (`SmartInsight`) şu durumlardan birini gösterir:
  - N gün üst üste çalışma,
  - "Sonraki izin: X gün sonra" (yaklaşan izin varsa),
  - "Sonraki mesai: <gün etiketi>" (bugün izinse).
- İki hızlı eylem butonu: takvime git, yeni plan oluştur.

### Takvim Ekranı (`app/(tabs)/calendar.tsx`)
- Türkçe haftanın gün başlıklarıyla ay ızgarası (grid oluşturucuda gün
  indeksi düzeltmesiyle Pazartesi başlangıçlı).
- Önceki/sonraki ay gezintisi ve "bugün" kısayolu
  (`goToPreviousMonth`, `goToNextMonth`, `goToToday`).
- Her hücre (`DayCell`) vardiya rengini ve kısa adını gösterir; hücreye
  dokunmak gün düzenleme ekranına (`/day/[date]`) götürür.
- Alt eylem çubuğunda "Oluştur" butonu.

### Gün Düzenleme Ekranı (`app/day/[date].tsx`)
- Ekran oluşturulmadan önce route parametresi olan `date` alanını ISO
  `YYYY-MM-DD` formatına göre doğrular.
- Görünür vardiya türlerinden seçim yaptırır (dahili `OFF1`/`OFF2`
  döngü varyantları gizlenir; tek bir "Off" seçeneği gösterilir).
- Opsiyonel, güne özel başlangıç/bitiş saati
  (`customStartTime`, `customEndTime`) `normalizeCustomTime` ile
  normalize edilir.
- Serbest metin not alanı.
- `isLocked` değerine eşlenen "Korumalı" anahtarı.
- Günü depodan kaldıran silme işlemi.

### Ay Oluştur (`app/generate.tsx`)
- Hedef ay ve başlangıç noktası (`month_start` ya da `today`) seçilir.
- Aralık ön-ayarları: bu ay, sonraki 3 ay, sonraki 6 ay, yıl sonuna
  kadar.
- Seçilen şablondan başlanır; kullanıcı şablonun okunabilir özetini
  (ör. "2 sabah, 2 öğle, 2 gece, 2 izin") önizleyebilir.
- Scheduling engine'in preview fonksiyonu üzerinden kaç günün
  üretileceği ve kaç günün (kilitli/manuel koruma nedeniyle)
  atlanacağı gösterilir.

### Aralık Revize (`app/revise.tsx`)
- Ekrandaki aylık seçiciyle serbest bir başlangıç/bitiş tarihi seçilir.
- İki mod:
  - `single_shift`: aralığı tek bir vardiya kodu ile doldur.
  - `from_template`: aralığı seçilen şablonla yeniden üret.
- Her işlem için `overrideLocked` ve `overrideManual` anahtarları.

### Şablonlar (`app/templates/index.tsx`, `app/templates/[id].tsx`)
- Tüm şablonları (varsayılanlar + kullanıcı oluşturduğu) kısa örüntü
  özetiyle listeler ve aktif olanı işaretler.
- Detay ekranı şablonun adı, döngü uzunluğu ve vardiya kodu listesi
  üzerinde düzenleme yaptırır; ayrıca yeni şablon oluşturma ve
  kullanıcıya ait şablonu silme imkânı sunar.

### Ayarlar (`app/(tabs)/settings.tsx`)
- Tema seçimi: Açık / Koyu.
- Şablonlar ekranına gitme (aktif şablonu gösterir).
- **Verileri Dışa Aktar** — UTF-8 BOM başlıklı bir CSV üretir
  (`zekra-vardiya-YYYY-MM-DD.csv`). Sütunlar: Tarih, Gün, Vardiya,
  Saat Başlangıç, Saat Bitiş, Not, Korumalı. Dosya uygulamanın cache
  dizinine yazılır ve `expo-sharing` ile sistem paylaşım sayfası
  üzerinden açılır.
- **Tüm Verileri Sil** — onay ister ve repository üzerinde
  `clearAllData()` çağırır, ardından varsayılanları yeniden ekler.
- Hakkında bölümünde sürüm (`1.0.0`) ve jenerik bilgi.

### Vardiya Türleri (sistem varsayılanları, `src/constants/shifts.ts`)
Kullanıcıya görünür 5 vardiya türü ve 2 dahili OFF varyantı:

| Kod    | Ad    | Saat        | Çalışma | Gece aşan |
|--------|-------|-------------|---------|-----------|
| 0715   | Sabah | 07:00–15:00 | evet    | hayır     |
| 1523   | Öğle  | 15:00–23:00 | evet    | hayır     |
| 2307   | Gece  | 23:00–07:00 | evet    | evet      |
| OFF    | Off   | —           | hayır   | hayır     |
| OFF1   | Off   | —           | hayır   | hayır     |
| OFF2   | Off   | —           | hayır   | hayır     |

`OFF1` ve `OFF2` ayrı kodlar olarak saklanır; böylece döngü rotasyonda
birinci ve ikinci izin gününü ayırt edebilir. Arayüz bunları tek bir
"Off" girdisine indirir (`isOffCode` üzerinden).

### Varsayılan Program Şablonları (`DEFAULT_TEMPLATES`)
Dördünün de `cycleLength` değeri 8'dir:

- **BYG-A1** — `OFF1, OFF2, 0715, 0715, 1523, 1523, 2307, 2307`
- **BYG-B1** — `2307, OFF1, OFF2, 0715, 0715, 1523, 1523, 2307`
- **BYG-C1** — `OFF2, 0715, 0715, 1523, 1523, 2307, 2307, OFF1`
- **BYG-D1** — `0715, 1523, 1523, 2307, 2307, OFF1, OFF2, 0715`

İlk açılışta aktif şablon `BYG-A1`'dir.

### Scheduling Engine (`src/services/schedulingEngine.ts`)
- `generateDays` — `preserveLocked` ve `preserveManual` koruma
  bayraklarına uyarak bir tarih aralığı için planlı günler üretir.
- `generateSchedule` — `generateDays`'i bir sonuç nesnesi
  (`success / daysGenerated / daysSkipped / errors`) ile sarmalar.
- `calculatePhaseForNewMonth` — yeni bir ayın başlangıç döngü offset'ini
  önceki günün `cycleIndex` değerinden hesaplar; cycleIndex yoksa
  şablon içinde son vardiya kodunu arayan fallback mekanizmasını
  kullanır.
- `calculatePhaseForAlignment` — belirli bir vardiya kodunu belirli bir
  tarihe denk getirecek offset değerini çözer (onboarding tarzı
  hizalama için).
- `reviseRange` — yukarıdaki iki revize modunu günlük
  `overrideLocked` / `overrideManual` kontrolleriyle uygular.
- `previewGeneration` / `previewRevision` — depoyu değiştirmeden
  aralık için istatistik döndürür.
- `isOvernightShiftByTime` / `formatOvernightShiftDisplay` — gece
  aşan vardiyalar için yardımcılar.

Engine'de kodlanmış alan kuralları:
- Gece aşan vardiyalar **başlangıç** tarihine aittir (ör. 3 Mart'ta
  23:00–07:00 vardiyası 3 Mart'a yazılır, 4 Mart'a değil).
- Kilitli günlere otomatik işlemler asla dokunmaz; manuel günler
  varsayılan olarak korunur.
- Aylar arası döngü sürekliliği her üretilen/revize edilen güne
  kaydedilen `cycleIndex` üzerinden sağlanır.

### Kalıcı Depolama (`src/repositories/`)
- `IScheduleRepository` (`types.ts`) — planlı günler, vardiya türleri,
  şablonlar, ayarlar ve veri yönetimi (`exportAllData`, `clearAllData`,
  `initializeDefaults`) için tamamen senkron bir repository arayüzü.
- `FileRepository` (`fileRepository.ts`) — `expo-file-system` (v55 yeni
  API: `File` / `Paths`) kullanan üretim uygulaması. Belleğe alınmış bir
  cache tutar ve tüm durumu `Paths.document/vardiya-plani-data.json`
  altında tek bir JSON dosyası olarak yazar. İlk erişim senkron
  `file.textSync()` okumasını tetikler, böylece React render edilmeden
  önce store zaten dolu olur. Global anahtarlı singleton Metro hot
  reload'u atlatır.
- `MemoryRepository` (`memoryRepository.ts`) — aynı arayüze sahip,
  kalıcı olmayan uygulama. Birim testlerde kullanılır ve
  `createTestRepository()` aracılığıyla erişilir.
- `index.ts` uygulama için `getRepository()` fonksiyonunu
  `getFileRepository()`'ye bağlar; testler memory varyantını doğrudan
  kullanır.

Saklanan JSON içinde ileride geriye dönük uyumluluk için bir
`version: 1` alanı vardır; şu an için migrasyon kodu gerekmemektedir.

### Durum Yönetimi (`src/stores/scheduleStore.ts`)
- Dosya repository'sinden **modül yüklenirken senkron olarak** hidrate
  edilen bir Zustand store. İlk render'da vardiyalar, şablonlar, planlı
  günler ve ayarlar hazırdır.
- Planlı günler, vardiya türleri, şablonlar ve ayarlar için CRUD
  eylemlerini; ayrıca `generateMonth`, `reviseRange`, `exportData`,
  `clearAllData` ile UI gezintisi yardımcılarını (`goToPreviousMonth`,
  `goToNextMonth`, `goToToday`, `setSelectedDate`, `setViewMonth`)
  dışa açar.
- Selektörler: `selectTodayShift`, `selectActiveTemplate`,
  `selectShiftType`, `selectViewMonthDays`.

### Tema (`src/context/ThemeContext.tsx`)
- `ThemeProvider`, store'dan `settings.theme` değerini okur ve bir
  `ThemeColors` nesnesi ile `isDark` bayrağını `useTheme()` aracılığıyla
  sunar.
- İki palet (`lightTheme`, `darkTheme`) — semantik renk yuvalarıyla
  (arka plan, yüzey, metin, primary, danger, warning, vs.).

### Splash Yaşam Döngüsü (`src/utils/splashController.ts`,
`app/_layout.tsx`)
- `preventAutoHide()` modül değerlendirmesi sırasında, React monte
  olmadan önce çağrılır ve native splash ekranını sabitler.
- İlk görünür ekran (Ana Sayfa), `onLayout` içinde
  `notifyFirstScreenReady()` çağırır; bir animasyon kare sonrası splash
  dismiss edilir.
- Root layout'ta çalışan 4 saniyelik bir watchdog, ilk ekran sinyal
  verememesi durumunda bile splash'ın gizleneceğini garanti eder.
- `src/utils/startupTimer.ts` içindeki `startupMark()`, başlangıç yolu
  boyunca adlandırılmış zaman damgaları kaydeder.

### Türkçe İşleme (`src/utils/turkish.ts`, `src/utils/date.ts`)
- `toUpperTR` / `toLowerTR` / `compareTR`, `tr-TR` locale kullanır;
  böylece `i/İ/ı/I` dönüşümü doğru çalışır.
- `includesTR` / `startsWithTR` Türkçeye duyarlı içerme / önek
  yardımcılarıdır.
- `TURKISH_MONTHS`, `TURKISH_WEEKDAYS`,
  `TURKISH_WEEKDAYS_MONDAY_START` ile `getMonthNameTR` /
  `getWeekdayNameTR` yerel isimleri sağlar.
- Tüm tarih biçimlendirmesi `date-fns` + `tr` locale üzerinden geçer
  (`formatDateTR`, `formatDateShortTR`, `formatMonthYearTR`,
  `formatWeekdayTR`).

### Dışa Aktarma Biçimi
UTF-8 BOM'lu ve Türkçe yerelleştirilmiş sütun başlıklarına sahip CSV.
Notlar tırnaklanarak kaçış yapılır; böylece içinde gömülü virgül/tırnak
olması parser'ı bozmaz. Dosya adı UTC yerine yerel tarihi kullanır, yani
"bugün" kullanıcının saat dilimiyle eşleşir.

### Testler
- `src/services/schedulingEngine.test.ts`,
  `src/repositories/fileRepository.test.ts` ve
  `src/utils/turkish.test.ts` Vitest ile yazılmıştır
  (`environment: 'node'`). `FileRepository`, ortak arayüz üzerinden
  bellekte test edilir.

---

## Proje Yapısı

```
zegra/
├─ README.md                        # bu dosyanın İngilizce versiyonu
├─ README.tr.md                     # bu dosya
├─ vardiya-plani-v1-final.md        # orijinal planlama dokümanı (TR)
├─ eski-planlar/                    # eski planlama taslakları (TR)
├─ komut.txt                        # Expo / Gradle komut notları
├─ ornek-program.jpeg               # referans ekran görüntüsü
├─ Kayit1.mp4                       # demo kaydı
├─ Çalışma Programı 2026.xlsx       # kaynak iş çizelgesi (Excel)
└─ vardiya-plani/                   # uygulama
   ├─ app/                          # expo-router route'ları
   │  ├─ _layout.tsx                # root Stack + tema + splash bağlantısı
   │  ├─ (tabs)/                    # alt sekme grubu
   │  │  ├─ _layout.tsx             # Ana Sayfa / Takvim / Ayarlar
   │  │  ├─ index.tsx               # ana sayfa
   │  │  ├─ calendar.tsx            # aylık takvim
   │  │  └─ settings.tsx            # ayarlar + CSV dışa aktarma + silme
   │  ├─ day/[date].tsx             # tek gün düzenleme (modal)
   │  ├─ generate.tsx               # şablondan ay oluşturma
   │  ├─ revise.tsx                 # tarih aralığı revize etme
   │  └─ templates/
   │     ├─ index.tsx               # şablon listesi
   │     └─ [id].tsx                # şablon detay / düzenleme
   ├─ src/
   │  ├─ components/
   │  │  ├─ calendar/               # CalendarHeader, Grid, DayCell, Actions
   │  │  ├─ home/                   # TodayShiftCard, UpcomingDays,
   │  │  │                          #   QuickActions, SmartInsight
   │  │  └─ ui/                     # PressableScale
   │  ├─ constants/shifts.ts        # varsayılan vardiya türleri + şablonlar
   │  ├─ context/ThemeContext.tsx   # açık/koyu tema
   │  ├─ repositories/              # IScheduleRepository, File + Memory
   │  ├─ services/schedulingEngine.ts
   │  ├─ stores/scheduleStore.ts    # Zustand store (modül yüklenirken hidrate)
   │  ├─ types/index.ts             # alan tipleri
   │  └─ utils/                     # date, turkish, splashController,
   │                                #   startupTimer, shiftTime
   ├─ assets/                       # ikonlar, splash
   ├─ app.json                      # Expo yapılandırması (isim, ikon, pluginler)
   ├─ eas.json                      # EAS build profilleri
   ├─ babel.config.js
   ├─ tsconfig.json                 # strict TS, @/* → src/*
   ├─ vitest.config.ts              # node ortamı, @ alias
   ├─ package.json
   └─ android-sdk-setup-note.md     # Android SDK kurulum notu (TR)
```

---

## Kullanılan Teknolojiler

[vardiya-plani/package.json](vardiya-plani/package.json) içinde
tanımlanmıştır:

- **Runtime / UI** — React 19.2.4, React Native 0.83.2, Expo SDK 55
  (`expo`, `expo-router`, `expo-splash-screen`, `expo-status-bar`,
  `expo-linear-gradient`, `expo-linking`, `expo-localization`,
  `expo-constants`, `expo-dev-client`).
- **Gezinti** — `expo-router` (dosya tabanlı, `experiments.typedRoutes`
  ile tipli route'lar) ve `@react-navigation/native`.
- **Durum** — `zustand` ^5 (tek store, `scheduleStore`).
- **Kalıcılık** — `expo-file-system` v55 yeni `File` / `Paths` API'si
  (`textSync` / `write`), tek JSON dosyası.
- **Tarihler** — `date-fns` + Türkçe (`tr`) locale.
- **Safe area / screens** — `react-native-safe-area-context`,
  `react-native-screens`.
- **Dışa aktarma / paylaşım** — CSV paylaşım sayfası için
  `expo-sharing`.
- **Yeni mimari** — `app.json` içinde `newArchEnabled: true`.
- **Dil / build** — TypeScript ~5.9 (strict,
  `noUncheckedIndexedAccess`, `baseUrl: "."`, `@/*` → `src/*`),
  `babel-preset-expo`.
- **Test** — Vitest ^4, Node ortamı.
- **Dağıtım** — `eas.json` içinde `development`, `preview`,
  `production` (app bundle) ve `production-apk` profilleri.

Android paketi / iOS bundle identifier: `com.vardiyaplani.app`.
Expo project ID: `9db300d9-669f-474f-98cf-4d9184605880`.

---

## Kurulum

Komutlar `vardiya-plani/` altından çalıştırılır.

```bash
cd vardiya-plani
npm install
```

Gereksinimler (bulut build'ler için `eas.json` içinde tanımlıdır):
- EAS build'leri için Node 22.14.0. `package.json` yerel için bir Node
  sürümü sabitlemez, ancak Expo SDK 55 güncel bir LTS (18+) bekler.
- Expo CLI `npx expo` / `npx eas` üzerinden çağrılır.
- Native Android build için Android SDK kurulu olmalıdır; `local.properties`
  ve `ANDROID_HOME` ayarları için
  [vardiya-plani/android-sdk-setup-note.md](vardiya-plani/android-sdk-setup-note.md)
  (TR) dokümanına bakın.

---

## Kullanım

[vardiya-plani/package.json](vardiya-plani/package.json) içindeki
betikler:

```bash
# Geliştirme
npm run start                 # expo start (Metro dev sunucusu)
npm run android               # expo run:android (yerel native build)
npm run ios                   # expo run:ios
npm run web                   # expo start --web

# Testler
npm run test                  # vitest (watch)
npm run test:run              # vitest run (tek geçiş)

# EAS bulut build'leri
npm run build:android:preview     # dahili APK
npm run build:android:prod        # Play Store app bundle
npm run build:android:prod-apk    # production APK
npm run build:ios:preview         # dahili iOS
npm run build:ios:prod            # App Store iOS
npm run build:all:preview         # her iki platform, preview profili
```

Açıldıktan sonra temel in-app akış:

1. İlk açılışta aktif şablon `BYG-A1`'dir ve hiç planlı gün yoktur;
   ana sayfa "plan oluştur" çağrısı gösterir.
2. **Plan Oluştur**'a dokununca Ay Oluştur ekranı açılır; aralık
   ön-ayarı ve başlangıç noktası seçip onaylarsınız — ay dolar.
3. Takvim sekmesi ayı ızgara olarak gösterir; herhangi bir güne
   dokunmak gün düzenleme modalını açar.
4. Aralık değişiklikleri için Revize ekranını, şablon yönetimi için
   Şablonlar ekranını, CSV dışa aktarma / tema değişimi / tüm verileri
   silme için Ayarlar'ı kullanın.

---

## Yapılandırma

- **Expo uygulama yapılandırması** —
  [vardiya-plani/app.json](vardiya-plani/app.json):
  uygulama adı `Vardiya Planı`, slug `vardiya-plani`, scheme
  `vardiyaplani`, sadece dikey yönlendirme, yeni mimari etkin, adaptive
  ikon arka planı `#0B1026`, splash-screen plugin'i `assets/icon2.png`
  ile yapılandırılmış, `extra.eas.projectId` içinde Expo project ID
  gömülü.
- **EAS build profilleri** —
  [vardiya-plani/eas.json](vardiya-plani/eas.json) —
  `development` (dev client, internal), `preview` (internal APK /
  internal iOS), `production` (app bundle / store), `production-apk`
  (production APK varyantı). Build'lerde Node 22.14.0 zorlanır ve
  `base` env'e `NPM_CONFIG_LEGACY_PEER_DEPS=true` eklenir.
- **TypeScript** — strict mode, `noUncheckedIndexedAccess`, path alias
  `@/*` → `src/*`
  ([vardiya-plani/tsconfig.json](vardiya-plani/tsconfig.json)).
- **Vitest** — `environment: 'node'`, `@` alias `src/`
  ([vardiya-plani/vitest.config.ts](vardiya-plani/vitest.config.ts)).
- **Uygulama ayarları** (çalışma anında, yerelde saklanır;
  [vardiya-plani/src/types/index.ts](vardiya-plani/src/types/index.ts)):
  - `activeTemplateId`
  - `weekStartsOnMonday` (varsayılan `true`)
  - `theme` (`light` | `dark`)
  - `showOffVariants` (`OFF1`/`OFF2` ayrı ayrı mı gösterilsin; varsayılan
    olarak kapalıdır — UI bunları tek bir "Off" olarak birleştirir)
  - `onboardingDone`

Çalışma anında herhangi bir ortam değişkeni, `.env` dosyası veya uzak
uç nokta kullanılmaz.

---

## Mimari / Temel Bileşenler

Katmanlı, tek yönlü veri akışı:

```
              React ekranları & bileşenleri  (app/ + src/components/)
                              │
                              ▼
                  Zustand store — scheduleStore
                  (src/stores/scheduleStore.ts)
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
  Scheduling engine     Repository (sync)    Domain tipleri
  (saf fonksiyonlar)    IScheduleRepository  (src/types)
  src/services/…        src/repositories/…
                              │
                              ▼
                    FileRepository (JSON)
                    expo-file-system v55
                    Paths.document/vardiya-plani-data.json
```

- **Ekranlar** store'dan Zustand selektörleriyle veri okur ve aksiyon
  tetikler; doğrudan repository ile konuşmaz.
- **Store** bellekteki tek kaynak doğrulayıcıdır. Her değiştirici
  aksiyon, değişikliği aynı anda kalıcı hale getirmek için repository'yi
  de çağırır.
- **Scheduling engine** saf fonksiyonlardan oluşur; hem store
  (üretim/revize sırasında) hem de doğrudan testlerden çağrılır.
- **Repository** iki uygulaması olan bir arayüzdür. Üretim
  bağlamında `FileRepository` kullanılır; `MemoryRepository` testler ve
  dosya sistemi olmayan ortamlar içindir.
- **Tema**, store'daki `settings.theme` değerine abone olan küçük bir
  React context tarafından yönetilir; böylece tema değişimi anlık ve
  kalıcıdır.

### Alan Modeli (`src/types/index.ts`)

- `ShiftType` — `id, code, name, shortName, color, startTime, endTime,
  isWorking, isOvernight, isEditable`.
- `ProgramTemplate` — `id, name, cycleLength, steps[], isActive,
  isDefault`.
- `PlannedDay` — `date (ISO), shiftCode, isLocked, source
  ('generated' | 'manual' | 'revised'), templateId, note, cycleIndex?,
  customStartTime?, customEndTime?`.
- `AppSettings` — `activeTemplateId, weekStartsOnMonday, theme,
  showOffVariants, onboardingDone`.
- `GenerateOptions` / `GenerateResult` / `RevisionOptions` engine'i
  besler.
- `MonthlySummary` / `ShiftCount` aylık özet için tanımlanmıştır.

---

## Veri Akışı / İşlem Akışı / Runtime Akışı

### Soğuk Başlangıç

1. `scheduleStore.ts` import edilir — modül değerlendirmesi sırasında
   `getRepository()` çağrılır ve `FileRepository` singleton döner.
2. `FileRepository.load()` **senkron olarak** `file.textSync()` ile
   çalışır ve bellekteki diziler/map'ler doldurulur;
   `initializeDefaults()` dosya boşsa varsayılan vardiya türü / şablonu
   ekler.
3. İlk değerler `useScheduleStore` içine akar, böylece her selektör ilk
   render'da gerçek veriye sahiptir.
4. `app/_layout.tsx` modül yüklenirken `preventAutoHide()` çağırır;
   root layout 4 saniyelik splash watchdog'u başlatır.
5. İlk ekran (`app/(tabs)/index.tsx`) `onLayout` bildirir,
   `notifyFirstScreenReady()` çağrılır ve bir animasyon kare sonrasında
   native splash kapatılır.

### Ay Üretme

1. Kullanıcı Ay Oluştur ekranını açar, şablon, başlangıç noktası ve
   aralık seçer.
2. Store'un `generateMonth` fonksiyonu aktif şablonu bulur; önceki
   günün saklı `cycleIndex` değerinden `calculatePhaseForNewMonth` ile
   bir `phaseOffset` türetir (cycleIndex yoksa vardiya kodu üzerinden
   geriye dönüşe başvurur).
3. `generateDays`, tarih aralığını dolaşır; `preserveLocked && isLocked`
   ya da `preserveManual && source='manual'` koşullarını sağlayan
   günleri atlar; geri kalan her tarihe `source: 'generated'` ve
   `cycleIndex` içeren bir `PlannedDay` yazar.
4. `FileRepository.setPlannedDays` cache'i günceller ve tüm JSON
   dosyasını diske yazar; store kendi `plannedDays` map'ini günceller.

### Aralık Revize

1. Kullanıcı bir aralık ve mod (`single_shift` veya `from_template`)
   seçer, kilitli/manuel günlerin üzerine yazma anahtarlarını ayarlar.
2. `reviseRange`, `source: 'revised'` ile yeni `PlannedDay` nesneleri
   üretir; mümkünse mevcut `note` ve `cycleIndex` değerlerini korur.
3. Sonuçlar kalıcı hale getirilir ve store'a yansıtılır.

### Tek Gün Düzenleme

1. Gün düzenleme ekranı `:date` route parametresini doğrular.
2. Değişiklikler `setPlannedDay(day)` ile kaydedilir; bu aksiyon günü
   (bu ekrandan oluşturulmuşsa) `source: 'manual'` olarak ve isteğe
   bağlı `isLocked: true` olarak işaretler; `deleteDay` günü siler.
3. Güne özel başlangıç/bitiş saatleri `PlannedDay` üzerinde saklanır
   (`customStartTime`, `customEndTime`), vardiya türünde değil.

### CSV Dışa Aktarma

1. Ayarlar → Verileri Dışa Aktar.
2. `generateExcelContent()` BOM ön ekli, Türkçe başlıklı, yerelleştirilmiş
   gün adlarına sahip ve notları tırnaklanmış bir CSV üretir.
3. Dosya `Paths.cache/zekra-vardiya-<yerel-tarih>.csv` konumuna yazılır
   ve `Sharing.shareAsync` ile gösterilir.

### Gece Aşan Vardiya Kuralı

Bir vardiya, `startHour > endHour` ise gece aşan olarak kabul edilir
(`isOvernightShiftByTime` kontrol eder). Gece aşan vardiyalar depoda
her zaman **başlangıç** tarihine bağlanır; UI, bitiş saatini
`formatOvernightShiftDisplay` üzerinden gösterir
("<ad> (<bitiş>'e kadar)").

---

## Sınırlamalar / Mevcut Durum

- **Sadece yerel.** Tüm veri tek bir JSON dosyasında
  (`Paths.document/vardiya-plani-data.json`) saklanır. Bulut senkronu,
  içe aktarma veya çok cihazlı destek yoktur. "Dışa Aktar" bir CSV
  üretir; "Tüm Verileri Sil" tüm verileri siler ve varsayılanları
  yeniden ekler.
- **Türkçe arayüz.** Etiketler, CSV başlıkları, ay/gün isimleri ve
  "Bugün", "Yarın", "Sonraki mesai" gibi ifadeler Türkçedir. Intl'in
  kullandığı sistem locale'i dışında çalışma anında locale geçişi
  yoktur.
- **Dahili OFF1 / OFF2.** Bunlar döngü rotasyonunun ayırt edilebilir
  kalmasını sağlar ama gün düzenleme seçicisinde gizlenir; aktif UI
  tek bir "Off" girdisi gösterir. `showOffVariants` ayarı tiplerde ve
  varsayılan ayarlarda mevcuttur ancak mevcut ekranlarda bu bayrağı
  değiştiren bir arayüz unsuru yoktur.
- **Varsayılanlar her zaman kaldırılamaz.** `DEFAULT_SHIFT_TYPES` ve
  `DEFAULT_TEMPLATES`, ilgili liste boşsa `initializeDefaults()` ile
  yeniden eklenir. Kullanıcının oluşturduğu şablon silinebilir;
  varsayılanları "temizlemek" ise "Tüm Verileri Sil" işlemidir (onları
  yine geri yükler).
- **Onboarding akışı yok.** `settings.onboardingDone` alanı tiplerde ve
  varsayılan ayarlarda mevcut ancak mevcut route'larda bir onboarding
  ekranı tanımlı değildir.
- **Hooks klasörü boş.** `src/hooks/` klasörü var ama içinde dosya
  yok.
- **Hafta başlangıcı.** `settings.weekStartsOnMonday` varsayılan olarak
  `true`'dur ve takvim, ay ızgarası oluşturucusu tarafından Pazartesi
  başlangıçlı çizilir; bunu çalışma anında değiştiren bir UI anahtarı
  yoktur.
- **Native build önkoşulları.** `expo run:android` için Android SDK
  yapılandırılmalıdır (bkz.
  [vardiya-plani/android-sdk-setup-note.md](vardiya-plani/android-sdk-setup-note.md)).
- **Depo kök içeriği.** Kök dizin ayrıca planlama dokümanları (TR),
  referans bir ekran görüntüsü (`ornek-program.jpeg`), bir demo kaydı
  (`Kayit1.mp4`) ve varsayılan vardiya türlerine ilham veren kaynak
  Excel çizelgesini (`Çalışma Programı 2026.xlsx`) barındırır.
  Uygulamanın kendisi tamamen
  [vardiya-plani/](vardiya-plani/) altındadır.

---

## Notlar

- "Zekra", uygulama başlığında ve Ayarlar'ın Hakkında bölümünde görünen
  ürün/marka adıdır; paket / bundle identifier `com.vardiyaplani.app`
  ve uygulamanın görünen adı "Vardiya Planı"dır.
- Uygulama **yeni React Native mimarisini** hedefler (`app.json` içinde
  `newArchEnabled: true`), React 19.2 ve RN 0.83 kullanır.
- Store hidrasyonu bilerek senkrondur: store dosyayı modül yüklenirken
  okur ve böylece ilk render zaten dolu olur; native splash de ilk
  ekran ekrana geldiği anda kapatılabilir.
- Depo kökündeki `Kayit1.mp4` bir demo kaydıdır ve build ile ilgisi
  yoktur; `komut.txt`, yazar tarafından saklanan kısa komut notlarını
  (`expo prebuild`, `gradlew assembleDebug`, `eas build` varyantları)
  içerir.
